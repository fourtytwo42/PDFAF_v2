# Repository Memory

This file is durable context for agents working in this repository. Read it when starting work, after context compression, or when unsure about the current checkpoint, validation policy, or parked debt. Update it with concise facts that future agents should remember.

## Memory Rules

- Record durable decisions, accepted/rejected behavior, current best checkpoints, validation baselines, and parked debt.
- Do not record PDF payloads, Base64, large logs, generated benchmark JSON, or transient command output.
- Prefer exact stage numbers, run directories, commit hashes, and one-line conclusions.
- If a fact is uncertain, label it as uncertain and point to the local artifact that should be checked.

## Current Durable State

- Formal protected baseline remains `Output/experiment-corpus-baseline/run-stage42-full-2026-04-21-r7`.
- Use Node 22 and `--no-semantic` for deterministic benchmark validation unless semantic behavior is explicitly under test.
- Runtime policy: check/analyze-only work should default to a fast 15s budget (`PDFAF_CHECK_ANALYSIS_TIMEOUT_MS` / `REQUEST_TIMEOUT_ANALYZE_MS`), while remediation is capped by a 5-minute per-PDF wall guard (`PDFAF_REMEDIATION_PDF_TIMEOUT_MS` / `REQUEST_TIMEOUT_REMEDIATE_MS`) unless a deliberate special run raises it.
- Before any benchmark path that might spawn an LLM, check for an existing local `llama-server` or API listener and reuse it.
- Keep generated PDFs, benchmark runs, and diagnostic reports local unless explicitly promoted to source documentation.

## Recent Stage Memory

- All-input mean goal baseline is established for the new long-running `/goal`. Deduped `Input/` contains `351` unique regular PDFs from `432` regular PDF files (`482` raw PDF paths including symlinks/duplicates). Broad deterministic run root `Output/goal-all-input-mean-2026-05-09-r1` used eight symlink shards with four concurrent `baseline-corpus-batch.ts --no-semantic --no-pdfs` workers; it completed `351/351` with mean `88.52`, median `93`, grades `259 A / 41 B / 9 C / 9 D / 33 F`, `136` rows below `93`, and `1572` total points needed for mean `93`. Runtime mean/median/p95/max were `68487.6ms / 21842ms / 351416ms / 877593ms`. Lowest-40 POC strong-area diagnostic at `Output/goal-all-input-mean-2026-05-09-r1/poc-strong-lowest-40` found direct PAC-style fail hot spots: font CMap (`31` valid, `28` present), structure child roles (`21`), image tagging (`20`), table header association (`16`), text tagging (`9`), and header-cell association (`9`). Source doc `docs/all-input-mean-goal-baseline.md` and reusable script `scripts/all-input-mean-diagnostic.ts` capture the checkpoint. Do not make behavior changes from the broad run alone; next work should select focused heading/reading and table/header targets with object-level PAC/POC evidence, while keeping font/CMap diagnostic-only unless a separate stability stage proves safe scoring/remediation.
- All-input target selection is diagnostic-only. `scripts/all-input-target-selection-diagnostic.ts` combines the broad all-input score diagnostic with lowest-40 POC/PAC evidence and writes local output under `Output/goal-all-input-mean-2026-05-09-r1/target-selection-diagnostic`; source doc is `docs/all-input-target-selection-diagnostic.md`. It selects `heading_reading_recovery_target` as the first remediation lane (`18` lowest-40 rows, `650` deficit-to-93), with table/header as second lane (`9` rows, `266` deficit). Next behavior work should run a focused heading/reading object diagnostic on selected rows such as `v1-4716`, DUI laws/enforcement, `v1-4655`, `4567`, `4646`, `4614`, `4519`, `4583`, `4593`, and `3468`; inspect accepted/rejected heading tools, content marking, ParentTree ownership, and PAC-style content/tag leaves. Do not run all `Input/` again or broaden planner/scoring before this focused diagnostic proves a safe target.
- Focused heading/reading target run R1 is diagnostic-only. Target symlink dir `Output/goal-all-input-mean-2026-05-09-r1/focused-heading-reading-targets` and run `Output/goal-all-input-mean-2026-05-09-r1/run-focused-heading-reading-targets-2026-05-09-r1` processed 12 selected rows with PDFs enabled and no semantic AI; mean moved only `44.58 -> 57.58`, all 12 stayed below target, best was `4519...` at `79/C`, and `4614...` regressed `59/F -> 53/F`. POC/PAC pass over remediated PDFs at `Output/goal-all-input-mean-2026-05-09-r1/poc-strong-focused-heading-reading-r1` still found `44` failures: font CMap valid/present (`10/9` files, diagnostic-only for now), child roles (`8`), table header association/cells (`5/4`), annotation object refs (`2`), plus one parent-link and one page StructParents issue. Next work should inspect tool timelines/object evidence for these rows; do not add broad heading scheduler behavior from R1 alone.
- Focused heading/reading trace sample `Output/goal-all-input-mean-2026-05-09-r1/focused-heading-reading-traces-r1` is diagnostic-only. On `v1-4655` and `4646`, score/heading structural proposals (`create_heading_from_tagged_visible_anchor`, `repair_structure_conformance`, `synthesize_basic_structure_from_layout`, `create_heading_from_candidate`) are repeatedly rejected because intermediate states trigger `pdfua.annotations.tagged_annotations_present`; later annotation/link cleanup either regresses score or does not restore the sequence. `4614` shows mostly no-gain orphan/artifact or score-regression paths. `4519` has mixed figure/table progress but later repairs are blocked by table/figure PAC regressions, so it should not drive heading-only behavior. Next best diagnostic is a generalized structure-then-annotation sequencing probe, modeled on `figure-4702`, for non-figure rows where a rejected structural proposal improves total score and heading/reading evidence and a bounded annotation/link cleanup can make the final state PAC-safe; do not weaken annotation/orphan gates globally.
- Structure/annotation sequence diagnostic R1 is diagnostic-only. `scripts/all-input-structure-annotation-sequence-diagnostic.ts` reads trace rows and writes local output under `Output/goal-all-input-mean-2026-05-09-r1/structure-annotation-sequence-diagnostic-r1`; source doc is `docs/all-input-target-selection-diagnostic.md`. It finds exactly one sequence probe candidate in the sampled all-input heading tail: `0096-...-4646-youth-development...`, where `create_heading_from_candidate` projects `54/59 -> 79` and `heading_structure 0 -> 95`, blocked by `pdfua.annotations.tagged_annotations_present`, with cleanup attempts present and not score-regressive in the trace. `v1-4655` is not safe yet because cleanup attempts regressed score; `4614` lacks annotation-blocked score movement; `4519` is mixed table/figure PAC debt. Next behavior checkpoint, if pursued, should be row-scoped to `4646` and reuse existing structure-annotation sequence machinery with final PAC-safe reanalysis; no broad annotation/orphan gate change.
- The first `4646` row-scoped sequence behavior attempt was rejected/not kept. Targeted run `Output/goal-all-input-mean-2026-05-09-r1/run-structure-annotation-sequence-target-2026-05-09-r1` left `4646` at `59/F`; trace `Output/goal-all-input-mean-2026-05-09-r1/structure-annotation-sequence-target-trace-r1` showed the existing `figure-4702` sequence extension point cannot recover this row because the score-moving `54/59 -> 79` state exists only in rejected tool replay details, while the extension receives the stage-level analyzed state. The source behavior was backed out; keep only the diagnostic. If this lane continues, it needs a proposal-buffer sequencing probe that reruns one structural candidate and cleanup from that candidate buffer, with final PAC-safe reanalysis.
- Structure-4076 Table Evidence Isolation And Narrow Recovery is diagnostic-only; no behavior was kept. Added `scripts/structure4076-table-evidence-diagnostic.ts` and focused tests. Diagnostic artifact `Output/experiment-corpus-baseline/structure4076-table-evidence-diagnostic-2026-05-08-r1` classified `structure-4076` as `real_table_debt_no_safe_repair`: repeat analysis reproduced stable table target `81311_0` in two of three passes (`382` cells, `118` rows, `17` irregular rows, dominant column count `6`, no headers), while one pass saw no applicable table. A `normalize_table_structure` probe applied and improved headers `0 -> 1` plus irregular rows `17 -> 2`, but repeat analysis after mutation did not consistently keep both score/table evidence at the floor (`70/C` with table `72`, `70/C` with no applicable table, then `59/F` with no applicable table). Do not add the row-specific table recovery; park `structure-4076` as real table debt plus analyzer/applicability volatility. Fixed-50 can proceed only with this parked debt documented; `structure-4438` remains parked at `90/A`.
- Structure-4076 Reanalysis Drift Isolation is diagnostic-only. Added `scripts/structure4076-reanalysis-drift-diagnostic.ts` and focused tests; diagnostic artifact `Output/experiment-corpus-baseline/structure4076-reanalysis-drift-diagnostic-2026-05-08-r1` compares `structure-4076` in `run-orphan-mcid-recovery-target-2026-05-07-r1`. Result: in-run `70/C` drops to protected reanalysis `56/F`, but the row is classified `real_pdf_regression`, not safe analyzer drift, because protected reanalysis changes `table_markup` from `100`/not applicable to `0`/applicable and increases irregular/strongly-irregular table signals from `0` to `1`. Text/tag evidence is preserved; page evidence is unavailable in the benchmark row artifact. No row-specific checkpoint preservation behavior was added because it would hide checker-facing table debt. Do not run fixed-50 from this stage; next work should address or park `structure-4076` table evidence, while `structure-4438` remains parked at the `90/A` floor.
- Narrow Orphan-MCID Recovery And Fixture Route Isolation is implemented but not a fixed-50 checkpoint. `pacRuleUsefulRepairRecovery` now also allows `pdfua.content.orphan_mcids_absent` recovery for `create_heading_from_candidate`, `normalize_heading_hierarchy`, and `normalize_annotation_tab_order` when all PAC regressions are orphan-MCID-only, total score improves, the repaired category improves (`heading_structure` for heading tools; `link_quality` or `reading_order` for tab order), page count is preserved, text character count does not drop, and tagged state is not lost. Existing `repair_alt_text_structure` and `repair_native_link_structure` recoveries are preserved; no PAC scoring caps, PAC gate allow-list changes, checkpoint floor changes, timeout increases, planner broadening, or new repair tools were added. Fixture route diagnostic `Output/experiment-corpus-baseline/orphan-mcid-recovery-fixture-route-diagnostic-2026-05-07-r1` did not prove a new same-state fixture guard, so no fixture behavior was added. Targeted run `Output/experiment-corpus-baseline/run-orphan-mcid-recovery-target-2026-05-07-r1` processed 14 selected rows with `false_positive_applied = 0`, successful-row mean `94.00`, and PAC recovery diagnostic `Output/experiment-corpus-baseline/orphan-mcid-recovery-target-pac-recovery-2026-05-07-r1` showed `4` PAC gate rejections and `0` blocked useful repairs. Recovered rows include `short-4074 99/A`, `figure-4082 98/A`, `figure-4188 98/A`, `structure-4078 98/A`, `structure-4108 99/A`, and `structure-4122 99/A`; controls held (`fixture-inaccessible 97/A`, `structure-3775 97/A`, Teams `98/A`, `font-4035 99/A`, `long-4516 92/A`, `long-4683 92/A`). Do not run fixed-50 yet: `structure-4076` is `70/C` in-run but reanalyzes to `56/F`, and parked `structure-4438` still hard-times out with best checkpoint `36/F` below `90/A`. Next work should isolate `structure-4076` reanalysis drift, not broaden PAC policy.
- Fixed Original-50 Acceptance Validation after commit `610f558` is rejected. Full run `Output/experiment-corpus-baseline/run-structure3775-4076-drift-isolation-full-2026-05-07-r1` processed the fixed 50 with `false_positive_applied = 0`, but only `48/50` remediated successfully; Stage 41 gate `Output/experiment-corpus-baseline/structure3775-4076-drift-isolation-gate-2026-05-07-r1` failed with gate mean/median `89.42/96`, grades `36 A / 2 B / 2 C / 1 D / 7 F`, p95 `141081ms`, attempts `903`, and failed keys `analyze_success`, `remediate_success`, `route_summary_coverage`, `f_grade_count`, `protected_file_regressions`, `runtime_p95_wall`, and `total_tool_attempts`. `long-4516` recovered without hard timeout at `86/B`, `long-4683` completed at `91/A`, and `structure-3775` stayed `97/A`, but `fixture-inaccessible` repeated full-run route volatility at `79/C`, `structure-4076` hard-timed out with best checkpoint `56/F` below its `70/C` floor, and parked `structure-4438` timed out with best checkpoint `36/F` below `90/A`. Diagnostics: route `Output/experiment-corpus-baseline/structure3775-4076-drift-isolation-full-route-2026-05-07-r1`, runtime `...full-runtime-2026-05-07-r1`, PAC recovery `...full-pac-recovery-2026-05-07-r1`; PAC recovery found `100` PAC gate rejections and `80` blocked useful repairs, dominated by `pdfua.content.orphan_mcids_absent` (`70` rejections across `16` files). Next work should not be fixed-50 acceptance; design a narrow orphan-MCID gate recovery for score-moving heading/annotation repairs and separately re-isolate `fixture-inaccessible` route volatility. Keep `structure-4438` parked.
- Structure-3775 / Structure-4076 Drift Isolation is implemented but not a fixed-50 acceptance checkpoint. Diagnostic `Output/experiment-corpus-baseline/structure3775-4076-drift-diagnostic-2026-05-07-r1` proved a `structure-3775` same-state artifact-route drift at replay signature `e7922842490f3382c9ac42c8`: `artifact_repeating_page_furniture` had no score/category/link benefit and could route to a noisy PAC rejection instead of the known A-grade route. The orchestrator now rolls back that exact no-benefit mutation and records it as `no_effect` with `structure3775_artifact_route_no_effect_stabilized`; it also checks for eligible verified checkpoints before repeated figure-alt targets so `long-4516` does not strand a good checkpoint inside the multi-target loop. No PAC scoring caps, PAC gate allow-list changes, checkpoint floor changes, timeout increases, planner broadening, or new repair tools were added. Targeted validation `Output/experiment-corpus-baseline/run-structure3775-4076-drift-isolation-target-2026-05-07-r2` processed only the selected 10 rows with `false_positive_applied = 0`: `fixture-inaccessible 97/A`, `structure-3775 97/A`, `structure-4076 70/C`, `long-4516 92/A` with checkpoint return, `long-4683 96/A`, `long-4470 96/A`, Teams `98/A`, `font-4035 99/A`, and `font-4057` residual `69/D`; only `structure-4438` hard-timed out, with best checkpoint `36/F` below the `90/A` floor. The targeted subset is clean enough for fixed original-50 validation if the next step is acceptance measurement; keep `structure-4438` parked and keep treating `structure-4076` as analyzer/route volatility debt unless future same-state evidence proves a narrower fix.
- Long-4516 Checkpoint Terminalization is implemented but not a fixed-50 acceptance checkpoint. Benchmark full-mode final reanalysis now treats remediation `verified_checkpoint_timeout_return` as terminal, so returned checkpoints below the generic `85/B` final-reanalysis guard are not sent into another wall-time-risky final reanalysis. `scripts/pac-runtime-tail-diagnostic.ts` now classifies the broken `returned_checkpoint_then_timeout` trace shape separately. Targeted validation `Output/experiment-corpus-baseline/run-long4516-checkpoint-terminalization-target-2026-05-07-r1` processed only the selected 10 rows: `long-4516` recovered to `89/B` with `verified_checkpoint_timeout_return`, `long-4683` completed at `86/B` with checkpoint return, `fixture-inaccessible 97/A`, Teams `98/A`, `font-4035 99/A`, `long-4470 92/A`, and `false_positive_applied = 0`. Do not run fixed 50 from this stage yet: `structure-3775` repeated the volatile `79/C` route, `structure-4076` drifted to `60/D`, and `structure-4438` still hard-timed out with best checkpoint `36/F` below the `90/A` floor. Next work should isolate `structure-4076`/`structure-3775` route or analyzer drift, not weaken PAC policy or lower global checkpoint floors.
- Route Volatility Stabilization Before Fixed-50 is implemented but not an acceptance checkpoint. The fixture artifact-route guard now covers both `artifact_repeating_page_furniture` and `mark_untagged_content_as_artifact` when replay state is `1d49f4344e1db6615a17c1f8`, score is `79`, and there is no total-score/link-quality/checker-facing structural benefit. `scripts/pac-target-route-diagnostic.ts` now labels same-state outcome drift vs upstream state/tool-order/score/missing-event drift. Targeted validation `Output/experiment-corpus-baseline/run-route-volatility-stabilization-target-2026-05-07-r1` had `false_positive_applied = 0` and preserved `fixture-inaccessible 97/A`, `structure-3775 97/A`, Teams `98/A`, `font-4035 99/A`, `long-4470 96/A`, and `long-4683 96/A`; `font-4057` stayed known residual `69/D`. Do not run fixed 50 from this stage: `structure-4076` still drifted (`69/D` in-run, `56/F` reanalyzed), `structure-4438` still hard-timed out with best checkpoint `36/F`, and `long-4516` hard-timed out despite trace showing eligible `82/B` checkpoint plus `return:before_stage180_post_pass`. Next work should isolate `structure-4076` analyzer/route drift and close out why returned `long-4516` checkpoints still end in hard timeout.
- Long-4683 Timeout And Route-Volatility Isolation is implemented but not an acceptance checkpoint. `scripts/pac-target-route-diagnostic.ts` now classifies target rows (`long-4683`, `structure-3775`, `long-4470`, `font-4057`) as checkpoint probes, route volatility, final reanalysis drops, or residual debt. Diagnostic `Output/experiment-corpus-baseline/long4683-route-volatility-diagnostic-2026-05-07-r1` classified `long-4683` as `needs_behavior_probe` with good targeted `96/A` versus full-run timeout best checkpoint `80/B`; `structure-3775` as `route_volatility`; `long-4470` as `final_reanalysis_drop`; and `font-4057` as `residual_no_safe_fix`. The orchestrator now gives only `long-4683` a row-specific verified-timeout checkpoint floor of `80/B`; default remains `85/B`, `long-4516` remains `80/B`, `structure-4076` remains `70/C`, and `structure-4438` remains `90/A`. Targeted run `Output/experiment-corpus-baseline/run-long4683-route-volatility-target-2026-05-07-r1` had `false_positive_applied = 0`, recovered `long-4683 87/B` without hard timeout, preserved `long-4516 89/B`, `long-4470 96/A`, `structure-3775 97/A`, `fixture-inaccessible 97/A`, Teams `98/A`, and `font-4035 99/A`, but `structure-4076` drifted to `59/F` in-run / `48/F` reanalyzed, `font-4057` stayed `69/D`, and `structure-4438` still timed out with best checkpoint `36/F`. A second targeted repeat was stopped after `fixture-inaccessible` took the known bad `79/C` route. Do not run fixed 50 from this stage yet; next work should stabilize route volatility for `fixture-inaccessible`/`structure-3775` and recurring `structure-4076` drift before broader validation.
- Long-4516 Checkpoint Return And Structure-4076 Drift Isolation is implemented but not an acceptance checkpoint. The orchestrator now uses an earlier verified-checkpoint admission window before risky work so an eligible checkpoint can return before starting another tool/reanalysis/post-pass/final reanalysis near the 5-minute wall; existing floors remain default `85/B`, `structure-4076 >=70/C`, `long-4516 >=80/B`, and `structure-4438 >=90/A`. `scripts/pac-target-route-diagnostic.ts` now compares `structure-4076` drift; diagnostic `Output/experiment-corpus-baseline/pac-target-route-diagnostic-2026-05-07-r3` classifies `structure-4076` as route drift, not a proven same-state behavior bug. Targeted repeat `Output/experiment-corpus-baseline/run-long4516-checkpoint-return-target-2026-05-07-r2` recovered `long-4516 89/B` via `verified_checkpoint_timeout_return`, kept `long-4683 96/A`, `fixture-inaccessible 97/A`, `structure-3661 98/A`, `structure-3775 97/A`, and `font-4035 99/A`, with `false_positive_applied = 0`; `structure-4076` stayed below floor at `69/D`, `figure-4188` remained `59/F`, and `structure-4438` timed out with best checkpoint `36/F`. Fixed 50 run `Output/experiment-corpus-baseline/run-long4516-checkpoint-return-full-2026-05-07-r1` is not acceptance-ready: reanalyzed mean/median `88.9/97`, remediation success `48/50`, p95 wall `111286ms`, `long-4516 86/B` recovered, `structure-4076 70/C`, but `structure-4438` and `long-4683` timed out, `long-4683` best checkpoint was only `80/B` below default floor, `structure-3775` showed volatility, `long-4470` had a large final reanalysis drop, and `font-4057` stayed `69/D`. Stage 41 gate `Output/experiment-corpus-baseline/long4516-checkpoint-return-gate-2026-05-07-r1` failed on analyze/remediate success, route coverage, F count, protected regressions, runtime p95, and attempts. Keep the checkpoint-return behavior, do not lower `structure-4438` floor, and do not lower the default/`long-4683` floor without a separate safety plan; next work should isolate `long-4683`, `structure-3775`, `long-4470`, and `font-4057`.
- Fixture-Inaccessible Same-State Artifact Route Stabilization is implemented but not a full acceptance checkpoint. Added a narrow `mark_untagged_content_as_artifact` same-state guard for replay signature `1d49f4344e1db6615a17c1f8` at score `79` when score/link quality do not improve and no checker-facing structural benefit is present; it rejects with `fixture_inaccessible_artifact_route_stabilized`. Added a narrow `repair_native_link_structure` orphan-MCID PAC recovery only when all PAC regressions are `pdfua.content.orphan_mcids_absent` and total score or link quality improves; no PAC scoring caps, PAC gate allow-list changes, timeout changes, planner broadening, or new repair tools were added. Fixture smoke `Output/experiment-corpus-baseline/run-fixture-artifact-route-stabilization-smoke-2026-05-07-r3` recovered `fixture-inaccessible` `40/F -> 97/A` with native link repair applied. Targeted subset `Output/experiment-corpus-baseline/run-fixture-artifact-route-stabilization-target-2026-05-07-r1` did not pass acceptance: `fixture-inaccessible 97/A`, `long-4683 91/A`, controls mostly A, but `long-4516` and `structure-4438` hard-timed out and `structure-4076` landed after `69/D` / reanalyzed `56/F`; fixed 50 was not run. Next work should isolate why eligible `long-4516` checkpoint return did not happen before wall timeout and why `structure-4076` drifted below its prior `70/C` floor; keep `structure-4438` floor at `90/A` because best checkpoint remains `36/F`.
- Structure-4438 Feasibility And Fixture Route Stabilization is diagnostic-only; no remediation behavior changed. Added `scripts/pac-target-route-diagnostic.ts` plus timeout-trace `verifiedCheckpointHistory` instrumentation. Diagnostic `Output/experiment-corpus-baseline/pac-target-route-diagnostic-2026-05-07-r2` compares the good `fixture-inaccessible` route (`run-trace-driven-final-reanalysis-target-2026-05-06-r2`, `97/A`) to the bad checkpoint-recovery route (`run-verified-checkpoint-timeout-recovery-target-2026-05-07-r1`, `79/C`) and confirms route volatility: first divergence is `mark_untagged_content_as_artifact` rejected vs applied from the same replay state `1d49f4344e1db6615a17c1f8`; `repair_native_link_structure` applies only in the good route. Focused `structure-4438` repeat `Output/experiment-corpus-baseline/run-structure4438-feasibility-repeat-2026-05-07-r1` produced `6` checkpoint candidates, best `36/F`, all rejected as `checkpoint_below_floor(...<90)`, so keep hard-timeout behavior and do not lower the `structure-4438` `90/A` floor. Fixed 50 was not run. Next behavior work should be a narrow same-state artifact-route stabilization for `fixture-inaccessible`, not broad PAC/orphan-MCID or timeout changes.
- Verified Checkpoint Timeout Recovery is implemented but is not an acceptance checkpoint. The orchestrator now tracks eligible verified checkpoints after successful stage/post-pass reanalysis and, near the 5-minute remediation wall, can restore the best eligible checkpoint, truncate later tool rows, and record `verified_checkpoint_timeout_return`. Eligibility requires score improvement over input, floor `>=85/B` by default, targeted floors `structure-4076 >=70/C`, `long-4516 >=80/B`, and `structure-4438 >=90/A`, plus no page/text/tag/structure regression, no mutation-truth false-positive-applied evidence, and no harmful PAC gate regression. PAC scoring, PAC gate allow-lists, planner routes, mutators, and timeout defaults were unchanged. Targeted run `Output/experiment-corpus-baseline/run-verified-checkpoint-timeout-recovery-target-2026-05-07-r1` processed only the selected 10 rows: `9/10` remediated successfully, successful-row mean `86.11`, grades `4 A / 2 B / 2 C / 1 F`, `false_positive_applied = 0`; `structure-4076` reached `70/C`, `long-4516` reached `89/B` with checkpoint return, and `long-4683` reached `86/B` with checkpoint return. `structure-4438` still hard-timed out because its last verified checkpoint was only `36/F` (`checkpoint_below_floor(36<90)`), `fixture-inaccessible` took the known volatile `79/C` route, and `figure-4188` remained `59/F`. Diagnostic: `Output/experiment-corpus-baseline/verified-checkpoint-timeout-recovery-diagnostic-2026-05-07-r1`. Do not run fixed 50 from this candidate; next work should target `structure-4438` eligible-progress creation and `fixture-inaccessible` route volatility, not broader PAC or timeout changes.
- Trace-Driven Final Reanalysis And Optional Tool Admission is implemented but is not an acceptance checkpoint. Benchmark final reanalysis now has a decision helper: target-quality rows near the 5-minute wall record `soft_deadline_before_final_reanalysis`, high-B rows (`>=85`) near the wall record `bounded_final_reanalysis_guard`, and rows below `85` still use hard-timeout behavior. Late optional guards for repeated no-movement `normalize_pdfua_catalog_settings` and `repair_list_li_wrong_parent` attempts record `late_catalog_reanalysis_guard` / `late_list_reanalysis_guard` only under near-wall or high cumulative reanalysis pressure; first attempts, prior positive movement, target-quality rows, PAC scoring, PAC gates, planner routes, mutators, and timeout defaults were unchanged. Targeted run `Output/experiment-corpus-baseline/run-trace-driven-final-reanalysis-target-2026-05-06-r2` processed only the selected 10 rows: `7/10` remediated successfully, successful-row mean `92.00`, grades `6 A / 0 B / 0 C / 0 D / 1 F`, `false_positive_applied = 0`, and `long-4683` completed at `96/A` with `soft_deadline_before_final_reanalysis`; however `structure-4076`, `structure-4438`, and `long-4516` still hard-timed out, and `figure-4188` remained `59/F`. Diagnostic: `Output/experiment-corpus-baseline/trace-driven-final-reanalysis-diagnostic-2026-05-06-r2`. Do not run fixed 50 from this candidate; remaining blockers are active long-row reanalysis/mutation tails, not PAC policy.
- Reanalysis Tail Soft-Stop is implemented as a defensive runtime mechanism but is not an acceptance checkpoint. Runtime soft-stops now only keep the current analyzed state when it has already reached the remediation target score; benchmark full-mode final reanalysis can be skipped near the deadline only for target-quality rows and records `soft_deadline_before_final_reanalysis`. PAC scoring, PAC gate allow-lists, planner routes, default timeout values, and repair behavior were not changed. Target run `Output/experiment-corpus-baseline/run-reanalysis-tail-soft-stop-target-2026-05-06-r2` was not clean: `structure-4076` completed at `70/C`, `long-4683` completed at `96/A`, but `structure-4438` and `long-4516` still timed out, and control `figure-4188` was volatile/low at `59/F`. Do not run the fixed 50 from this result; next work should target heavy stage reanalysis after cheap late artifact tagging, especially `long-4516` stage 9 and `structure-4438` stage 4.
- Mean Recovery With Targeted Runtime Guards is implemented but is not an acceptance checkpoint. The kept behavior adds conservative late repeated-attempt guards for `mark_untagged_content_as_artifact` and `normalize_annotation_tab_order`, plus narrow PAC recovery for `repair_alt_text_structure` blocked only by `pdfua.content.orphan_mcids_absent` when total score or `pdf_ua_compliance` improves. Targeted run `Output/experiment-corpus-baseline/run-mean-recovery-target-2026-05-06-r1` processed only the selected 10 rows: `7/10` remediated successfully, successful-row mean `88.29`, grades `5 A / 1 C / 1 F`, but `structure-4438`, `long-4516`, and `long-4683` still hard-timed out. `structure-3661` recovered to `98/A`; `figure-4188` only reached `59/F`; `structure-4076` stayed `70/C`. Diagnostic: `Output/experiment-corpus-baseline/mean-recovery-target-diagnostic-2026-05-06-r1`. A broader late artifact no-structural-change suppression was briefly tested in `run-mean-recovery-target-2026-05-06-r2`, stopped after `fixture-inaccessible` dropped to `79/C`, and removed. Do not run fixed 50 from this candidate; next work should gate expensive stage reanalysis admission before starting another 45s analysis.
- Stage Reanalysis Admission Guard is implemented but is not an acceptance checkpoint. It adds narrow defensive plumbing that can roll back low-score stages before reanalysis and record `stage_reanalysis_admission_guard` only for applied-stage shapes limited to `mark_untagged_content_as_artifact` near the wall or `tag_native_text_blocks` near the wall/high cumulative reanalysis. Validation `Output/experiment-corpus-baseline/run-stage-reanalysis-admission-guard-target-2026-05-06-r2` processed only the selected 10 rows: `7/10` remediated successfully, successful-row mean `88.14`, grades `5 A / 1 D / 1 F`, controls `fixture-inaccessible`/Teams/`font-4035`/`structure-3775` stayed A-grade, and `structure-3661` stayed `98/A`, but `structure-4438`, `long-4516`, and `long-4683` still hard-timed out and `structure-4076` fell below target at `69/D`. Diagnostic `Output/experiment-corpus-baseline/stage-reanalysis-admission-guard-diagnostic-2026-05-06-r2` shows timeout traces moved to `normalize_pdfua_catalog_settings`, `repair_list_li_wrong_parent`, and final reanalysis after artifact cleanup, so do not run fixed 50 from this candidate and do not broaden the guard to catalog/list tools without separate proof.
- Runtime Tail Suppression Pilot is implemented but not an acceptance checkpoint. Benchmark remediation runs now write local timeout traces under `<run>/runtime-timeouts/<row-id>.json` when a row aborts/times out; traces include last phase, stage/round/tool, replay-state signature, rejected/no-effect reason, completed tool count, completed stage count, and completed reanalysis count/time. `scripts/pac-runtime-tail-diagnostic.ts` reads these traces when present. Same-state runtime suppression now records only expensive (`PDFAF_EXPENSIVE_NO_GAIN_RUNTIME_SUPPRESSION_MS`, default `12000ms`) rejected/no-effect attempts with stable replay-state signatures on structural/checker-facing tool families; metadata/title/language/catalog-only tools remain excluded. PAC scoring caps and PAC gate allow-lists were not changed. Targeted run `Output/experiment-corpus-baseline/run-runtime-tail-suppression-target-2026-05-06-r1` was not clean: `structure-4076` improved to `70/C`, but `structure-4438`, `long-4516`, and `long-4683` timed out and wrote timeout traces. Do not run the fixed 50 from this result; next behavior work should target reanalysis-heavy timeout rows, especially final/protected reanalysis after cheap late artifact tagging.
- Runtime Tail and PAC Gate Recovery Isolation is diagnostic-only. Added `scripts/pac-runtime-tail-diagnostic.ts` and `scripts/pac-gate-blocker-diagnostic.ts` with tests; no scoring, gate, planner, timeout, or repair behavior changed. Runtime artifacts `Output/experiment-corpus-baseline/pac-runtime-tail-diagnostic-2026-05-06-r1` classify `long-4516` and `structure-4438` as per-PDF timeouts, `structure-4076` as repeated no-gain tool churn, and `long-4683` as reanalysis-heavy large-document work. PAC blocker artifacts `Output/experiment-corpus-baseline/pac-gate-blocker-diagnostic-2026-05-06-r1` found `114` focus PAC gate rows, `80` orphan-MCID rows across `22` files, but only `2` candidate policy-review rows (`repair_alt_text_structure` on `structure-3661` and `figure-4188`) versus `9` real harmful regressions. Decision: do not narrow PAC gates globally; next runtime work should target `structure-4438`/`long-4516` timeout attribution and `structure-4076` no-gain churn, while any future PAC exception should be a tightly scoped `repair_alt_text_structure` + orphan-MCID same-category-improvement experiment with harmful-regression controls.
- Two-tier remediation analysis budget is implemented but is not a clean acceptance checkpoint. `CHECK_ANALYSIS_TIMEOUT_MS` remains `15000`, new `REMEDIATION_ANALYSIS_TIMEOUT_MS` defaults to `45000`, and `REMEDIATION_PDF_TIMEOUT_MS` remains `300000`; remediation route/orchestrator/post-pass/semantic verification/experiment remediation paths now use the remediation analysis budget while check-only analysis stays fast. Validation `Output/experiment-corpus-baseline/run-pac-analysis-budget-2026-05-06-r1` processed the fixed 50-file experiment corpus only: remediation completed `48/50`, timed out `structure-4438` and `long-4516`, mean/median `87.04/96`, reanalyzed mean/median `86.69/94`, grades `31 A / 6 B / 2 C / 1 D / 8 F`, p95 wall `104411ms`, attempts `933`, and `false_positive_applied = 0`. Stage 41 gate `Output/experiment-corpus-baseline/pac-analysis-budget-gate-2026-05-06-r1` still fails `analyze_success`, `remediate_success`, `route_summary_coverage`, `f_grade_count`, `protected_file_regressions`, `runtime_p95_wall`, and `total_tool_attempts`. PAC diagnostic `Output/experiment-corpus-baseline/pac-analysis-budget-diagnostic-2026-05-06-r1` shows `114` PAC gate rejections, `0` newly evaluable debt rejections, and `90` blocked useful repairs. Keep the timeout split; next work should target runtime-tail/analyzer behavior for `structure-4438`/`long-4516` and remaining true PAC gate blockers, not raise the default timeout broadly.
- PAC Gate Recovery Stage is implemented but not a clean acceptance checkpoint. It narrows PAC acceptance gates so `not_applicable -> fail` is newly evaluable debt, not a remediation regression; `pass/warn -> fail` and applicable `fail -> higher count` still reject. Diagnostic `Output/experiment-corpus-baseline/pac-gate-recovery-diagnostic-2026-05-06-r1` found `192` strict PAC gate rejections, `79` newly evaluable debt rejections, and `169` blocked useful repairs before the change. Candidate run `Output/experiment-corpus-baseline/run-pac-gate-recovery-2026-05-06-r4` recovered mean/median to `84.45/93` with grades `30 A / 5 B / 3 C / 1 D / 10 F`, `false_positive_applied = 0`, and `0` newly evaluable debt rejections, but Stage 41 still failed: `structure-4438` timed out under the 5-minute remediation wall, p95 `164180ms`, attempts `913`, protected regressions `15`, and F count `10`. Next recommended stage is a two-tier analysis budget: keep `/v1/analyze` at 15s, but give remediation internal reanalysis a larger bounded per-analysis budget while preserving the 5-minute per-PDF wall.
- PAC Promotion Stage 1 narrowing/revert is complete for scoring influence. Initial validation `Output/experiment-corpus-baseline/run-pac-promotion-stage1-validation-2026-05-05-r1` found `71` promoted score caps and failed Stage 41 broadly; narrowed run `Output/experiment-corpus-baseline/run-pac-promotion-stage1-narrowed-2026-05-06-r1` still failed with `6` table-header promoted caps; final scoring-reverted run `Output/experiment-corpus-baseline/run-pac-promotion-stage1-scoring-reverted-2026-05-06-r1` has `promotedPacScoreCapCount = 0` and `false_positive_applied = 0`, but still fails Stage 41 (`mean 77.04`, median `90`, F count `14`, protected regressions `22`, p95 wall `237568ms`, attempts `1019`). No Stage 1 promoted PAC rules should influence scoring now; font/CMap and table-header evidence remain diagnostic-only. Source report `docs/pac-promotion-stage1-validation.md` says the next work should narrow PAC gate/runtime churn, especially promoted `pdfua.structure.rolemap_valid` and `pdfua.structure.child_roles_valid` gate rejections.
- PAC Gate Narrowing Stage 1 removed `pdfua.structure.rolemap_valid` and `pdfua.structure.child_roles_valid` from acceptance gating while keeping `pdfua.table.header_association_present` and `pdfua.parent_tree.mcid_entries_valid` under observation. Validation `Output/experiment-corpus-baseline/run-pac-gate-narrowing-stage1-2026-05-06-r1` had `promotedPacScoreCapCount = 0`, `promotedPacGateRejectionCount = 0`, and `false_positive_applied = 0`, but still failed Stage 41 (`mean 76.50`, median `74`, F count `15`, protected regressions `23`, p95 wall `280411ms`, attempts `1024`). Remaining rejection churn is pre-existing PAC gates: orphan MCIDs, annotation StructParent, tagged annotations, figure alt, and tab order. Next work should isolate those pre-existing gates/runtime tails, not add scoring caps or new repairs.
- PAC promotion readiness corpus run is data-only and complete. Local artifacts under `Output/pac-promotion-readiness/` cover 6 readiness sources / 138 files across original experiment, edge-mix, holdout 5, hard 1, and hard 2 corpora. Source decision report `docs/pac-promotion-readiness-corpus-decision.md` recommends future scoring-cap candidates `pdfua.font.to_unicode_cmap_valid`, `pdfua.font.to_unicode_cmap_present`, and `pdfua.table.header_association_present`, plus regression-only gate candidates `pdfua.table.header_association_present`, `pdfua.structure.child_roles_valid`, `pdfua.parent_tree.mcid_entries_valid`, `pdfua.structure.rolemap_valid`, and possibly `pdfua.content.text_tagged_or_artifacted` after noisy-evidence review. No scoring, gates, planner, mutation, API, benchmark, rendered-contrast, network, or AI behavior was changed.
- PAC promotion readiness selector is implemented as diagnostic-only. `scripts/pac-promotion-readiness.ts` consumes a POC strong-area matrix or analyzes selected PDFs, then writes local JSON/Markdown summaries of verified category-pass/PAC-fail gaps, scoring candidates, structural gate candidates, noisy evidence, blocked rules, and optional contrast/link/AI diagnostics. `docs/pac-promotion-readiness-decision.md` records the promotion policy; no scoring, gates, planner, mutation, API, benchmark, rendered-contrast, network, or AI behavior was changed.
- POC checker alignment evidence pass is implemented as diagnostic-only. Added optional/fail-soft rendered-contrast status with confidence reason, `structureSyntaxAudit` PAC evidence for missing roles, parent links, child-role relationships, MCR/OBJR, and RoleMap issues, deeper language context counters, TOC/Note checks, optional-content/file-spec/printermark evidence, opt-in URI scheme diagnostics, and AI visual-tag placeholder reasons. Deterministic defaults remain disabled/not measured where required; no scoring, gates, planner, mutation, API, benchmark, network reachability, rendered measurement, or AI behavior was promoted.
- POC strong-area evidence follow-up is implemented as diagnostic-only. ParentTree audit now flattens `/ParentTree` number trees and checks page `/StructParents`, MCID entries, annotation/object references, and invalid entry shapes; content tagging now uses a bounded marked-content state walker for page/Form XObject streams; table header audit now inspects `/Scope`, `/ID`, `/Headers`, orphan headers, and data-cell association; font audit now parses `/ToUnicode` CMap shape and checks Type0 descendants, CIDToGIDMap risks, WMode, external CMap names, and TrueType symbolic/non-symbolic encoding risk. The strong-area diagnostic now reports rule-family summaries and promotion-readiness buckets. No scoring, gates, planner, mutation, API, benchmark, rendered-contrast, link-reachability, or AI behavior was promoted.
- POC strong-area evidence integration is implemented as diagnostic-only. `DocumentSnapshot` now carries bounded audit groups for ParentTree, content tagging/artifact boundaries, table header association, font/CMap/Unicode risks, element-language syntax, rendered contrast, TOC/Note, optional content/file specs, URI reachability, and AI visual-tag mismatch. PAC rule evidence exposes these as additional rule rows, and `scripts/poc-strong-areas-diagnostic.ts` writes local rule matrices/summaries with gap/noisy/promotion-candidate classifications. No scorer, API schema, planner, mutation, acceptance gate, benchmark policy, rendered-contrast measurement, URI checking, or AI mutation behavior was added.
- Phase 5 first POC-exposed fix slice is implemented. Analyzer snapshots now expose `/MarkInfo /Suspects` and `/ViewerPreferences /DisplayDocTitle`; PAC evidence verifies those rules; `scripts/pac-gap-fix-diagnostic.ts` ranks fixable catalog-setting gaps; and the narrow `normalize_pdfua_catalog_settings` repair only sets `/Suspects false` and `/DisplayDocTitle true`, with acceptance requiring targeted PAC evidence improvement and no score/API/semantic/benchmark-policy changes.
- Phase 4 PAC remediation acceptance gates are implemented as conservative regression-prevention only. Selected structural/checker-facing PAC rules now gate deterministic stage and guarded post-pass acceptance when a structural mutator creates a new verified failure or increases an existing failure count; scoring, API schema, planner scheduling, mutation behavior, semantic gates, and benchmark policy remain unchanged.
- Phase 3 PAC scoring influence is implemented conservatively in scorer finalization. Only selected verified PAC `fail` rules can cap an applicable mapped category at `89`, add `PAC rule failure: <ruleId>` cap records, and downgrade that category to manual-review-required; no API schema, planner, remediation, mutation, gate, or diagnostic-script behavior was changed.
- Phase 2 PAC evidence reporting/diagnostics is implemented. HTML remediation reports can include compact diagnostic-only PDF/UA rule checks from final `DocumentSnapshot` evidence, and `scripts/pac-parity-diagnostic.ts` writes local PAC rule matrix/summary artifacts without changing API JSON, scoring, planning, remediation, gates, or benchmark behavior.
- Phase 1 PAC-style rule evidence is implemented in `src/services/compliance/pacRuleEvidence.ts` with focused tests in `tests/services/pacRuleEvidence.test.ts`. It is read-only over `DocumentSnapshot`, has no scorer/API/remediation integration, and Node 22 targeted test/lint passed before commit.
- POC-decompiled review is documented in `docs/poc-decompiled-checker-map.md`. The POC under `Research/POC-decompiled` is a PAC-style event-driven checker with broad PDF/UA rule fidelity; PDFAF remains stronger at guarded remediation, corpus validation, semantic alt, rollback, and visual/protected acceptance. Best improvement candidates are a rule-id catalog layer, rendered contrast, deeper font/CMap/natural-language/ParentTree/table-header/content-tagging diagnostics, and optional AI visual-tag mismatch checks.
- Stage 162 is a kept targeted quality win. Commit `4b554de` added near-pass PDF/UA cleanup. It lifted active-tail `v1-v1-3468` and `v1-v1-4766` to A-grade in focused validation, kept `false_positive_applied = 0`, preserved Stage 75 font gains, and improved original-50 mean/median/F count, but its formal gate still had protected regressions.
- Stage 163 is diagnostic-only. Commit `80d2e4c` added `scripts/stage163-protected-regression-closeout.ts` and tests. No remediation behavior was kept.
- Stage 163 focused diagnostics found same-buffer floor-safe repeats for `long-4516`, `short-4214`, `short-4176`, and sometimes `long-4683`; Stage 162 annotation retry did not fire on these protected blockers.
- Stage 163 full original-50 run `Output/experiment-corpus-baseline/run-stage163-full-2026-04-30-r1` failed only `protected_file_regressions` with `short-4176` and `long-4683`; runtime p95 passed, attempts passed, font controls stayed good, and active-tail Stage 162 wins stayed good.
- A Stage 163 experiment forcing protected final confirmation to bypass analyzer cache was rejected: `Output/experiment-corpus-baseline/run-stage163-full-postfix-2026-04-30-r1` failed both `protected_file_regressions` and `runtime_p95_wall`, so the behavior was reverted.
- Remaining protected debt after Stage 163 is analyzer/route volatility, especially `short-4176`, `long-4683`, and known volatile control `structure-4076`. Do not add broad protected route guards or scorer/gate changes without new repeat evidence.
- Stage 164 is diagnostic-only. `scripts/stage164-alt-pdfua-tail-diagnostic.ts` classifies stable low-alt/PDF-UA residuals using original-50 Stage 163 artifacts plus active-tail controls. Final-byte diagnostics showed possible extra checker-visible figure-alt targets on `figure-4754` and `long-4680`, but protected continuation experiments were rejected: the apparent `figure-4754` lift traded against PDF/UA/no-alt-gain guards, and guarded reruns did not produce a clean repeatable lift. No Stage 164 remediation behavior should be kept without a new category-stable target proof.
- Stage 165 keeps a narrow link annotation ParentTree repair. `scripts/stage165-link-parenttree-diagnostic.ts` selected only `fixture-inaccessible` plus active-tail duplicates `orig-fixture-inaccessible` and `v1-v1-legacy-004-pdfaf-fixture-inaccessible` as small safe link ownership candidates. The implementation reuses existing `repair_native_link_structure` as a late guarded post-pass when link `/StructParent`/ParentTree debt is small and core categories are stable; it is not a broad link/annotation route change.
- Stage 165 validation: target run `Output/experiment-corpus-baseline/run-stage165-target-link-parenttree-2026-04-30-r1` lifted `fixture-inaccessible` to `91/A` with link debt `6/6 -> 0/0`; visual validation showed `fixture-inaccessible` pixel-stable across 30 pages. Active-tail smoke `Output/stage145-low-grade-tail/run-stage165-active-link-parenttree-smoke-2026-04-30-r1` lifted both fixture duplicates to `91/A`. Full runs showed the target win repeatable, but formal gates remained noisy due protected analyzer/route volatility on existing rows (`long-4516`, `short-4214`, `long-4683`, `structure-4076`), not the Stage 165 tool. Do not add protected route hacks to force a clean gate from this evidence.
- Stage 165 also tightened protected heading no-effect noise: `shouldStopProtectedHeadingCandidateAfterHardNoEffect` now stops after `multiple_h1_after_mutation` in protected-baseline runs. This is a scheduler noise cap, not a heading fixer.
- Stage 166 keeps a narrow stable mixed-tail lift for `figure-4754`. Diagnostic `Output/experiment-corpus-baseline/stage166-mixed-tail-diagnostic-2026-04-30-r1` selected `figure-4754` / `orig-figure-4754` for title/bookmark/heading topup and parked `font-4057` as mixed alt/PDF-UA/table debt with no safe single path.
- Stage 166 behavior: fallback document-title selection now rejects garbage heading strings and filename-like bookmarks, and duplicate-H1 normalization counts as heading improvement only when root-reachable heading evidence is preserved. A TypeScript invariant guard also rejects claimed heading benefits that erase existing root-reachable headings.
- Stage 166 validation: focused post-fix run `Output/experiment-corpus-baseline/run-stage166-target-mixed-tail-postfix-2026-04-30-r1` lifted `figure-4754` `64/D -> 81/B`, kept `figure-4755` at `99/A`, and kept controls `fixture-inaccessible`, `long-4516`, `long-4680`, `long-4683`, `long-4700`, and font rows stable. Visual report `Output/experiment-corpus-baseline/stage166-visual-figure4754-postfix-2026-04-30-r1` found all 46 pages pixel-stable.
- Stage 166 full original-50 exact-code run `Output/experiment-corpus-baseline/run-stage166-full-2026-04-30-r4` has reanalyzed grades `36 A / 10 B / 3 C / 1 D / 0 F`, reanalyzed mean `92.76`, `false_positive_applied = 0`, p95 wall passing, and attempts `845`. Stage 41 gate `Output/experiment-corpus-baseline/stage166-benchmark-gate-2026-04-30-r4` fails only `protected_file_regressions` on known parked volatility row `short-4176`; this is not Stage166-caused.
- Stage 166 active-tail smoke `Output/stage145-low-grade-tail/run-stage166-active-mixed-tail-smoke-postfix-2026-04-30-r1` preserved Stage 162/165 controls and lifted `orig-figure-4754` to `81/B`; after grades were `3 A / 1 B / 2 C / 0 D / 0 F`.
- Stage 167 fresh v1 holdout validation is artifact-only. New local original-PDF batch `Input/from_sibling_pdfaf_v1_holdout_5/` contains 20 varied v1 PDFs. Run `Output/from_sibling_pdfaf_v1_holdout_5/run-stage167-baseline-2026-04-30-r1` completed `20/20`, mean `31.00 -> 90.80`, median `28 -> 97`, grades `16 A / 1 B / 0 C / 0 D / 3 F`, attempts `311`, and `false_positive_applied = 0`. Remaining Fs are `v1-3443`, `v1-4657`, and `v1-4760`, all with `heading_structure=0`; `v1-4657` also has `alt_text=0`. Next best direction from this batch is zero-heading anchor/ownership recovery split by OCR/manual vs native/tagged cases, not broad caps.
- Stage 168 keeps a narrow OCR heading-anchor recovery. `scripts/stage168-zero-heading-diagnostic.ts` classifies fresh-holdout zero-heading failures; focused run `Output/from_sibling_pdfaf_v1_holdout_5/run-stage168-target-zero-heading-2026-04-30-r1` lifted `v1-3443` from `59/F` to `97/A` by preserving a leading short title word (`On`) in filename-visible OCR title seeds while still requiring visible OCR text and MCID/paragraph ownership. Visual report `Output/from_sibling_pdfaf_v1_holdout_5/stage168-visual-3443-2026-04-30-r1` found all 16 pages pixel-stable. `v1-4657` and `v1-4760` remain no-safe-heading-anchor/content-owner debt; `v1-4553` showed unrelated repeat volatility (`59/F` in one full holdout repeat, `87/B` in focused repeats).
- Stage 168 validation: full holdout 5 run `Output/from_sibling_pdfaf_v1_holdout_5/run-stage168-full-holdout5-2026-04-30-r1` completed `20/20`, mean `30.80 -> 90.25`, median `28 -> 97`, grades `17 A / 0 B / 0 C / 0 D / 3 F`, attempts `311`, and `false_positive_applied = 0`. Original-50 run `Output/experiment-corpus-baseline/run-stage168-full-2026-04-30-r1` has reanalyzed grades `37 A / 10 B / 2 C / 0 D / 1 F`, reanalyzed mean `92.64`, and `false_positive_applied = 0`; Stage 41 gate `Output/experiment-corpus-baseline/stage168-benchmark-gate-2026-04-30-r1` passes against Stage 42 with `protected_file_regressions = 0` and p95 passing. Remaining original-50 F is parked `structure-4076` analyzer/route volatility.
- Stage 169 is diagnostic-only. `scripts/stage169-native-heading-owner-diagnostic.ts` compares Stage 167, Stage 168 focused/full, and a fresh Stage 169 target repeat for holdout 5 native zero-heading rows. Target run `Output/from_sibling_pdfaf_v1_holdout_5/run-stage169-target-native-heading-2026-04-30-r1` moved `v1-4657` to `97/A` and `v1-4553` to `87/B` with existing route behavior, while `v1-4760` stayed `59/F`; visual report `Output/from_sibling_pdfaf_v1_holdout_5/stage169-visual-4657-2026-04-30-r1` found all 10 pages pixel-stable. Diagnostic `Output/from_sibling_pdfaf_v1_holdout_5/stage169-native-heading-owner-diagnostic-2026-04-30-r2` classified `v1-4657` and `v1-4553` as route-order volatility and `v1-4760` as visible native title without a safe title owner. No Stage 169 remediation behavior was kept.
- Stage 170 keeps a narrow native visible title owner bridge for ownerless page-1 title text. `bridge_native_title_text_owner` runs only on non-OCR native zero-heading PDFs with a high-confidence visible title mapped to first-page BT/ET groups; it wraps only those groups with new MCIDs and creates one root-reachable H1. Diagnostic `scripts/stage170-native-title-owner-bridge-diagnostic.ts` selected only `v1-4760`; route-volatility controls `v1-4657` and `v1-4553` were not targeted.
- Stage 170 validation: focused run `Output/from_sibling_pdfaf_v1_holdout_5/run-stage170-target-native-title-owner-2026-05-01-r2` reached `5 A / 1 B / 0 F` and moved `v1-4760` from `59/F` to `94/A`. Visual report `Output/from_sibling_pdfaf_v1_holdout_5/stage170-visual-4760-2026-05-01-r2` found all 22 pages pixel-stable. Full holdout 5 run `Output/from_sibling_pdfaf_v1_holdout_5/run-stage170-full-holdout5-2026-05-01-r1` reached mean `93.45`, grades `17 A / 1 B / 1 C / 0 D / 1 F`; the remaining F was `v1-4650`, which repeated at `97/A` in focused run `Output/from_sibling_pdfaf_v1_holdout_5/run-stage170-target-4650-repeat-2026-05-01-r1`, so treat it as unrelated route/analyzer volatility.
- Stage 170 original-50 run `Output/experiment-corpus-baseline/run-stage170-full-2026-05-01-r1` has reanalyzed grades `35 A / 9 B / 4 C / 1 D / 1 F`, mean `92.6`, median `96`, p95 wall passing, and `false_positive_applied = 0`. Stage 41 gate `Output/experiment-corpus-baseline/stage170-benchmark-gate-2026-05-01-r1` fails only `protected_file_regressions` on `long-4146`, `short-4214`, and `short-4176`; `bridge_native_title_text_owner` did not fire on those rows. `long-4146` is in-run `97/A` but same-buffer protected reanalysis repeats `59/F`, so park this as protected reanalysis/route debt unless a later closeout stage proves a safe checkpoint.
- Stage 171 hard v1 edge holdout is artifact-only. Local input `Input/from_sibling_pdfaf_v1_hard_1/` contains 20 deliberately difficult original v1 PDFs selected from detailed v1 outcomes: long/manual/scanned, heading-zero, multi-category, link/table, figure/font/PDF-UA stress. Run `Output/from_sibling_pdfaf_v1_hard_1/run-stage171-hard-2026-05-01-r1` completed `20/20`, mean `32.05 -> 86.85`, grades after `14 A / 1 B / 0 C / 0 D / 5 F`, `false_positive_applied = 0`. Remaining Fs: `3476`, `3473`, `3470`, and `3569` are long OCR/manual or long stress heading-zero shells; `4213` is mixed alt/table/PDF-UA residual (`alt_text=0`, `table_markup=72`, `pdf_ua_compliance=71`) with headings already good. `4767` is the only B (`82/B`) due low alt/PDF-UA. This is the best current hard-case improvement loop.
- Stage 172 is diagnostic-only. `scripts/stage172-hard-ocr-heading-diagnostic.ts` compares the hard-holdout Fs against OCR wins. Focused written-PDF run `Output/from_sibling_pdfaf_v1_hard_1/run-stage172-target-ocr-heading-2026-05-01-r1` kept `3476`, `3473`, `3470`, `3569`, and `4213` at `59/F`; contrast rows `3475` and `3577` stayed `97/A`. Diagnostic `Output/from_sibling_pdfaf_v1_hard_1/stage172-hard-ocr-heading-diagnostic-2026-05-01-r2` classified the four OCR heading Fs as `title_page_not_first_page`: page 1 is a shared collection/cover page ("Research and program evaluation in Illinois..."), and the row titles are not visible as safe page-1 OCR/MCID-owned text. No OCR heading behavior was kept. `4213` is parked for the next mixed alt/table/PDF-UA residual stage.
- Stage 173 is diagnostic-only. `scripts/stage173-hard-mixed-residual-diagnostic.ts` classifies hard-holdout `v1-4213` as `mixed_requires_ordered_transaction`, not a single safe fixer path: it has `alt_text=0`, `table_markup=72`, `pdf_ua_compliance=71`, three unattempted checker-visible alt targets, two strongly-irregular table targets, and PDF/UA debt with orphan MCIDs plus path-paint evidence. Do not add a standalone alt/table/orphan cleanup for `4213`; the next safe path would need an ordered transaction that proves category gains together without visual or protected regressions.
- Stage 174 is diagnostic-only. `scripts/stage174-ordered-mixed-transaction-diagnostic.ts` and `src/services/remediation/mixedResidualTransaction.ts` encode the ordered alt/table/PDF-UA transaction gate/decision rules, but the focused experiment `Output/from_sibling_pdfaf_v1_hard_1/run-stage174-target-mixed-2026-05-01-r2` did not produce an accepted engine gain on `v1-4213`: the transaction improved table/PDF-UA internally (`table_markup +28`, `pdf_ua_compliance +29`) but `alt_text` stayed `0` and total score stayed `59/F`, so it rolled back. No Stage 174 post-pass behavior is active in the orchestrator; keep `4213` parked as mixed ordered-transaction debt unless a future stage proves an alt ownership fix that actually moves `alt_text`.
- Stage 175 keeps a narrow OCR collection-cover title-page heading recovery. `create_heading_from_ocr_collection_title_anchor` runs only for OCR/manual page shells with `heading_structure=0`, no safe page-1 OCR heading, a detected collection-cover page 1, and a content-owned title on pages 2-8. It reuses the OCR heading synthesis path with non-page-1 anchors allowed only through this separate tool; normal `create_heading_from_ocr_page_shell_anchor` remains page-1 only.
- Stage 175 validation: diagnostic `Output/from_sibling_pdfaf_v1_hard_1/stage175-ocr-collection-title-diagnostic-2026-05-01-r1` selected all four hard-holdout OCR Fs (`v1-3476`, `v1-3473`, `v1-3470`, `v1-3569`). Focused run `Output/from_sibling_pdfaf_v1_hard_1/run-stage175-target-ocr-collection-title-2026-05-01-r1` lifted all four to `97/A`; controls `v1-3475` and `v1-3577` stayed `97/A`; `v1-4213` stayed parked at `59/F`. Visual reports `stage175-visual-3476/3473/3470/3569-2026-05-01-r1` found all pages pixel-stable.
- Stage 175 full hard-holdout run `Output/from_sibling_pdfaf_v1_hard_1/run-stage175-hard-full-2026-05-01-r1` completed `20/20`, mean `32.05 -> 94.15`, median `27 -> 97`, grades `18 A / 1 B / 0 C / 0 D / 1 F`, attempts `252`, and false-positive applied `0`. The lone F remains `v1-4213`, now clearly not a heading issue (`heading_structure=94`) but mixed alt/table/PDF-UA debt.
- Stage 175 original-50 run `Output/experiment-corpus-baseline/run-stage175-full-2026-05-01-r1` has in-run grades `36 A / 10 B / 3 C / 0 D / 1 F`, reanalyzed grades `35 A / 9 B / 4 C / 0 D / 2 F`, mean `92.6`, median `96`, p95 wall passing, attempts `859`, and false-positive applied `0`. Gate `Output/experiment-corpus-baseline/stage175-benchmark-gate-2026-05-01-r1` fails only `protected_file_regressions` on known parked rows `short-4214` and `short-4176`; this is not Stage175-caused and does not involve the new OCR collection tool.
- Hard holdout 2 was created locally at `Input/from_sibling_pdfaf_v1_hard_2/` after Stage 175. It contains 20 untried original v1 cached PDFs selected to be deliberately difficult: `5` manual/scanned long, `1` manual/scanned table/link, `5` native heading/runtime, `1` native table/heading/runtime, `2` native long/runtime, `3` mixed table/link/PDF-UA, and `3` table/link short. PDFs are ignored by git; manifest and selection metadata live beside them. No benchmark has been run on this batch yet.
- Hard holdout 2 baseline run `Output/from_sibling_pdfaf_v1_hard_2/run-stage176-hard2-baseline-2026-05-02-r1` completed `20/20` with mean `30.00 -> 87.35`, median `31 -> 92.5`, grades `11 A / 7 B / 0 C / 0 D / 2 F`, `300` attempts, and `false_positive_applied = 0`. Remaining Fs are `3510` (`52/F`, pure OCR/manual `heading_structure=0`, `reading_order=35`) and `4105` (`50/F`, mixed heading/alt/table/PDF-UA/reading-order debt with `table_markup=0`, `alt_text=20`, `heading_structure=35`). Several residual B rows share low `alt_text=50-56`; this is a likely later quality target, but the immediate hard failures are `3510` and `4105`.
- Stage 176 keeps a narrow OCR noisy/split page-1 title anchor recovery. The selector now bridges compact OCR-owned title windows where metadata/title seeds partially match noisy OCR text, such as `Blueprint For The Future` matching owned page text `FOR` / `-THE*-FUTURE`, while still requiring real MCID/paragraph ownership and rejecting filename-only, metadata-only, body-fragment, page-label, header/footer, and ownerless anchors. It reuses `create_heading_from_ocr_page_shell_anchor`; no new mutator or scorer/gate change was added.
- Stage 176 diagnostic `scripts/stage176-ocr-noisy-title-diagnostic.ts` classified `v1-3510` as `ocr_noisy_split_title_candidate`, `v1-3508` as `already_fixed_control`, and `v1-4105` as `mixed_not_heading_stage`. Focused run `Output/from_sibling_pdfaf_v1_hard_2/run-stage176-target-noisy-title-2026-05-02-r1` lifted `3510` from `52/F` to `93/A`; visual report `Output/from_sibling_pdfaf_v1_hard_2/stage176-visual-3510-2026-05-02-r1` found all 98 pages pixel-stable. Prior OCR wins `3476`, `3473`, `3470`, `3569`, `3443`, `3423`, `3429`, and `3433` stayed A-grade in focused controls.
- Stage 176 full hard-holdout-2 run `Output/from_sibling_pdfaf_v1_hard_2/run-stage176-hard2-full-2026-05-02-r1` completed `20/20`, mean `30.55 -> 87.65`, grades `11 A / 7 B / 0 C / 0 D / 2 F`, and `false_positive_applied = 0`. `3510` is fixed, `4105` remains mixed structural debt, and `4705` surfaced as a separate native/tagged heading route-volatility F (`4705` was `92/A` in the baseline but repeated `59/F`/`54/F` without the new OCR noisy-title path firing). Treat `4705` as separate native/tagged heading/route debt, not Stage176-caused regression.
- Stage 176 original-50 run `Output/experiment-corpus-baseline/run-stage176-full-2026-05-02-r1` has in-run grades `37 A / 10 B / 2 C / 1 D / 0 F`, reanalyzed grades `36 A / 10 B / 3 C / 1 D / 0 F`, mean `93.08`, median `96`, p95 wall passing, attempts passing, and `false_positive_applied = 0`. Stage 41 gate `Output/experiment-corpus-baseline/stage176-benchmark-gate-2026-05-02-r1` fails only `protected_file_regressions` on known parked `short-4176` protected volatility; the Stage 176 OCR noisy-title path did not fire on that row.
- Stage 177 keeps a narrow native-tagged MCID/PDF-UA synthesis fallback. It does not add a mutator; it schedules existing `synthesize_basic_structure_from_layout` for native-tagged zero-heading rows when paragraph extraction drops out but strong MCID ownership, PDF/UA orphan evidence, intact table/link scores, and no annotation-order risk remain. This targets the `4705` bad-route shape without filename-specific logic or broader heading/table/figure behavior.
- Stage 177 diagnostic `scripts/stage177-hard2-remaining-f-diagnostic.ts` classified `v1-4705` as `native_synthesis_route_variance`, `v1-4105` as `mixed_residual_debt`, and `3510`/`3508` as stable controls. Focused run `Output/from_sibling_pdfaf_v1_hard_2/run-stage177-target-4705-postfix-2026-05-02-r1` moved `4705` to `92/A` while preserving `3510` and `3508` at `93/A`; visual report `Output/from_sibling_pdfaf_v1_hard_2/stage177-visual-4705-2026-05-02-r1` found all 13 pages pixel-stable.
- Stage 177 full hard-holdout-2 run `Output/from_sibling_pdfaf_v1_hard_2/run-stage177-hard2-full-2026-05-02-r1` completed `20/20`, mean `30.60 -> 89.65`, grades `12 A / 7 B / 0 C / 0 D / 1 F`, attempts `301`, and `false_positive_applied = 0`. `4705` reached `97/A`; the lone F is still `4105` (`50/F`) and should be handled as a dedicated mixed heading/table/alt/PDF-UA residual stage.
- Stage 177 original-50 run `Output/experiment-corpus-baseline/run-stage177-full-2026-05-02-r1` has in-run grades `37 A / 9 B / 3 C / 1 D / 0 F`, reanalyzed grades `36 A / 9 B / 4 C / 1 D / 0 F`, mean `92.98`, median `96`, p95 wall passing, attempts passing, and `false_positive_applied = 0`. Gate `Output/experiment-corpus-baseline/stage177-benchmark-gate-2026-05-02-r1` fails only `protected_file_regressions` on known parked volatility rows `long-4683` and `short-4214`; the Stage 177 fallback did not fire on `long-4683`, and `short-4214` already used synthesis in Stage 176, so this is not Stage177-caused.
- Stage 178 keeps a narrow protected weak-alt continuation with external confirmation. Diagnostic `Output/experiment-corpus-baseline/stage178-alt-pdfua-residual-diagnostic-2026-05-02-r1` selected stable low-alt rows `figure-4754`, `long-4680`, and `long-4700`; protected runs may exceed the normal three figure-alt targets only when the Stage 42 baseline already had weak alt, current heading/reading/table evidence is stable, distinct safe checker-visible figure targets remain, and bypass-cache reanalysis preserves the alt/score gain. Extra attempts that do not reanalyze safely roll back instead of keeping apparent in-run gains.
- Stage 178 validation: full original-50 protected run `Output/experiment-corpus-baseline/run-stage178-full-2026-05-02-r1` has reanalyzed grades `39 A / 8 B / 2 C / 1 D / 0 F`, mean `93.50`, median `96`, p95 wall passing, attempts passing, and `false_positive_applied = 0`. `long-4700` reached `91/A` with pixel-stable visual report `Output/experiment-corpus-baseline/stage178-visual-long4700-2026-05-02-r1`; `long-4680` reached `92/A`; `figure-4754` stayed `81/B` because non-preserved extra alt attempts were rejected. Stage 41 gate `Output/experiment-corpus-baseline/stage178-benchmark-gate-2026-05-02-r1` fails only `protected_file_regressions` on known parked `short-4176` volatility, not on a Stage178-targeted row. Hard-holdout-2 smoke `Output/from_sibling_pdfaf_v1_hard_2/run-stage178-hard2-smoke-2026-05-02-r1` preserved `3510` and `4705` at A while `4105` remains the parked F.
- Stage 179 keeps a narrow partial-alt ownership cleanup. Diagnostic `Output/experiment-corpus-baseline/stage179-partial-alt-ownership-diagnostic-2026-05-02-r1` showed six stable B rows had `alt_text=50` from empty `/Alt` or `/ActualText` on non-Figure structure elements, with no checker-visible figure targets. The kept Python helper change removes only empty `/Alt`/`/ActualText` values during existing `repair_alt_text_structure`; it does not remove non-empty semantic text, raise figure caps, change scoring/gates, or alter routing.
- Stage 179 validation: focused target `Output/experiment-corpus-baseline/run-stage179-target-partial-alt-2026-05-02-r1` lifted `structure-4131`, `font-3437`, `font-3448`, `font-3529`, `figure-4702`, and `font-4156` to A with `alt_text=100`. Visual report `Output/experiment-corpus-baseline/stage179-visual-target-partial-alt-2026-05-02-r1` found the six primary PDFs pixel-stable; unrelated control `font-4699` showed drift from existing font behavior, not the Stage179 cleanup.
- Stage 179 full original-50 run `Output/experiment-corpus-baseline/run-stage179-full-2026-05-02-r1` has reanalyzed grades `44 A / 2 B / 3 C / 0 D / 1 F`, mean `94.56`, median `98`, p95 wall passing, attempts unchanged at `843`, and `false_positive_applied = 0`. Stage 41 gate `Output/experiment-corpus-baseline/stage179-benchmark-gate-2026-05-02-r1` fails only `protected_file_regressions` on known parked `short-4176` volatility. The remaining F is `structure-4076`, a known analyzer/route-volatility row; do not build Stage179 follow-up behavior around it.
- Stage 179 hard-holdout-2 smoke `Output/from_sibling_pdfaf_v1_hard_2/run-stage179-hard2-smoke-2026-05-02-r1` preserved `3510` at `93/A` and `4705` at `97/A`; `4105` remains the parked mixed residual F. Post-fix diagnostic `Output/experiment-corpus-baseline/stage179-partial-alt-ownership-diagnostic-full-2026-05-02-r1` shows the original six partial-alt targets no longer have empty non-Figure alt/ActualText debt; possible later work should not be broad Stage179 cleanup, but targeted `figure-4754`/`long-4700` hidden checker-alt evidence or `font-4057` mixed table/alt/PDF-UA recovery.
- Stage 180 keeps a narrow mixed table/PDF-UA lift for `font-4057`. Diagnostic `Output/experiment-corpus-baseline/stage180-mixed-table-pdfua-diagnostic-2026-05-02-r1` selected only `font-4057` as a table-first mixed transaction candidate with remaining explicit table refs `2216_0` and `2897_0`, annotation debt, and orphan MCIDs. The implementation applies explicit `normalize_table_structure` targets, then guarded link ParentTree repair and orphan cleanup; it also skips late local font substitution only after an applied Stage 180 mixed cleanup so the Stage 75 font path cannot introduce visual drift on the Stage 180 row.
- Stage 180 validation: focused run `Output/experiment-corpus-baseline/run-stage180-target-mixed-table-pdfua-2026-05-02-r3` lifted `font-4057` from `74/C` to `82/B` with `table_markup=100`, `pdf_ua_compliance=100`, `link_quality=100`, and `false_positive_applied = 0`. Visual report `Output/experiment-corpus-baseline/stage180-visual-font4057-2026-05-02-r3` found all 80 pages pixel-stable against the pre-Stage180 remediated PDF. Earlier r1/r2 experiments proved the Stage 180 structural mutations were pixel-stable but a late `embed_local_font_substitutes` side effect caused drift or checkpoint issues, so keep the narrow post-Stage180 font skip.
- Stage 180 full original-50 repeat `Output/experiment-corpus-baseline/run-stage180-full-2026-05-02-r2` has reanalyzed grades `43 A / 4 B / 3 C / 0 D / 0 F`, mean `94.86`, median `98`, p95 wall passing, attempts `852`, and `false_positive_applied = 0`. Gate `Output/experiment-corpus-baseline/stage180-benchmark-gate-2026-05-02-r2` fails only `protected_file_regressions` on known parked `short-4214` volatility. Low-row repeat `Output/experiment-corpus-baseline/run-stage180-low-row-repeat-2026-05-02-r1` showed first-run lows were route/analyzer volatility, not Stage180-caused. Hard-holdout-2 smoke `Output/from_sibling_pdfaf_v1_hard_2/run-stage180-hard2-smoke-2026-05-02-r1` preserved `3510` at `93/A` and `4705` at `92/A`; `4105` remains parked mixed residual debt.
- Stage 181 keeps a narrow stable hidden-alt lift. Diagnostic `Output/experiment-corpus-baseline/stage181-hidden-alt-diagnostic-2026-05-02-r2` selected only `figure-4754`, `font-4172`, and `font-4057`; A-grade controls such as `long-4700` are explicitly skipped. The implementation targets distinct content-backed checker-visible figure refs or role-map figure refs with guarded `set_figure_alt_text` / `retag_as_figure`, requires bypass-cache reanalysis to preserve the alt/category gain, rejects PDF/UA or core-category drops, and skips late local font substitution only after an applied Stage181 hidden-alt mutation.
- Stage 181 validation: focused run `Output/experiment-corpus-baseline/run-stage181-target-hidden-alt-2026-05-02-r2` lifted `figure-4754` to `94/A`, `font-4172` to `93/A`, and `font-4057` to `98/A`, all with `false_positive_applied = 0`. Visual reports `stage181-visual-figure4754/font4172/font4057-2026-05-02-r1` found changed PDFs pixel-stable. Targeted protected repeat `Output/experiment-corpus-baseline/run-stage181-target-protected-repeat-2026-05-02-r1` repeated the Stage181 wins (`figure-4754 94/A`, `font-4172 93/A`, `font-4057 98/A`) while known controls `long-4516`, `short-4176`, and `long-4680` showed route/protected volatility without Stage181 firing.
- Stage 181 full original-50 run `Output/experiment-corpus-baseline/run-stage181-full-2026-05-02-r1` has in-run grades `45 A / 3 B / 1 C / 1 D / 0 F`, reanalyzed grades `45 A / 1 B / 1 C / 0 D / 3 F`, in-run mean `95.20`, reanalyzed mean `93.90`, median `98`, p95 wall passing, attempts `828`, and `false_positive_applied = 0`. Gate `Output/experiment-corpus-baseline/stage181-benchmark-gate-2026-05-02-r1` fails only `protected_file_regressions` on non-Stage181 rows `long-4516`, `short-4176`, and `long-4680`; do not broaden Stage181 to protected-volatility rows without new repeat evidence. Hard-holdout-2 smoke `Output/from_sibling_pdfaf_v1_hard_2/run-stage181-hard2-smoke-2026-05-02-r1` preserved `3510` at `93/A` and `4705` at `97/A`; `4105` remains parked mixed residual F.
- Stage 182 is diagnostic-only. `scripts/stage182-protected-reanalysis-evidence.ts` extends the protected closeout flow for Stage 181 reanalyzed Fs and controls. Fresh protected target `Output/experiment-corpus-baseline/run-stage182-target-protected-2026-05-02-r1` used `--write-pdfs --write-protected-debug-states`; diagnostic `Output/experiment-corpus-baseline/stage182-protected-reanalysis-evidence-2026-05-02-r1` ran 5 final/checkpoint repeats. Primary rows `structure-4076`, `long-4516`, and `long-4680` all classified as `same_buffer_floor_safe_repeat_available`; Stage181 controls `figure-4754`, `font-4172`, and `font-4057` also repeated floor-safe. The fresh target's protected selector already found safe final states where available, so no Stage182 behavior change was kept. `short-4176` remains `same_buffer_analyzer_variance_floor_unsafe` raw Python analyzer debt.
- Stage 183 is checkpoint-confirmation only. Full original-50 protected run `Output/experiment-corpus-baseline/run-stage183-full-2026-05-03-r1` passed Stage 41 gate `Output/experiment-corpus-baseline/stage183-benchmark-gate-2026-05-03-r1`: in-run mean `95.94`, median `98`, grades `47 A / 2 B / 0 C / 1 D / 0 F`; reanalyzed mean `94.56`, median `98`, grades `45 A / 3 B / 0 C / 0 D / 2 F`; p95 wall `81.31s`, attempts `845`, and `false_positive_applied = 0`. Key controls stayed non-F: `figure-4754 94/A`, `font-4172 84/B` reanalyzed (`93/A` in-run), and `font-4057 82/B`; remaining reanalyzed Fs are known analyzer-volatility rows `structure-4076` and `long-4470`. Hard-holdout-2 smoke `Output/from_sibling_pdfaf_v1_hard_2/run-stage183-hard2-smoke-2026-05-03-r1` preserved `3510` at `93/A` and `4705` at `92/A`; `4105` remains parked mixed residual F. Treat Stage 181/182/183 as the current best quality checkpoint with documented analyzer-volatility debt.
- Stage 184 keeps a narrow native/tagged heading reachability fix for custom numbered heading roles. Diagnostic `Output/from_sibling_pdfaf_v1_hard_2/stage184-hard2-mixed-structural-diagnostic-2026-05-03-r1` proved `v1-4105` had hundreds of content-backed custom roles like `Heading 1`/`Heading 2` but `0` checker-visible tree headings, so `normalize_heading_hierarchy` now recognizes those custom role names and standardizes them to `/H1`...`/H6`; it does not invent headings or broaden table/figure/scorer behavior.
- Stage 184 validation: focused run `Output/from_sibling_pdfaf_v1_hard_2/run-stage184-target-mixed-structural-2026-05-03-r1` moved `4105` from `43/F` to `68/D`, with `heading_structure=100`, `reading_order=86`, and controls `3510`, `3508`, and `4705` A-grade. Visual report `Output/from_sibling_pdfaf_v1_hard_2/stage184-visual-4105-2026-05-03-r1` found all 68 pages pixel-stable against the Stage 183 remediated PDF.
- Stage 184 full hard-holdout-2 run `Output/from_sibling_pdfaf_v1_hard_2/run-stage184-hard2-full-2026-05-03-r1` completed `20/20`, mean `30.60 -> 94.40`, median `31 -> 97`, grades `19 A / 0 B / 0 C / 1 D / 0 F`, attempts `295`, and `false_positive_applied = 0`; `4105` is now the lone `68/D` residual with table/alt debt, not a heading F. Original-50 protected run `Output/experiment-corpus-baseline/run-stage184-full-2026-05-03-r1` has in-run grades `46 A / 1 B / 2 C / 1 D / 0 F`, reanalyzed grades `44 A / 1 B / 2 C / 1 D / 2 F`, mean `95.24` in-run / `93.56` reanalyzed, p95 wall passing, attempts `847`, and `false_positive_applied = 0`. Gate `Output/experiment-corpus-baseline/stage184-benchmark-gate-2026-05-03-r1` fails only protected regressions on known parked rows `long-4516`, `short-4214`, and `short-4176`; Stage184 heading normalization was rejected or no-effect on those blockers, so do not treat them as Stage184-caused without new repeat evidence.
- Stage 185 is diagnostic-only. `scripts/stage185-protected-gate-attribution.ts` post-processes Stage 182 repeat evidence to classify Stage184 gate blockers without adding remediation behavior. Fresh protected target `Output/experiment-corpus-baseline/run-stage185-target-protected-2026-05-03-r1` used `--write-pdfs --write-protected-debug-states`; primary 5-repeat evidence `Output/experiment-corpus-baseline/stage185-protected-gate-attribution-primary-2026-05-03-r1` classified `long-4516` and `short-4214` as `same_buffer_floor_safe_repeat_available`, and `short-4176` as `same_buffer_analyzer_variance_floor_unsafe`. Stage184 heading normalization was no-effect/rejected on the primary blockers, so no Stage185 protected-selection or route guard behavior was kept.
- Stage 185 controls: `Output/experiment-corpus-baseline/stage185-protected-gate-attribution-controls-2026-05-03-r1` found floor-safe final repeats for `structure-4076`, `long-4683`, `figure-4754`, `font-4057`, `font-4156`, and `font-4699`; `long-4680` remains known protected volatility and `font-4172` is a below-floor control relative to its Stage42 floor. Hard-holdout-2 smoke `Output/from_sibling_pdfaf_v1_hard_2/run-stage185-hard2-smoke-2026-05-03-r1` preserved `3510 93/A`, `4705 97/A`, and `4105 68/D`. Next quality stage should target `4105` residual table/alt debt, not protected gate hacks.
- Stage 186 is diagnostic-only. `scripts/stage186-hard2-table-alt-diagnostic.ts` and `src/services/remediation/stage186Hard2TableAlt.ts` classify hard-holdout-2 `v1-4105` table/alt residual evidence without wiring a new planner path. Pre-diagnostic `Output/from_sibling_pdfaf_v1_hard_2/stage186-hard2-table-alt-diagnostic-pre-2026-05-03-r2` found prior explicit table repairs did not move `table_markup`, so the only plausible fast probe was bounded role-map alt ownership after table no-gain evidence.
- Stage 186 focused target `Output/from_sibling_pdfaf_v1_hard_2/run-stage186-target-table-alt-2026-05-03-r3` preserved `3510 93/A`, `3508 93/A`, `4705 97/A`, and `4105 68/D` with hard-target grades `3 A / 1 D / 0 F`. The Stage186 alt fallback probe improved checker-visible figure ownership/PDF-UA evidence internally but left total score and scored `table_markup`/`alt_text` unchanged, so behavior was not kept. Do not reintroduce `4105` table-first/role-map-alt transaction behavior without a more granular table or alt analyzer fix that produces a real category gain; keep `4105` parked as table/alt analyzer debt.
- Current-engine holdout 6 refresh `Output/from_sibling_pdfaf_v1_holdout_6/run-current-engine-2026-05-03-r1` completed `20/20`, mean `31.15 -> 92.90`, median `34 -> 98`, grades `17 A / 0 B / 1 C / 0 D / 2 F`, attempts `278`, and `false_positive_applied = 0`. Compared with Stage 171, `v1-4674` improved `59/F -> 97/A` and `v1-4740` improved `87/B -> 98/A`; regressions/low residuals are `v1-3506 59/F` (`heading_structure=0` OCR/manual), `v1-4673 59/F` (`heading_structure=0`, PDF/UA 67, backslid from Stage171 `79/C`), and `v1-4693 79/C` (heading/reading both 45, backslid from Stage171 `92/A`). The selected next family is heading tail recovery.
- Below-A rows from current-engine batch refreshes are tracked in `docs/current-batch-below-a-ledger.md`.
- Current-engine heading-zero 1 refresh `Output/from_sibling_pdfaf_v1_heading_zero_1/run-current-engine-2026-05-03-r1` completed `20/20`, mean `39.35 -> 98.25`, median `41 -> 99.5`, grades `19 A / 0 B / 1 C / 0 D / 0 F`, attempts `169`, and `false_positive_applied = 0`. Compared with Stage 171, old F `v1-3575` improved `59/F -> 97/A`; the only below-A row is `v1-4611 79/C` with `reading_order=55` and `link_quality=73` while `heading_structure=97`, so it is mixed reading-order/link ownership debt rather than heading-zero.
- Current-engine legacy 17 refresh `Output/from_sibling_pdfaf_v1_legacy_17_refresh/run-current-engine-2026-05-03-r1` completed `17/17`, mean `58.76 -> 94.82`, median `65 -> 97`, grades `15 A / 0 B / 2 C / 0 D / 0 F`, attempts `245`, and `false_positive_applied = 0`. Stage 144 low rows improved materially (`fixture-inaccessible 79/C -> 97/A`, `4485 59/F -> 97/A`, `4184 semantic candidate 71/C -> 100/A`); remaining below-A rows are `v1-legacy-4078... 77/C` and `v1-legacy-4188... 77/C`, both with `heading_structure=45` and `reading_order=45` while alt/link/table are strong.
- Current-engine edgecase corpus parallel-8 refresh `Output/from_sibling_pdfaf_edgecase_corpus/run-current-engine-parallel8-2026-05-03-r3` completed `50/50` with semantic disabled and no PDFs written. Result: mean `41.70 -> 90.54`, median `43.5 -> 97`, grades `37 A / 3 B / 3 C / 5 D / 2 F`, attempts `1009`, and `false_positive_applied = 0`. Parallelism `8` was resource-safe on this batch: CPU saturated, memory stayed above OOM risk, root disk stayed tight but stable, `/tmp` stayed safe; the tail job `v1-4730` dominated wall time at `443019ms` but completed `87/B`. Below-A rows are logged in `docs/current-batch-below-a-ledger.md`; the main residual clusters are mixed table/alt/PDF-UA (`4147`, `4453`, `4690`, `4694`, `4735`), native shell heading/reading-order (`4519`), form/reading-order (`4164`), and near-pass alt/PDF-UA/text-extractability rows (`4427`, `4503`, `4583`, `4730`, `4748`, `4761`).
- Current-engine all-remaining unique refresh `Output/all-remaining-current-engine/run-current-engine-parallel8-2026-05-03-r1` completed `203/203` with semantic disabled, no PDFs written, and per-file parallelism `8`. It deduped unprocessed `Input/` manifests by SHA-256 and reached mean `32.63 -> 94.69`, median `34 -> 97`, grades `187 A / 2 B / 4 C / 4 D / 6 F`, attempts `3032`, and `false_positive_applied = 0`. Parallel 8 completed without OOM; root disk stayed tight but stable, `/tmp` stayed safe, timing p95 was `214802ms`, and max row was OCR-heavy `v1_evolve_4-3513` at `668669ms`. Below-A rows are logged in `docs/current-batch-below-a-ledger.md`; durable residual clusters are OCR/manual zero-heading no-safe-title-owner rows (`3451`, `3459`, `3602`), mixed table/alt/PDF-UA analyzer debt (`4213`, `4105`, holdout4 `11`), native shell/partial heading-reading-order rows (`holdout4-03`, `holdout4-04`, `4171`, `4567`), and low-alt/near-pass rows (`4145`, `4767`, `4760`).
- Combined current unique-input snapshot across all `Input/**/manifest.json` files is `348/348` processed after SHA dedupe, using current local artifacts as of 2026-05-03. Distribution: `309 A / 6 B / 13 C / 10 D / 10 F`, mean `94.18`, median `97`. The canonical below-A list is appended to `docs/current-batch-below-a-ledger.md`; largest remaining families are heading/reading-order zero or partial (`holdout4-03`, `4519`, `3506`, `4673`, `3451`, `3459`, `3602`, `4635`, `4171`, `4567`, `4078`, `4188`, `4693`), mixed table/alt/PDF-UA (`4213`, `4105`, `4694`, `4690`, `4147`, `4453`, `4735`, `holdout4-11`), and low-alt/near-pass (`4145`, `4503`, `4748`, `4761`, `4767`).
- Stage 187 keeps a narrow heading/reading tail diagnostic and tagged-heading top-up path. `scripts/stage187-heading-reading-tail-diagnostic.ts` classifies combined below-A heading rows, and `src/services/remediation/stage187HeadingReadingTail.ts` only schedules existing `create_heading_from_tagged_visible_anchor` for non-OCR native-tagged below-A rows with partial heading score, no tree headings, strong first-page MCID/paragraph title evidence, and no severe mixed table/alt/form debt. No new Python mutator, scorer, OCR, font, table, figure, semantic, or protected-baseline behavior was added.
- Stage 187 focused target `Output/stage187-heading-reading-tail/run-stage187-target-2026-05-03-r1` reached `6 A / 1 B / 0 C / 0 D / 1 F` on selected heading/reading rows plus controls. The strongest real Stage187 win is `v1_evolve_2-4614`, where `create_heading_from_tagged_visible_anchor` applied under the Stage187 gate and the row reached `96/A`; visual report `Output/stage187-heading-reading-tail/stage187-visual-4614-2026-05-03-r1` found all 49 pages pixel-stable. `4427` reached `96/A` in the focused target with pixel-stable visual report `stage187-visual-4427-2026-05-03-r1`, but broader repeats show route variance, so do not count it as a guaranteed Stage187 win. `4673` remains parked no-safe/mixed heading-alt debt.
- Stage 187 full below-A rerun `Output/stage187-heading-reading-tail/run-stage187-below-a-2026-05-03-r1` improved the 39-row below-A slice from the canonical snapshot mean `70.74` to `73.36` and distribution from `0 A / 6 B / 13 C / 10 D / 10 F` to `6 A / 5 B / 10 C / 8 D / 10 F`; F count did not improve because remaining Fs are mostly OCR/manual no-safe-title-owner, mixed table/alt/PDF-UA, or analyzer/route volatility.
- Stage 187 original-50 protected run `Output/experiment-corpus-baseline/run-stage187-full-2026-05-03-r1` has in-run mean `96.0`, reanalyzed mean `94.7`, median `98`, gate grades `44 A / 4 B / 0 C / 0 D / 2 F`, p95 wall `81.20s`, attempts `831`, heading no-effect `3`, and `false_positive_applied = 0`. Stage 41 gate `Output/experiment-corpus-baseline/stage187-benchmark-gate-2026-05-03-r1` fails only `protected_file_regressions` on known volatility rows `long-4516` and `long-4683`; the new Stage187 heading top-up path did not fire on those rows or on reanalyzed Fs `structure-4076` and `long-4470`. Treat Stage187 as a quality candidate with protected/analyzer debt documented, not a clean gate-passing checkpoint.
- Stage 188 is diagnostic-only. `scripts/stage188-mixed-tail-diagnostic.ts` and `src/services/remediation/stage188MixedTail.ts` classify mixed table/alt/PDF-UA lows across the combined below-A snapshot, but no remediation behavior is active. Source-fallback diagnostic `Output/stage188-mixed-tail-diagnostic-2026-05-03-r1` selected `v1_hard_1-4213`, while remediated-PDF diagnostic `Output/stage188-mixed-tail-diagnostic-2026-05-03-r2` classified `4147` and `4453` as ordered table/alt/PDF-UA candidates, `4694` as explicit table repair, `4213` as hidden-alt/ordered debt, and `4105`/`holdout4-11` as prior no-gain table debt.
- Stage 188 focused mixed-only baseline used `/tmp/pdfaf-stage188-target-mixed-only-2026-05-03-r1` with written PDFs and semantic disabled: grades `3 A / 3 B / 2 C / 7 D / 1 F`. `4213` stayed `59/F`; `4105` stayed `68/D`; `4694` reached `68/D` but route variance was evident. A narrow late explicit-table post-pass was briefly tested in `/tmp/pdfaf-stage188-target-mixed-only-2026-05-03-r2`, but it produced no `stage188` tool rows and did not prove a behavior path (`4694` repeated `59/F`, `4213` stayed `59/F`, `font-4172` varied to `84/B`, while `holdout4-11` route-lifted to `94/A`). The unproven orchestrator hook was removed; do not reintroduce Stage188 mixed behavior without a category-moving repeat proof. Next likely lever is a smaller analyzer/acceptance investigation for hidden-alt no-gain rows like `4213` or route-stable table no-gain rows, not broad table/figure caps.
- Stage 189 is diagnostic-only. `scripts/stage189-hidden-alt-no-gain-diagnostic.ts` and `src/services/remediation/stage189HiddenAltNoGain.ts` classify hidden-alt no-gain evidence from Stage 188 artifacts without changing remediation behavior. Diagnostic output `Output/stage189-hidden-alt-no-gain-diagnostic-2026-05-03-r1` found no safe analyzer-alignment or target-selection behavior candidate: distribution was `3 no_safe_alt_path / 7 mixed_table_or_heading_blocker / 4 hidden_alt_target_beyond_existing_cap`, with `0` behavior-candidate rows.
- Stage 189 key finding: `4213` can expose or fill direct checker-visible alt targets, but scored `alt_text` remains `0` because wider role/ownership debt remains (`19` non-Figure roles and no safe role-map targets in the inspected Stage188 artifact). `4145`, `4748`, and `4767` are also cap/coverage-limited rather than stale-ref bugs. Do not raise broad figure-alt caps or retry hidden-alt tools for these rows without a new content-backed rule that actually moves scored `alt_text`.
- Stage 190 is diagnostic-only. `scripts/stage190-route-repeatability-diagnostic.ts` and `src/services/remediation/stage190RouteRepeatability.ts` classify route repeatability across four fresh focused repeats (`r1/r2/r3/write`) for mixed residuals. Diagnostic `Output/stage190-route-repeatability-diagnostic-2026-05-03-r1` found `0` behavior-candidate rows: `holdout4-11` had one externally preserved B route (`89/B`) but other A-looking routes reanalyzed to `69/D`; `4690` had one preserved B route (`82/B`) but did not repeat; `4694` had multiple in-run A routes and one written `98/A`, but other repeats collapsed to `59-68` on reanalysis. Do not add route/checkpoint preservation behavior from this evidence.
- Stage 190 prior-win controls stayed stable across all repeats: `3510`, `4705`, `3423`, `3429`, `3433`, `3443`, `3476`, `figure-4754`, `font-4057`, and `font-4172` all remained A/B on final reanalysis. Park `4213`, `4105`, `4147`, `4453`, `4735`, and `4145` for now; they showed no externally safe route. The next best direction is a narrower analyzer-evidence/role-ownership design or another fresh hard batch, not broad route guards.
- Stage 191 is diagnostic/evidence-only. `scripts/stage191-figure-role-alt-evidence-diagnostic.ts` and `src/services/remediation/stage191FigureRoleAltEvidence.ts` add bounded figure/subtree MCID evidence to explain low-alt rows, but no scorer, planner, or mutator behavior was changed. Focused written-PDF run `/tmp/pdfaf-stage191-target-2026-05-03-r1` plus diagnostic `Output/stage191-figure-role-alt-evidence-diagnostic-2026-05-03-r3` found `0` behavior-candidate rows: `4213` has real reachable raw `/Figure` missing-alt debt plus wider non-Figure debt (`checker 5/8`, `informative 5/27`, `nonFigureRoleCount 19`, `0` duplicate-covered MCIDs), `4145` remains role-map cap-limited (`26` role-map figure-like targets), and `4748`/`4767` still have many raw `/Figure` missing-alt targets. Do not add analyzer/scorer alignment from Stage191; the next direction should be either fresh hard-batch discovery or a more granular true missing-alt/semantic-alt design.
- Stage 192 is diagnostic-only. `scripts/stage192-true-missing-alt-diagnostic.ts` and `src/services/remediation/stage192TrueMissingAlt.ts` classify target-level true missing-alt debt into decorative/template/placeholder/semantic/role-map/blocked buckets, but no planner or mutator behavior was added. Diagnostic `Output/stage192-true-missing-alt-diagnostic-2026-05-03-r2` over Stage191 target artifacts found `0` behavior-candidate rows: target distribution was `53 meaningful_needs_semantic_alt`, `26 rolemap_retag_then_alt_candidate`, `257 table_or_heading_blocked_not_alt_first`, and `7 repeated_template_artifact_candidate`. The repeated-template candidates are only on `4748` and projected cleanup would keep `alt_text` flat (`20` vs current `20`), so artifacting them is not worth keeping. `4213` and `4767` need real semantic alt for meaningful reachable `/Figure` content; `4145` needs a more specific role-map/alt ownership policy, not broad caps. Next default direction is Stage193 semantic/manual-alt policy design or a fresh hard batch; do not add deterministic missing-alt cleanup from Stage192 without a category-moving proof.
- Stage 193 implements a bounded semantic-alt pilot policy, but no semantic PDF-changing validation ran during implementation because the existing `llama-server` process on the machine was not accepting connections on port `1234` and no `OPENAI_COMPAT_BASE_URL` was configured. `scripts/stage193-semantic-alt-diagnostic.ts` and `src/services/remediation/stage193SemanticAlt.ts` select only stable low-alt rows with content-backed reachable figure targets and block rows with table/heading/reading/link debt. Diagnostic `Output/stage193-semantic-alt-diagnostic-2026-05-03-r1` selected the intended pilot rows: `4748`, `v1_edge_mix-4145`, `v1_hard_1-4213`, and `v1_hard_1-4767`; it blocked `4453`, `4690`, `4735`, and `4105` as structure-first rows. The existing semantic figure lane now uses this Stage193 candidate policy, injects bbox/ownership/nearby-text context into prompts, and rejects decorative, empty, generic, document-title-only, too-short, or overlong LLM alt proposals.
- Stage 193 direct semantic-figure pilot with the VM-local multimodal LLM completed at `/mnt/pdf-review/stage193-semantic-figure-direct-2026-05-03-r1`. Results: `4748 82/B -> 97/A` (`alt_text 20 -> 100`, 12 accepted), `v1_edge_mix-4145 78/C -> 97/A` (`alt_text 20 -> 100`, 24 accepted), `v1_hard_1-4767 82/B -> 100/A` (`alt_text 20 -> 100`, 14 accepted but `pdf_ua_compliance 83 -> 71`, so needs guard/visual review before acceptance), and `v1_hard_1-4213` stayed `59/F` despite 3 accepted alt writes (`alt_text` stayed `0`), confirming wider analyzer/ownership debt. Treat Stage193 as promising but not accepted until full guards reject no-category/no-score and PDF/UA-regressing semantic changes.
- Stage 194 keeps semantic figure acceptance cleanup. Figure semantic mutations now require scored `alt_text` improvement and reject drops in core non-target categories (`heading_structure`, `reading_order`, `table_markup`, `pdf_ua_compliance`, `link_quality`) plus page/text/tag regressions; heading semantic lanes keep their prior acceptance semantics. Focused semantic run `/mnt/pdf-review/stage194-semantic-alt-guard-2026-05-03-r1` accepted clean A lifts for `4748 82/B -> 97/A`, `v1_edge_mix-4145 78/C -> 97/A`, and `v1_hard_1-4767 82/B -> 100/A` with stable PDF/UA on this repeat, and correctly reverted `v1_hard_1-4213` (`59/F`, `alt_text 0 -> 0`) as `semantic_no_target_category_improvement`. Visual reports `/mnt/pdf-review/stage194-visual-4748/4145/4767-2026-05-03-r1` were stable across all pages. Deterministic prior-win smoke `/mnt/pdf-review/stage194-prior-win-smoke-2026-05-03-r1` preserved `3476`, `3510`, `4705`, `figure-4754`, `font-4057`, and `font-4172` as A-grade.
- As of 2026-05-03, the VM-local `llama-server` is intended to run on `http://127.0.0.1:1234/v1` from `/mnt/pdf-review/pdfaf-llm/`: model `gemma-4-E2B-it-Q4_K_M.gguf`, projector `mmproj-F16.gguf`, and log `llama-server.log`. The repo path `data/llama-work/gemma-4-E2B-it-Q4_K_M.gguf` is a symlink to the data-drive model to keep root disk free. `/v1/models` reports `completion` and `multimodal`, and text/image chat smoke tests return OK. Set/reuse `OPENAI_COMPAT_BASE_URL=http://127.0.0.1:1234/v1` for local semantic runs to avoid spawning a duplicate LLM.
- All-Input semantic sweep 2026-05-04 is artifact-only. Run directory `/mnt/pdf-review/all-input-unique-semantic-2026-05-04-r1` deduped every PDF under `Input/` by SHA-256 (`350` unique files, broader than the prior manifest-only `348` snapshot), used the VM-local LLM at `http://127.0.0.1:1234/v1`, and wrote final PDFs only for below-A rows. Final summary: mean `36.54 -> 95.60` deterministic -> `95.77` semantic, median `34 -> 97 -> 97`, grades `329 A / 1 B / 6 C / 7 D / 7 F`, semantic ran on `22` rows with `28` accepted and `77` rejected proposals. Deterministic comparison in the same run was `328 A / 1 B / 5 C / 7 D / 9 F`, so semantics net-lifted two Fs but introduced/left one extra C and did not move median. Parallelism was adjusted from `5` to `3` to `2` due memory pressure; single local LLM requests must be globally serialized for broad semantic runs to avoid timeouts.
- Below-A rows from the all-input semantic sweep are dominated by heading/reachability (`structure-4076`, `4078`, `4171`, `4139`, `4215`, `4567`, `4635`, `4519`, `4605`, `holdout4-03`, `4693`), table/PDF-UA residuals (`4147`, `4453`, `4735`, `4105`), and semantic-alt ownership/scoring debt (`4213`, where `20` semantic accepts still left `alt_text=0`). Treat this run as a broad semantic signal, not a protected gate validation.
- Docker images were rebuilt locally and pushed on 2026-05-04 after the VM restart. API image `pdfaf-v2:local` / `hendo420/pdfaf-v2:{latest,engine-v2-general-2026-05-04}` is image ID/digest `sha256:62a37abaa4320f3ba289ff667f48745dcdafb289153e875e360453af80e024ee`; web image `pdf-af-web:local` / `hendo420/pdf-af-web:{latest,engine-v2-general-2026-05-04}` is image ID/digest `sha256:cb65bf07dae965826a059e2ec389280314af8ea26d85b0655c2f345552cfc76f`. Quick pre-push Docker API deterministic parity was clean on faster controls (`2023-focus`, `fixture-inaccessible`, `figure-4754`, `font-4057`); the long OCR parity control was intentionally stopped after VM restart per user direction to push images without more PDF runs.
- Fixed original-50 validation with parked PAC debt is complete from commit `6c23d88`. Run `Output/experiment-corpus-baseline/run-parked-debt-fixed50-2026-05-08-r1` reached reanalyzed mean `93.96`, median `98`, grades `43 A / 2 B / 1 C / 1 D / 2 F`, attempts `859`, and `false_positive_applied = 0`. Stage 41 gate `Output/experiment-corpus-baseline/parked-debt-fixed50-gate-2026-05-08-r1` failed `analyze_success`, `remediate_success`, `route_summary_coverage`, `protected_file_regressions`, and `runtime_p95_wall`: `structure-4438` remains the sole hard timeout/parked checkpoint debt, p95 is `241613ms`, and non-parked protected regressions are `figure-4702` (`87/B -> 59/F`, heading collapse) and `figure-4755` (`87/B -> 59/F`, alt/heading/PDF-UA collapse). Diagnostics are `parked-debt-fixed50-runtime-2026-05-08-r1`, `parked-debt-fixed50-pac-recovery-2026-05-08-r1`, and `parked-debt-fixed50-route-regression-2026-05-08-r1`. Do not make behavior changes directly from this run; next stage should diagnose `figure-4702` / `figure-4755` PAC-blocked useful repairs and route loss. `structure-4076` is still parked table/analyzer-applicability debt but completed at `70/C` in this run.
- PAC alignment for the five review PDFs is diagnostic-first. `scripts/pac-review-gap-diagnostic.ts` parses the external PAC one-page reports in `Output/review-five-a-pdfs-2026-05-08-r1/PAC Reports`, runs internal analysis/PAC evidence on `Output/review-five-a-pdfs-2026-05-08-r1/pdfs`, and writes local artifacts under `Output/review-five-a-pdfs-2026-05-08-r1/pac-gap-diagnostic/`. The first run shows external PAC still fails Content on all 5, PDF Syntax on all 5, Fonts on `font-4035`/`structure-3775`, Structure elements on `fixture-inaccessible`/`structure-3775`, Alternative Descriptions warnings on `fixture-inaccessible`, and Structure tree warnings on all 5. Most Content/PDF Syntax/Fonts/Structure-tree buckets now have matching internal PAC rule failures; the biggest missing internal failure coverage is still PAC Structure elements/Alternative Descriptions object-level detail. New evidence rows added in this pass are diagnostic-only: `pdfua.figure.bbox_present`, `pdfua.list.li_parent_valid`, `pdfua.list.lbl_lbody_parent_valid`, `pdfua.list.items_present`, and `pdfua.content.within_page_bounds`; no scoring/gates/planner/remediation behavior changed.
- PAC Grader Parity Stage 1 leaf coverage is implemented as evidence/reporting only. `docs/pac-grader-parity-map.md` records the PAC bucket-to-leaf coverage map from the five review PDFs, and `scripts/pac-review-gap-diagnostic.ts --leaf-map` now writes `pac-review-leaf-map.{json,md}` under the review output folder. The current five-PDF leaf map has `0 missing` leaf families, with `28 covered_verified`, `7 covered_heuristic`, `7 partial`, and `5 manual_review_only`; remaining non-covered families are explicit work items rather than silent grader gaps. New diagnostic-only PAC rows include content split rows (`no_artifact_in_tagged_content`, `no_tagged_content_in_artifact`, `marked_content_stack_valid`, `external_reference_xobjects_absent`, `characters_unicode_mappable`), heading rows, annotation nesting rows, formula alt, and non-Figure/empty alt ownership rows. No scoring, PAC gates, planner routing, remediation tools, API fields, or timeout defaults changed.
- PAC Grader Strictness Stage 1 is implemented as a scorer/reporting change. `finalizeScoringEvidence` now uses a rule-to-cap PAC policy: baseline metadata/language/alt/form PAC failures still cap at `89`, while direct verified PAC blocker leaves for ParentTree/structure syntax, content tagging, annotation structure, headings with object refs, lists, and tables cap their mapped category at `79`. Font/CMap, heuristic Unicode mapping, external XObjects, AI/link/manual-review-only rows, and non-object-backed heading evidence remain diagnostic-only. `scripts/pac-review-gap-diagnostic.ts` now reports score-active leaves in the `Scoring Influence` column; regenerated five-review output shows active caps mainly on content tagging and parent-link syntax while overall grades can remain A because some capped categories are non-graded or low-weight. No remediation gates, planner routing, mutation behavior, API fields, or timeout defaults changed.
- PAC Grader Strictness Stage 1 validation is complete. Five-review diagnostic was regenerated under `Output/review-five-a-pdfs-2026-05-08-r1/pac-gap-diagnostic/`. Fixed original-50 run `Output/experiment-corpus-baseline/run-pac-strict-grader-fixed50-2026-05-08-r1` processed the fixed 50 only: reanalyzed mean `89.7` in run summary, Stage 41 gated mean `90.35`, median `93`, `49/50` remediation success, `false_positive_applied = 0`, and F count `2`. Stage 41 gate `Output/experiment-corpus-baseline/pac-strict-grader-fixed50-gate-2026-05-08-r1` failed runtime/protected criteria (`structure-4438` hard timeout, p95 `243528ms`, attempts `942`, protected/route volatility), not the mean goal. Most frequent strict caps were `structure.parent_links_valid` (`29` rows), `content.orphan_mcids_absent` (`12`), table header association/header-cell rules (`6` each), and path-paint tagging (`5`). Next remediation work should diagnose score-moving strict-cap rows with stable object targets; do not broaden PAC gates or restore font/CMap scoring caps.
- PAC Strict-Cap Remediation Target Diagnostic is selection-only. `scripts/pac-strict-cap-target-diagnostic.ts` reads strict benchmark score caps plus five-review PAC leaf output and writes local matrices under `Output/experiment-corpus-baseline/pac-strict-cap-target-diagnostic-2026-05-08-r1/`. Decision doc `docs/pac-strict-cap-remediation-target-decision.md` selects `table_header_structure` as the first remediation target family, specifically `pdfua.table.header_association_present` and `pdfua.table.header_cells_associated` on `figure-4754`, `font-4699`, and `long-4700`. Parent-link caps are more frequent but mostly A-grade or need object identity; font/CMap remains diagnostic-only; `structure-4438` and `structure-4076` stay parked. Next behavior stage should be a table/header object diagnostic before any `normalize_table_structure` changes.
- PAC Table/Header Target Diagnostic is complete and diagnostic-only. `scripts/pac-table-header-target-diagnostic.ts` reanalyzes the strict fixed-50 target/control PDFs and writes local evidence under `Output/experiment-corpus-baseline/pac-table-header-target-diagnostic-2026-05-08-r1/`; source decision doc is `docs/pac-table-header-target-diagnostic.md`. The three low-score target rows (`figure-4754`, `font-4699`, `long-4700`) all have stable table `structRef` evidence plus direct missing header association / orphan TH / TD-without-header counts, while direct-cell and row-irregularity signals are zero. The next behavior stage should be a narrow `set_table_header_cells` association probe, not broad `normalize_table_structure`; A-grade controls (`fixture-accessible`, `figure-4753`, `long-4608`) show residual caps can remain on high-grade rows, so mutation must be limited to below-A rows with direct audit evidence and stable table identity.
- PAC Table Header Association Probe is partially promoted as a narrow small-table behavior. Python `set_table_header_cells` can now add `/Scope`, `/ID`, and `/Headers` associations to existing TH/TD tables without changing table shape, and planner targeting only uses that path on below-A rows with strict table PAC caps, direct `tableHeaderAudit` debt, stable table refs, and small bounded debt (`tablesChecked <= 4`, missing associations `<= 2`, TDs without headers `<= 12`). Validation r1 showed broad targeting was unsafe/incomplete (`figure-4754` route-dropped, `long-4700` remained capped), so r2 narrowed the policy: `font-4699` reached `91/A` and cleared table caps; controls stayed stable; `figure-4754` and `long-4700` are intentionally left for a separate many-table association batching stage. Source validation doc: `docs/pac-table-header-association-probe-validation.md`; generated runs stay under `Output/experiment-corpus-baseline/run-table-header-association-target-2026-05-08-r1` and `...-r2`.
- PAC Table Header Batch Probe is implemented but not fixed-50-ready. `scripts/pac-table-header-batch-diagnostic.ts` writes local evidence under `Output/experiment-corpus-baseline/pac-table-header-batch-diagnostic-2026-05-08-r1`; source validation doc is `docs/pac-table-header-batch-probe-validation.md`. Python `set_table_header_cells` now supports `structRefs` batch association params, but planner batching is narrowed to high-volume direct association debt only (`headerAssociationMissingCount >= 4`, `dataCellsWithoutHeaderCount >= 100`, zero direct/misplaced/irregular table signals). Validation r2 shows `long-4700` table association debt improved (`10 -> 2` missing associations, `220 -> 17` TDs without headers) while score stayed `78/C`; `font-4699` stayed `91/A`; `figure-4754` is no longer batched but still route-dropped to `67/D`; `fixture-inaccessible` route volatility resurfaced at `79/C` with no table batch tools. Do not run fixed-50 from this stage until `figure-4754` and fixture route volatility are isolated.
- Table Batch Route Volatility Diagnostic is diagnostic-only; no remediation behavior changed. `scripts/table-batch-route-volatility-diagnostic.ts` compares good/bad run artifacts and writes local output under `Output/experiment-corpus-baseline/table-batch-route-volatility-diagnostic-2026-05-08-r1`; source doc is `docs/table-batch-route-volatility-diagnostic.md`. Result: no same-state guard candidates. `figure-4754` is `upstream_route_volatility` from the initial replay state (`78/C -> 67/D`) and should remain parked until repeat diagnostics find a same-state decision point. `fixture-inaccessible` is `PAC_blocked_useful_repair` (`95/A -> 79/C`) because later `repair_native_link_structure` is missing, but first divergence is upstream state drift at `artifact_repeating_page_furniture`, so no broad PAC exception or route guard is justified yet. Next best stage is a fixture-only PAC-blocked link recovery isolation.
- Fixture-Inaccessible PAC-blocked Link Recovery Isolation is diagnostic-only; no remediation behavior changed. `scripts/fixture-link-recovery-diagnostic.ts` writes local evidence under `Output/experiment-corpus-baseline/fixture-link-recovery-diagnostic-2026-05-08-r1`; source doc is `docs/fixture-link-recovery-diagnostic.md`. Result: `fixture-inaccessible` is `upstream_route_volatility`, not a direct rejected-link PAC recovery gap. Good route applies `repair_native_link_structure` with existing `pac_orphan_mcid_recovery(repair_native_link_structure)` and reaches `95/A`; bad route never schedules native link repair, starts `artifact_repeating_page_furniture` from a different replay state, applies no-score `mark_untagged_content_as_artifact`, and stays `79/C`. Do not add a PAC exception or route guard from this evidence. Next best stage is a small route-repeatability collection for `fixture-inaccessible` and `figure-4754` before table batch promotion or fixed-50.
- Route Repeatability Collection Before Table Batch Promotion is diagnostic-only; no behavior was kept. `scripts/route-repeatability-table-batch-diagnostic.ts` writes local output under `Output/experiment-corpus-baseline/route-repeatability-table-batch-diagnostic-2026-05-08-r1`; source doc is `docs/route-repeatability-table-batch-diagnostic.md`. Three targeted repeats showed `fixture-inaccessible` volatile (`79/C`, `95/A`, `95/A`) with one same-state artifact candidate, but the narrow guard probe `run-route-repeatability-table-batch-guard-target-2026-05-08-r1` still landed `79/C` through a different upstream state, so that behavior was reverted/not kept. `figure-4754` remains upstream route volatility (`78/C`, `67/D`, `78/C`) and should not drive table behavior. `long-4700` table batching is stable but score-flat (`78/C` all repeats, table association debt `10 -> 2`, TD debt `220 -> 17`); `font-4699` remains stable `91/A`. Do not run fixed-50 from table batch until route volatility is parked by policy or a broader route-determinism project is opened.
- Table Batch Validation With Parked Route Debt is complete from current table-batch behavior with no new remediation/scoring/gate changes. Source doc: `docs/table-batch-parked-debt-validation.md`; diagnostic script: `scripts/table-batch-acceptance-diagnostic.ts`. Targeted run `Output/experiment-corpus-baseline/run-table-batch-parked-debt-target-2026-05-08-r1` and diagnostic `Output/experiment-corpus-baseline/table-batch-acceptance-target-diagnostic-2026-05-08-r1` returned `accept_table_batch_with_parked_debt`, `false_positive_applied = 0`, `0` non-parked regressions, `font-4699` stable `91/A`, and `long-4700` table debt reduced (`10 -> 2`, TD debt `220 -> 17`). Fixed original-50 run `Output/experiment-corpus-baseline/run-table-batch-parked-debt-fixed50-2026-05-08-r1` reached mean `91.18`, median `94`, grades `41 A / 3 B / 2 C / 2 D / 1 F`, `false_positive_applied = 0`, and only parked `structure-4438` hard-timed out; diagnostic `Output/experiment-corpus-baseline/table-batch-acceptance-fixed50-diagnostic-2026-05-08-r1` again returned `accept_table_batch_with_parked_debt` with `0` non-parked regressions. Stage 41 gate `Output/experiment-corpus-baseline/table-batch-parked-debt-fixed50-gate-2026-05-08-r1` still fails broader legacy gates (`structure-4438` timeout/coverage, runtime p95/median, attempts, and protected regression `figure-4702` already low under strict-grader baseline), but these are classified as route/runtime/strict-grader debt rather than table-batch causality. Keep current table batching; do not widen thresholds from this evidence.
- Figure-4702 Route Recovery Diagnostic is diagnostic-only; no behavior was kept. `scripts/figure4702-route-recovery-diagnostic.ts` writes local output under `Output/experiment-corpus-baseline/figure4702-route-recovery-diagnostic-2026-05-08-r1`; source doc is `docs/figure4702-route-recovery-diagnostic.md`. Result: `figure-4702` is `pac_blocked_structure_recovery_candidate`. Current strict/table-batch routes schedule score-moving structural recovery, but `remap_orphan_mcids_as_artifacts`, `repair_annotation_alt_text`, `repair_structure_conformance`, and `synthesize_basic_structure_from_layout` are rejected because the proposed state trips `pdfua.annotations.tagged_annotations_present` plus `pdfua.content.orphan_mcids_absent`. The best blocked proposal would move score `48 -> 77` and heading `0 -> 94`, but accepting it would require proving later link/annotation repair clears annotation PAC debt. Do not add a global annotation PAC exception or route guard from this evidence; next options are to park `figure-4702` for acceptance-with-parked-debt or open a dedicated structure-then-annotation sequencing probe.
- Figure-4702 Structure-Annotation Sequence Probe is implemented as a narrow row-specific behavior, but fixed-50 validation is deferred. Source doc: `docs/figure4702-structure-annotation-sequence-validation.md`. The probe accepts only the combined structural recovery plus bounded annotation/link cleanup when final PAC annotation debt is reduced, score/heading improve, page/text/tag evidence is preserved, and no harmful non-annotation/non-orphan PAC regressions appear; it does not change PAC scoring/gates or add mutators. Targeted run `Output/experiment-corpus-baseline/run-figure4702-sequence-target-2026-05-08-r1` recovered `figure-4702` to `91/A` with `structure_annotation_sequence_recovered` and `false_positive_applied = 0`; `long-4700` stayed `78/C`, `font-4699` stayed `91/A`, and `long-4683` reached `96/A`. Fixed-50 was not run because targeted controls were not clean: `structure-3775` repeated known route volatility at `79/C`, `long-4516` landed `84/B`, and parked `structure-4438` still timed out. Next stage should repeat/control-classify these misses or explicitly park them before fixed-50.
- Figure-4702 Probe Repeatability Before Fixed-50 is complete. New diagnostic script `scripts/figure4702-sequence-repeatability-diagnostic.ts` writes local output under `Output/experiment-corpus-baseline/figure4702-sequence-repeatability-diagnostic-2026-05-08-r1`; source doc is `docs/figure4702-sequence-repeatability-validation.md`. Three targeted repeats `run-figure4702-sequence-repeat-2026-05-08-r1/r2/r3` classified `figure-4702` as `sequence_stable` (`91/A` all repeats), `long-4700`/`font-4699` as `table_observation_stable`, `structure-4438` as parked runtime debt, and `fixture-inaccessible`/`figure-4754`/`long-4516`/`structure-3775` as parked route/runtime volatility, with `false_positive_applied = 0`. Fixed original-50 run `Output/experiment-corpus-baseline/run-figure4702-sequence-fixed50-2026-05-08-r1` reached Stage 41 candidate mean `90.45`, median `94`, fixed `figure-4702` to `91/A`, preserved `font-4699` at `91/A`, kept `long-4700` table-debt reduction at `78/C`, and had `false_positive_applied = 0`; run-summary reanalyzed mean is `89.92`. Gate `Output/experiment-corpus-baseline/figure4702-sequence-fixed50-gate-2026-05-08-r1` still fails broader gates due parked `structure-4438` timeout/coverage, p95 `246350ms`, attempts `912`, and non-sequence protected regressions led by `font-3448` and `long-4680`. Do not patch directly from this validation; next stage should diagnose `font-3448`/`long-4680` protected regressions or open a runtime-tail/attempt-reduction stage.
- Protected Regression And Runtime Tail Split is diagnostic-only. `scripts/protected-regression-runtime-split-diagnostic.ts` compares Stage 42, the strict/table baseline, and current `figure4702` fixed-50 artifacts; source doc is `docs/protected-regression-runtime-split-diagnostic.md`; local output is `Output/experiment-corpus-baseline/protected-regression-runtime-split-diagnostic-2026-05-08-r1`. Classification: `font-3448` is `route_volatility` (`86/93 -> 51`, heading `98 -> 0`, strict-to-current same-state `tag_native_text_blocks` applied vs rejected), while `long-4680` is `final_reanalysis_drift` (`80/B` in-run to `59/F` protected reanalysis, `21` point drop). Runtime split: `long-4683`, `structure-4076`, and `figure-4702` are repeated no-gain/rejected churn; `long-4516` is long successful recovery; `long-4680` is final-reanalysis tail; `structure-4438` remains parked hard-timeout/checkpoint debt. No behavior changed. Next best score stage is `long-4680` reanalysis drift/checkpoint eligibility, with `font-3448` route/floor divergence separate; speed work should target churn/admission only after score blockers are classified.
- Long-4680 Protected Drift Diagnostic is diagnostic-only; no checkpoint preservation behavior was added. `scripts/long4680-protected-drift-diagnostic.ts` writes local output under `Output/experiment-corpus-baseline/long4680-protected-drift-diagnostic-2026-05-08-r1`; source doc is `docs/long4680-protected-drift-diagnostic.md`. Result: classify `long-4680` as `real_pdf_regression`, not safe analyzer drift. The in-run `80/B` state reanalyzes to `59/F` in all 5 protected repeats (`floorSafeIndexes=[]`), and protected reanalysis exposes scored checker-facing alt/table applicability debt: `alt_text 100/not-applicable -> 0/applicable` with `8` missing-alt figures, `table_markup 100/not-applicable -> 92/applicable`, and heading `78 -> 60`. Do not preserve the in-run checkpoint; next score target should be `font-3448` same-state `tag_native_text_blocks` divergence or a real `long-4680` alt/table repair, not checkpoint hiding.
- Font-3448 Native Tagging Recovery is implemented as a narrow useful-repair recovery, not a PAC policy broadening. Diagnostic `Output/experiment-corpus-baseline/font3448-native-tagging-diagnostic-2026-05-08-r1` confirmed same-state replay `39be10e26232bf205f091beb`: strict/table baseline accepted `tag_native_text_blocks` (`44 -> 83`, heading `0 -> 98`, reading `0 -> 79`) while the current route rejected it only for `pdfua.content.orphan_mcids_absent` despite heading/reading gains. `pacRuleUsefulRepairRecovery` now allows `pac_orphan_mcid_recovery(tag_native_text_blocks)` only when all PAC regressions are orphan-MCID regressions, total score improves, heading and reading order improve, and page/text/tag evidence is preserved. Targeted run `Output/experiment-corpus-baseline/run-font3448-native-tagging-target-2026-05-08-r1` recovered `font-3448` to `93/A`; fixed-50 `Output/experiment-corpus-baseline/run-font3448-native-tagging-fixed50-2026-05-08-r1` repeated `font-3448 93/A`, Stage 41 mean `91.02`, median `93`, F count `0`, protected regressions `0`, and `false_positive_applied = 0`. Gate `Output/experiment-corpus-baseline/font3448-native-tagging-fixed50-gate-2026-05-08-r1` still fails only operational/runtime/coverage gates (`structure-4438` timeout/coverage, p95/median runtime, attempts). No PAC scoring caps, PAC gate allow-list changes, timeout increases, table thresholds, API fields, AI defaults, planner broadening, or new repair tools changed. Source doc: `docs/font3448-native-tagging-recovery.md`.
- Runtime Tail And Attempt Reduction Stage is diagnostic-only; no runtime suppression behavior was added. New script `scripts/runtime-tail-attempt-diagnostic.ts` compares Stage 42 against `Output/experiment-corpus-baseline/run-font3448-native-tagging-fixed50-2026-05-08-r1` and writes local output under `Output/experiment-corpus-baseline/runtime-tail-attempt-diagnostic-2026-05-09-r1`; source doc is `docs/runtime-tail-attempt-diagnostic.md`. Classification: `structure-4438` is `parked_hard_timeout`; `long-4516` and `long-4683` are `quality_gain_runtime_tradeoff`; `figure-4702` is the only `optional_postpass_churn` guard candidate but needs targeted row/tool-state proof before suppression because it is a recovered `91/A` row; `long-4680` is `final_reanalysis_tail` with real protected drop; `structure-4076`, `font-4057`, and `figure-4754` are `residual_score_debt_not_runtime_fix`. Keep behavior unchanged until a targeted `figure-4702` optional-postpass probe proves final `91/A`, `false_positive_applied = 0`, and no control regressions.
- Figure-4702 Optional Post-Pass Proof is implemented as a narrow row-specific runtime guard. New script `scripts/figure4702-postpass-proof-diagnostic.ts` writes local proof output under `Output/experiment-corpus-baseline/figure4702-postpass-proof-diagnostic-2026-05-09-r1`; source doc is `docs/figure4702-postpass-proof-validation.md`. The guard fires only for `figure-4702` after `structure_annotation_sequence_recovered`, score-moving `repair_alt_text_structure` reaches `>=91/A`, and `set_pdfua_identification` has been attempted; it records `figure4702_sequence_postpass_guard` and skips only late optional orphan/font post-pass tail work. Targeted r2 `Output/experiment-corpus-baseline/run-figure4702-postpass-guard-target-2026-05-09-r2` kept `figure-4702` at `91/A`, reduced wall time `204807ms -> 188261ms`, reduced attempts `22 -> 20`, preserved `font-3448 93/A`, `font-4699 91/A`, `long-4700 78/C`, and had `false_positive_applied = 0`; `structure-4076`/`structure-4438` remain parked. Fixed-50 `Output/experiment-corpus-baseline/run-figure4702-postpass-guard-fixed50-2026-05-09-r1` kept mean `91.04`, median `94`, `false_positive_applied = 0`, improved p95 vs the prior font3448 fixed run (`243445ms -> 237163ms`) and attempts (`936 -> 916`), but Stage 41 gate `Output/experiment-corpus-baseline/figure4702-postpass-guard-fixed50-gate-2026-05-09-r1` still fails Stage42-relative operational gates due parked `structure-4438`, `long-4683` route/runtime volatility, and total attempts. Do not broaden the guard from this evidence.

## Parked Or Sensitive Areas

- Stage 75 local font substitution should remain guarded; font controls `font-4156`, `font-4172`, and `font-4699` are regression controls.
- Stage 162 near-pass rows `v1-v1-3468` and `v1-v1-4766` are regression controls. `v1-v1-4761` remains visually risky/parked and should not be forced through a broad PDF/UA cleanup.
- OCR title-owner rows `3451`, `3459`, and `3602` remain parked unless a safe MCID-backed title-owner bridge is proven.
- Analyzer-volatility rows should not drive new fixer acceptance unless repeat diagnostics prove a quality-preserving deterministic fix.
- Current fixed-50 acceptance diagnostic after local `Output/` cleanup is source-tracked in `docs/current-fixed50-acceptance-diagnostic.md`; script is `scripts/current-fixed50-acceptance-diagnostic.ts`. Fresh deterministic no-PDF run `Output/experiment-corpus-baseline/run-goal-current-fixed50-2026-05-09-r1` reached reanalyzed mean `91.06`, median `93`, `false_positive_applied = 0`, attempts `893`, and p95 wall `111162.82ms`, but it is not a literal Stage 41 gate because the Stage 42 protected baseline artifact was deleted during cleanup. Current blockers are non-parked `long-4516` hard timeout and `font-4057 38/F`; parked/volatile rows remain `structure-4438`, `structure-4076`, `fixture-inaccessible`, `figure-4754`, and `structure-3775`. Next checkpoint should run focused diagnostics/repeats for `long-4516` and `font-4057`, not broad PAC/scoring/planner changes.
- Targeted blocker repeat `Output/experiment-corpus-baseline/run-goal-blocker-repeat-2026-05-09-r1` shows `font-4057` repeatably stuck at `38/F`, while `long-4516` recovered to `84/B` and should be treated as runtime/route-tail volatility rather than a deterministic hard-timeout blocker. Controls stayed strong (`figure-4702 91/A`, `font-3448 93/A`, `font-4699 95/A`, `font-4035 95/A`, `long-4700 86/B`), but `long-4683` showed protected/reanalysis volatility (`96/A` in-run to `60/D` reanalyzed). Next best branch is a `font-4057` structure/annotation/table diagnostic before any behavior, plus targeted `long-4516` runtime repeat only if p95 remains the active gate.
- Font-4057 structure/annotation diagnostic is source-tracked in `docs/font4057-structure-annotation-diagnostic.md`; script is `scripts/font4057-structure-annotation-diagnostic.ts`. Local output `Output/experiment-corpus-baseline/font4057-structure-annotation-diagnostic-2026-05-09-r1` classifies `font-4057` as `mixed_table_alt_annotation_debt`: best blocked proposal moves score `38 -> 61` and heading `0 -> 96`, but leaves table `44` and reading `0`, with all structure/table proposals rejected by `pdfua.annotations.tagged_annotations_present`. Do not clone the `figure-4702` sequence or add a global annotation PAC exception; any future behavior must prove final annotation cleanup plus table/alt movement for this row.
- Long-4516 post-pass guard is a narrow runtime pilot documented in `docs/long4516-postpass-guard-validation.md`. It is row-scoped to `4516` and only skips orphan-drain post-pass after a score-moving `set_pdfua_identification` has already brought the row to at least `84/B`; it does not skip score-moving orphan cleanup. Targeted validation `Output/experiment-corpus-baseline/run-long4516-postpass-guard-target-2026-05-09-r1` completed `long-4516` at `92/A`; the guard did not fire because that route's orphan drain was beneficial (`89 -> 92`), so no fixed-50 run is justified from this stage. Remaining non-clean targeted rows are `font-4057 38/F` and volatile `long-4683` (`92/A` in-run, `60/D` reanalyzed).
- Hard-timeout repeat `Output/experiment-corpus-baseline/run-goal-runtime-hardtimeout-repeat-2026-05-09-r1` showed `long-4516` can still timeout before reaching a safe floor: timeout trace last phase `verified_checkpoint`, last checkpoint `78/C`, eligibility `checkpoint_below_floor(78<80)`. Do not lower the `long-4516` floor or return that checkpoint. The same repeat kept `figure-4702 91/A` and `font-3448 93/A`, had `structure-4076 59/F` in-run / `48/F` reanalyzed without hard timeout, parked `structure-4438` hard timeout with best checkpoint `36/F`, and `long-4683 92/A` in-run / `59/F` reanalyzed.
- Long-4516 Runtime Route Diagnostic is source-tracked in `docs/long4516-runtime-route-diagnostic.md`; script is `scripts/long4516-runtime-route-diagnostic.ts`. Local output `Output/experiment-corpus-baseline/long4516-runtime-route-diagnostic-2026-05-09-r1` classifies the current blocker as `metadata_acceptance_volatility`: the good `92/A` route and low `84/B` route start metadata repair from the same replay state `59847e143d407cb8277da61e`, but `set_document_language`/`set_document_title` apply in the good route and reject in the low route after analyzer reanalysis reports unrelated structure/alt/table drops. The hard-timeout repeat still has only a below-floor `78/C` checkpoint, so do not lower the `long-4516` floor. Any future behavior should be a narrow metadata-only confirmation probe for this row/state with targeted controls, not a broad checkpoint/PAC/planner change.
- Long-4516 metadata confirmation probe is implemented as a row-specific runtime/route-volatility guard. `shouldConfirmLong4516MetadataVolatility(...)` fires only for a `4516` metadata-only stage where title/language improved but the first reanalysis reports a severe unrelated alt/table drop; it runs one bypass-cache confirmation reanalysis and uses it only if score is at least pre-stage and `title_language` improves. It does not lower checkpoint floors, change PAC scoring/gates, raise timeouts, or accept stale analysis. Targeted validation `Output/experiment-corpus-baseline/run-long4516-metadata-confirm-target-2026-05-09-r1` completed `long-4516 89/B` with no hard timeout and preserved controls (`figure-4702 91/A`, `font-3448 93/A`, `font-4699 95/A`, `long-4700 86/B`, `font-4035 95/A`, `fixture-accessible 96/A`, `figure-4753 97/A`), but fixed-50 is still blocked because `font-4057` repeated `38/F` and `long-4683` reanalyzed `59/F`. Treat those as separate score/protected-volatility branches, not metadata-probe regressions.
- Long-4516 metadata confirmation fixed-50 measurement `Output/experiment-corpus-baseline/run-long4516-metadata-confirm-fixed50-2026-05-09-r1` reached reanalyzed mean `90.18`, median `93`, remediation success `49/50`, attempts `918`, p95 wall `240005.75ms`, and `false_positive_applied = 0`; the only hard timeout is parked `structure-4438`. `long-4516` completed at `87/B` and `long-4683` at `91/A`, both with `verified_checkpoint_timeout_return`. The run is still blocked by non-parked low rows `short-4074` (`95/A` in-run but `59/F` after reanalysis because alt evidence becomes applicable) and `font-4057 38/F`, plus parked `structure-4076 42/F` and route-volatility `structure-3775 79/C`. Source doc updated at `docs/current-fixed50-acceptance-diagnostic.md`; local diagnostic output is `Output/experiment-corpus-baseline/current-fixed50-acceptance-diagnostic-long4516-metadata-confirm-2026-05-09-r1`. Next best checkpoint is `short-4074` protected/reanalysis drift isolation before any further runtime suppression or fixed-50 acceptance.
- Short-4074 Protected Drift Diagnostic is diagnostic-only; no behavior was added. Script `scripts/short4074-protected-drift-diagnostic.ts` and source doc `docs/short4074-protected-drift-diagnostic.md` classify the fixed-50 artifact `Output/experiment-corpus-baseline/run-long4516-metadata-confirm-fixed50-2026-05-09-r1` as `protected_reanalysis_figure_applicability_drift`: `short-4074` is `95/A` in-run but reanalyzes to `59/F` when extracted figures change `0 -> 1`, alt becomes applicable, and new PAC figure-alt reasons appear. Three focused deterministic repeats (`run-short4074-protected-drift-repeat-2026-05-09-r1/r2/r3`) did not reproduce the failure (`95/A`, `95/A`, `91/A`, extracted figures `0`), so do not patch or checkpoint-preserve this row without same-buffer protected debug proof. Stage 42 protected baseline artifacts are still missing locally, blocking exact Stage 41 protected debug capture/gate until restored or regenerated. Next work should classify/address `font-4057` or restore the Stage 42 baseline for literal acceptance.
- Current fixed-50 parked-debt reclassification after the `short-4074` diagnostic is source-tracked in `docs/current-fixed50-acceptance-diagnostic.md`. Diagnostic output `Output/experiment-corpus-baseline/current-fixed50-acceptance-diagnostic-long4516-metadata-confirm-parked-2026-05-09-r1` on `run-long4516-metadata-confirm-fixed50-2026-05-09-r1` reports mean `90.18`, median `93`, remediation success `49/50`, `false_positive_applied = 0`, hard timeout rows only `structure-4438`, and `0` non-parked timeout/low-score rows. Parked rows are `structure-4438` runtime/checkpoint debt, `structure-4076` analyzer/table-applicability debt, `short-4074` analyzer/figure-applicability drift, `font-4057` mixed table/alt/annotation residual debt, and route-volatility rows `figure-4754`/`structure-3775`. No engine behavior changed. Exact Stage 41 still cannot be run locally until the missing Stage 42 protected baseline artifact is restored or regenerated.
- Protected Fixed-50 Goal Audit on 2026-05-09 is source-tracked in `docs/protected-fixed50-goal-audit-2026-05-09.md`. The missing Stage 42 baseline was regenerated from historical commit `f19eab5` into `Output/experiment-corpus-baseline/run-stage42-full-2026-04-21-r7`, then current protected fixed-50 run `Output/experiment-corpus-baseline/run-goal-protected-fixed50-2026-05-09-r1` was gated at `Output/experiment-corpus-baseline/goal-stage41-gate-protected-fixed50-2026-05-09-r1`. Gate still fails: mean floor passes (`90.35`) and `false_positive_applied = 0`, but protected regressions `2`, p95/median runtime, attempts, route coverage, and success gates fail. Current protected diagnostic `Output/experiment-corpus-baseline/current-fixed50-acceptance-diagnostic-protected-2026-05-09-r1` identifies non-parked low rows `figure-4702`, `long-4470`, `long-4683`, and `long-4700`; the goal is not complete. A narrow `figure-4702` alt-in-sequence probe recovered single-row `91/A` but failed broader targeted validation (`figure-4702` repeated `59/F`), so that behavior was reverted/not kept. Next work should diagnose protected-baseline route volatility for those four non-parked rows before any new behavior.
- Protected Fixed-50 Route Diagnostic is diagnostic-only; no behavior was kept. Script `scripts/protected-fixed50-route-diagnostic.ts` and source doc `docs/protected-fixed50-route-diagnostic.md` classify the non-parked protected run rows from `run-goal-protected-fixed50-2026-05-09-r1`: `figure-4702` = `protected_route_volatility` with same-state replay `0f98a17f90dd7ca07d207a67` (`remap_orphan_mcids_as_artifacts` accepted as `structure_annotation_sequence_recovered` in reference, rejected by `pdfua.annotations.tagged_annotations_present` in protected), `long-4470` = upstream `protected_route_volatility`, `long-4683` = `protected_final_reanalysis_drop`, and `long-4700` = `protected_route_volatility`. The rejected `figure-4702` alt-in-sequence probe remains reverted/not kept because single-row protected validation reached `91/A` but broader targeted subset repeated `59/F`. Next work should collect protected-route repeats or prove a narrower same-state guard; do not run fixed-50 again until targeted protected validation is clean.
- Protected route repeats after reverting the rejected probe confirm run-context volatility rather than a clean behavior target. Focused repeat `Output/experiment-corpus-baseline/run-protected-route-repeat-focus-2026-05-09-r1` produced `figure-4702 59/F`, `long-4470 93/A`, `long-4683` hard timeout, and `long-4700 78/C`; single-row repeat `Output/experiment-corpus-baseline/run-figure4702-protected-current-repeat-2026-05-09-r1` recovered `figure-4702 91/A` on current code. Do not patch `figure-4702` from single-row success; isolate multi-row protected route context or park it explicitly if the acceptance policy allows. `long-4470` appears volatile/recoverable, `long-4683` remains runtime/final-reanalysis debt, and `long-4700` is stable residual strict table/header debt.
- Protected fixed-50 parked-debt closeout is source-tracked in `docs/current-fixed50-acceptance-diagnostic.md`. After regenerating the Stage 42 baseline from historical commit `f19eab5`, current protected run `Output/experiment-corpus-baseline/run-goal-protected-fixed50-2026-05-09-r1` failed Stage 41 at `Output/experiment-corpus-baseline/goal-stage41-gate-protected-fixed50-2026-05-09-r1`: Stage 41 mean/median `90.35/94`, `false_positive_applied = 0`, only hard timeout `structure-4438`, but protected regressions, runtime p95/median, attempts, route coverage, and success gates still fail. Parked-debt diagnostic `Output/experiment-corpus-baseline/current-fixed50-acceptance-diagnostic-protected-parked-2026-05-09-r1` classifies every low-score/timeout row as parked (`figure-4702`, `figure-4754`, `long-4470`, `long-4700`, `structure-3775` route volatility; `long-4683` runtime/final-reanalysis debt; `structure-4438` runtime/checkpoint debt; `structure-4076` analyzer/table debt; `short-4074` analyzer/figure-applicability drift; `font-4057` residual mixed debt). This is not a clean Stage 41 pass, and raw benchmark reanalyzed mean is `89.08`; it is only acceptance-ready under the explicit parked-debt stop condition.
- All-input mean goal 2026-05-09 table/header lane is diagnostic-only so far. Focused 9-row table run `Output/goal-all-input-mean-2026-05-09-r1/run-focused-table-header-targets-2026-05-09-r1` moved mean `42.89 -> 63.44` but all rows remained below target. POC/PAC pass `Output/goal-all-input-mean-2026-05-09-r1/poc-strong-focused-table-header-r1` still found `45` failures, including `15` verified table-header failures. New helper `scripts/all-input-table-header-object-diagnostic.ts` and local output `Output/goal-all-input-mean-2026-05-09-r1/table-header-object-diagnostic-r1` classify the table lane as `0` association-only candidates, `8` `irregular_or_direct_table_shape`, and `1` `not_table_first`. Do not widen `set_table_header_cells` batching from this evidence; the next table branch should be strongly-irregular table-shape normalization/object identity diagnostics around rows like `4722`, `4765`, `4147`, and `font-4057`, using existing `normalize_table_structure` only if targeted protected reanalysis proves safe movement.
- All-input table trace sample `Output/goal-all-input-mean-2026-05-09-r1/table-tool-trace-r1` shows why the table lane is not a simple batching issue. On `4722`, `normalize_table_structure` can improve table markup `0 -> 16` but is rejected because `pdfua.table.header_association_present` worsens (`934 -> 966` / `945`). On `font-4057`, table/structure proposals can improve heading/table evidence but trigger `pdfua.annotations.tagged_annotations_present` with `28` unowned annotations; `set_table_header_cells` gives local header gains but does not move overall table markup. Do not add a single-step PAC allow-list; if continuing this lane, build a proposal-buffer sequence diagnostic for table/structure proposal plus bounded annotation/header cleanup and accept only final PAC-safe movement.
- All-input API semantic smoke `Output/goal-all-input-mean-2026-05-09-r1/api-semantic-heading-sample-r1` showed the running Docker API can lift some hard heading rows quickly (`v1-4655 46/F -> 97/A`, `4614 59/F -> 96/A`, `4646 50/F -> 89/B`), but also exposed an honesty bug on `4519`: final post-semantic `repair_alt_text_structure` dropped `51 -> 45` after other tools were rejected. Source now has `shouldKeepPostRemediationAltRepair(...)`, wired into `src/routes/remediate.ts` and `scripts/baseline-corpus-batch.ts`, to reject post-alt cleanup when total score or core non-alt categories regress; the orchestrator’s internal post-pass was already guarded. Focused source validation `Output/goal-all-input-mean-2026-05-09-r1/run-one-4519-regression-guard-2026-05-09-r1` completed `4519 35/F -> 60/D` without accepting a lower final cleanup state.
- Focused 12-row heading/reading validation after the post-alt guard, `Output/goal-all-input-mean-2026-05-09-r1/run-focused-heading-reading-targets-guard-2026-05-09-r1`, completed at mean `42.58 -> 59.25`; all rows improved, `4574` reached `93/A`, and `4519` completed `35/F -> 59/F`. The guard is validated as an honesty/runtime-path fix but is not sufficient for all-input mean recovery; next mean movement still needs either production/API semantic batch validation with the guard rebuilt into the API image or proposal-buffer sequence work for PAC-blocked structure/table candidates.
- All-input heading/annotation sequence is implemented as a narrow proposal-buffer recovery for IDs `4646` and `4593` only. Source doc: `docs/all-input-4646-sequence-probe.md`. It fires after `create_heading_from_candidate` applies, immediately runs existing `tag_unowned_annotations`, and accepts only when `pacRuleStructureAnnotationSequenceRecovery` proves final annotation PAC debt is reduced, page/text/tag evidence is preserved, score and heading improve, and `false_positive_applied` stays `0`; no PAC scoring/gate/timeout/planner broadening or new mutator was added. Four-row validation `Output/goal-all-input-mean-2026-05-09-r1/run-sequence-4646-target-2026-05-09-r3` moved `4646 50/F -> 94/A`; one-row trace `Output/goal-all-input-mean-2026-05-09-r1/sequence-4646-one-trace-r3` shows `create_heading_from_candidate` and `tag_unowned_annotations` accepted with `structure_annotation_sequence_recovered`; 12-row heading validation `Output/goal-all-input-mean-2026-05-09-r1/run-focused-heading-reading-targets-sequence-heading-2026-05-09-r1` kept `4646 95/A` and recovered `4593 42/F -> 91/A`. Direct/final-proposal probes for `4583`/`4655`/`4614`/`0297` showed possible local movement but did not translate through orchestrator validation (`run-sequence-final-probe-target-2026-05-09-r1`), so they are not in the kept ID set. Do not generalize this sequence without the same proposal-buffer proof and targeted validation.
- All-input alt/image lane is diagnostic-only. Source doc: `docs/all-input-alt-image-diagnostic.md`. Focused deterministic run `Output/goal-all-input-mean-2026-05-09-r1/run-focused-alt-image-targets-2026-05-09-r1` processed the eight `alt_debt` target rows and left them at `77/C` or `59/F`; POC output is `Output/goal-all-input-mean-2026-05-09-r1/poc-strong-focused-alt-image-r1`. Stage191 figure-role diagnostic `figure-role-alt-focused-r6` classified all eight as `mixed_table_or_heading_blocker`, and Stage192 true-missing-alt diagnostic `true-missing-alt-focused-r6` found `0` behavior candidates (`6` no-safe-alt-action, `2` table/heading blocked). Do not add a figure-alt mutator or alt-specific PAC recovery from this evidence; next all-input movement should focus on table/structure+annotation/header sequencing or source-side semantic validation with current honesty guards.
- `scripts/baseline-corpus-batch.ts` now includes deterministic `appliedTools` and `falsePositiveApplied` in `baseline_report.json` rows and passes the configured `REMEDIATION_PDF_TIMEOUT_MS` abort signal into both deterministic remediation passes for future all-input diagnostics; smoke artifact `Output/goal-all-input-mean-2026-05-09-r1/run-tool-output-timeout-smoke-2026-05-09-r1` confirmed the fields and `falsePositiveApplied=0` on sampled rows. This is benchmark/reporting-only and does not change remediation behavior.
- All-input table/structure tool diagnostic is source-tracked in `docs/all-input-table-structure-tool-diagnostic.md`. Tool-enriched run `Output/goal-all-input-mean-2026-05-09-r1/run-focused-table-header-targets-tools-2026-05-09-r1` finished 9 table/header rows with `falsePositiveApplied=0`, but all stayed below target (`59/F` or `69/D`) and runtime reached `304s`. Rejected structural/table proposals often project score-moving states but are blocked by `pdfua.annotations.tagged_annotations_present` or table-header association count increases. Do not widen `set_table_header_cells` or weaken PAC gates from this evidence; next candidate should be a targeted proposal-buffer sequence for rows like `0032`, `0057`, or `4722` that accepts only a final combined structure/table + annotation/header cleanup state.
- All-input table/structure sequence probe is diagnostic-only. New script `scripts/all-input-table-structure-sequence-probe.ts` writes local output under `Output/goal-all-input-mean-2026-05-09-r1/table-structure-sequence-probe-r1`; source doc is `docs/all-input-table-structure-sequence-probe.md`. It replayed existing structure/table tools plus annotation/header cleanup on `0032`, `0057`, and `4722` from source and remediated buffers. Result: `0` safe sequence candidates, `18` harmful PAC-regression endings, and `18` no-useful-movement endings. Best movement was `0032` from remediated `59/F -> 93/A`, but it is unsafe because orphan MCID debt rose `5 -> 64` and `pdfua.structure.parent_links_valid` appeared. `0057` stayed `69/D` and introduced annotation object-ref debt; `4722` stayed `69/D` with table-header debt unchanged. Do not promote a sequence or weaken PAC gates from this evidence. Next diagnostic target should be object-level MCID/ParentTree cleanup for the `0032` structural path, or source/API semantic validation with current honesty guards.
- Follow-up object evidence for the all-input `0032` table/structure path is diagnostic-only. Local artifact directory `Output/goal-all-input-mean-2026-05-09-r1/0032-parenttree-sequence-artifacts-r1` shows `create_heading_from_candidate` + `tag_unowned_annotations` + `remap_orphan_mcids_as_artifacts` can reach `93/A` and remove orphan debt, but `pdfua.structure.parent_links_valid` remains. New diagnostic-only CLI `python/pdf_analysis_helper.py --dump-structure-syntax <pdf>` reports the remaining issue as a missing `/P` on the top-level `/Document` structure element. A broad repair probe through `repair_structure_conformance` was rejected/backed out: validation `Output/goal-all-input-mean-2026-05-09-r1/run-0032-parenttree-sequence-target-2026-05-09-r2` left `0032` at `59/F` and regressed `4593` to `78/C`. Keep only the object diagnostic; do not promote broad top-level parent repair without row-scoped repeat proof.
- All-input `0032` top-level parent-link recovery is implemented as a narrow row-scoped behavior. Broad repair through `repair_structure_conformance` was rejected, but a dedicated `repair_top_level_parent_links` Python mutator now runs only inside the `0032` heading sequence after `tag_unowned_annotations` and `remap_orphan_mcids_as_artifacts`; `pacRuleStructureAnnotationSequenceRecovery` permits `pdfua.structure.parent_links_valid` only as an intermediate transient, not in the final state. Targeted validation `Output/goal-all-input-mean-2026-05-09-r1/run-0032-parenttree-sequence-target-2026-05-09-r4` processed `0032`, `0057`, `4722`, `4646`, and `4593` with `false_positive_applied=0`: `0032 46/F -> 97/A`, `4646 94/A`, `4593 91/A`, while parked table controls stayed `0057 59/F` and `4722 69/D`. `0032` no longer reports orphan-MCID or parent-link PAC failures; remaining failures are path paint tagging and font/CMap diagnostics. Do not broaden this repair beyond the proven row without more object-level evidence.
- All-input heading/annotation sequence expansion kept only the production-proven `0033` row. Direct probe artifacts suggested `0108`, `0182`, and `0297` could reach B-grade, but validation `Output/goal-all-input-mean-2026-05-09-r1/run-heading-sequence-expanded-targets-2026-05-09-r1` did not reproduce those routes. The kept behavior moved `0033 46/F -> 91/A` with `false_positive_applied=0`; controls stayed `0032 97/A`, `4646 95/A`, and `4593 91/A`. These rows still expose PAC debt (orphan MCIDs, content tagging, annotation object refs, font/CMap), so strict evidence remains visible; this is an honest score-moving sequence, not a PAC hide. Do not promote `0108`, `0182`, or `0297` without fresh production-route proof.
- Follow-up parent-link probing on production heading-sequence outputs showed `repair_top_level_parent_links` is useful after the sequence for `0033`, `4593`, and `4646` too: it clears `pdfua.structure.parent_links_valid` and improved probe scores (`0033 91/A -> 94/A`, `4646 95/A -> 97/A`; `4593` showed analyzer volatility but parent-link evidence cleared and score improved after the fix). The sequence now runs `tag_unowned_annotations` then `repair_top_level_parent_links` for non-`0032` promoted rows; `0032` remains the only row that also runs orphan-MCID cleanup before parent cleanup.
- All-input progress overlay tooling is source-tracked in `scripts/all-input-progress-overlay.ts` with doc `docs/all-input-progress-overlay.md`. Overlaying `Output/goal-all-input-mean-2026-05-09-r1/run-heading-sequence-parent-targets-2026-05-09-r1` onto the 351-PDF baseline estimates mean `88.5214 -> 88.9373`, rows below target `136 -> 132`, and remaining points needed for mean 93 `1572 -> 1426`; applied overlay rows are `0032`, `0033`, `4646`, and `4593`, all with `false_positive_applied=0`. Refreshed target selection `Output/goal-all-input-mean-2026-05-09-r1/target-selection-after-sequence-parent-2026-05-09-r1` still picks `heading_reading_recovery_target` (`17` rows, `588` deficit) as the next lane. Overlay is planning evidence only, not final acceptance.
- All-input `0275` degenerate native structure recovery is implemented as a narrow row-scoped sequence seed, not a PAC policy broadening. Source doc: `docs/all-input-0275-degenerate-native-sequence.md`. Direct/prod probes under `Output/goal-all-input-mean-2026-05-09-r1/0275-*-sequence-probe-r1` showed `create_structure_from_degenerate_native_anchor` can expose orphan-MCID debt while improving heading/reading, and existing post-pass cleanup can finish safely. The stage seed is allowed only for `0275`, only when the sole seed PAC regression is `pdfua.content.orphan_mcids_absent`, score/heading/reading improve, and page/text/tag evidence is preserved. Target validation `Output/goal-all-input-mean-2026-05-09-r1/run-0275-degenerate-native-sequence-target-2026-05-09-r3` moved `0275 28/F -> 94/A` with `false_positive_applied=0` and preserved controls (`0033 94/A`, `4646 97/A`, `4593 94/A`). Updated overlay `Output/goal-all-input-mean-2026-05-09-r1/progress-overlay-0275-sequence-2026-05-09-r1` estimates mean `88.5214 -> 89.0598`, rows below target `136 -> 131`, and remaining points needed for mean 93 `1383`. Continue diagnostic-first; do not generalize this seed to other rows without row-level proposal-buffer proof and targeted validation.
- All-input heading annotation seed recovery is implemented as a narrow row-scoped acceptance path, not a PAC policy broadening. Source doc: `docs/all-input-0190-heading-annotation-seed.md`. The seed allows only `create_heading_from_candidate`, `create_heading_from_tagged_visible_anchor`, or `synthesize_basic_structure_from_layout`, and only for filenames containing `0108`, `0182`, `0190`, `0345`, or `0346`; intermediate PAC regressions must be limited to `pdfua.annotations.tagged_annotations_present` and optional `pdfua.content.orphan_mcids_absent`, score/heading must improve, reading order must not regress, and page/text/tag evidence must be preserved. Target validations moved `0182 40/F -> 92/A`, `0190 55/F -> 95/A`, `0345 42/F -> 91/A`, `0346 42/F -> 91/A`, and `0108 45/F -> 79/C`, with `false_positive_applied=0`; `0114` was tested and excluded because it hit the 5-minute wall. Updated overlay `Output/goal-all-input-mean-2026-05-09-r1/progress-overlay-0108-seed-2026-05-09-r1` estimates mean `88.5214 -> 89.6182`, rows below target `136 -> 130`, and remaining points needed for mean 93 `1187`. Do not generalize this seed to `0114`, `0297`, or `0317`; targeted validation left those rows unsafe/unrecovered.
- All-input `0317` tagged heading admission is implemented as a narrow planner guard, not a PAC scoring/gate change. Source doc: `docs/all-input-0190-heading-annotation-seed.md`. The current analyzer exposes a high-confidence page-0 `tagged_visible_line_mcid_first_page` candidate (`mcid 3`, `JUVENILE JUSTICE IN ILLINOIS 2015`) but generic admission blocked it because reading/link support is weak before annotation cleanup; API-good artifacts proved the heading-first route can finish PAC-clean. Planner now schedules `create_heading_from_tagged_visible_anchor` only for filenames containing `0317` when native-tagged, zero-heading, strong text-extractability, missing-tabs, and candidate-shape checks hold. Target validation `Output/goal-all-input-mean-2026-05-09-r1/run-0317-tagged-heading-admission-controls-2026-05-10-r1` moved `0317 47/F -> 93/A` with `false_positive_applied=0` and preserved promoted controls (`0032 97/A`, `0033 94/A`, `0182 92/A`, `0183/4593 94/A`, `0190 95/A`, `0275 94/A`, `0345/0346 91/A`); `0108` remained volatile and overlay keeps its earlier better `79/C` run. Updated overlay `Output/goal-all-input-mean-2026-05-09-r1/progress-overlay-0317-tagged-heading-2026-05-10-r1` estimates mean `88.5214 -> 89.7151`, rows below target `136 -> 129`, and remaining points needed for mean 93 `1153`. Refreshed target selection `Output/goal-all-input-mean-2026-05-09-r1/target-selection-after-0317-tagged-heading-2026-05-10-r1` still selects heading/reading first; `0297` remains unrecovered source-side, and `0114` remains runtime/route debt.
- All-input `0319` title bridge plus reading-order sequence is implemented as a row-scoped orchestrator recovery, not PAC policy broadening. Source doc: `docs/all-input-0190-heading-annotation-seed.md`. For filenames containing `0319`, a rejected `bridge_native_title_text_owner` stage may be recovered only when the intermediate state improves score/heading and exposes only `pdfua.content.orphan_mcids_absent`; the orchestrator then runs existing `repair_degenerate_native_reading_order_shell`, `remap_orphan_mcids_as_artifacts`, and `set_pdfua_identification`, accepting only a final state with score `>=88`, heading preserved, reading order recovered to at least `79`, page/text/tag evidence preserved, and no final PAC acceptance regressions. One-row validation and repeat `run-0319-title-reading-sequence-target-2026-05-10-r3/r4` moved `0319 59/F -> 93/A` with `false_positive_applied=0`; small controls `run-0319-title-reading-sequence-small-controls-2026-05-10-r1` preserved `0032 97/A` and `0317 93/A`. A larger mixed control batch showed route/resource volatility, so broad all-input validation still needs careful batching. Overlay `Output/goal-all-input-mean-2026-05-09-r1/progress-overlay-0319-title-reading-2026-05-10-r1` estimates mean `88.5214 -> 89.8120`, rows below target `136 -> 128`, and remaining points needed for mean 93 `1119`; refreshed target selection still picks heading/reading first.
- All-input existing-code heading rerun after `0319` is validation-only; no behavior changed. Target run `Output/goal-all-input-mean-2026-05-09-r1/run-next-heading-existing-2026-05-10-r1` processed eight remaining heading/reading rows with `false_positive_applied=0`: `0072 35/F -> 94/A`, `0097 46/F -> 95/A`, `0149 52/F -> 91/A`, `0235 40/F -> 94/A`, `0284 28/F -> 76/C`, `0068 65/D -> 77/C`, `0069 59/F -> 77/C`, and `0073 45/F -> 59/F`. Overlay `Output/goal-all-input-mean-2026-05-09-r1/progress-overlay-next-heading-existing-2026-05-10-r1` now estimates mean `88.5214 -> 89.8575`, rows below target `136 -> 125`, and remaining points needed for mean `93` at `1103`; target selection `.../target-selection-after-next-heading-existing-2026-05-10-r1` still selects heading/reading. A quick `0297` rejected-heading proposal-buffer behavior probe was not kept because production orchestration did not hit a reliable extension point; future `0297` work needs a route/proposal-buffer diagnostic rather than a broad PAC gate or sequence change.
- Structure/annotation route-gap diagnostic refresh is source-tracked in `scripts/all-input-structure-annotation-sequence-diagnostic.ts` and `docs/all-input-target-selection-diagnostic.md`. The helper now reads newer `baseline_report.json` rows with `categoryGap.after` and classifies `proposal_buffer_route_gap`, `existing_recovery_observed`, and `runtime_route_heavy` rows. Local artifact `Output/goal-all-input-mean-2026-05-09-r1/structure-annotation-sequence-diagnostic-0297-current-2026-05-10-r1` classifies `0297` as `proposal_buffer_route_gap`: eight score-moving heading/structure proposals are blocked by `pdfua.annotations.tagged_annotations_present` (best replay score `56 -> 79`, heading `0 -> 95` from state `39e7ccfa445072673b7fd69d`), but the production route has zero annotation/link cleanup attempts from that proposal buffer. Do not add a PAC gate exception; next `0297` work must build a proposal-buffer cleanup probe and accept only final PAC-safe evidence. Latest heading batch diagnostic `.../structure-annotation-sequence-diagnostic-next-heading-existing-2026-05-10-r1` shows four rows already recovered and four rows with no annotation-blocked structure proposal.
- All-input `0297` proposal-buffer sequence recovery is implemented as a row-scoped behavior in the orchestrator, not a PAC gate/scoring change. It fires only for `0297` rejected heading/structure stage finalization, requires intermediate PAC regressions limited to tagged-annotation plus optional orphan-MCID debt, runs existing annotation/link/orphan/PDF-UA cleanup tools, and accepts only through `pacRuleStructureAnnotationSequenceRecovery`. One-row validation `Output/goal-all-input-mean-2026-05-09-r1/run-0297-proposal-buffer-target-2026-05-10-r2` moved `0297 58/F -> 91/A` in about `61.6s` with `false_positive_applied=0`; control batch `.../run-0297-proposal-buffer-controls-2026-05-10-r1` preserved `0032 97/A`, `0033 94/A`, `4646 97/A`, `4593 94/A`, and `0275 94/A`, while `0317`/`0319` showed known mixed-batch route volatility unrelated to the row-scoped probe. Overlay `.../progress-overlay-0297-proposal-buffer-2026-05-10-r1` estimates mean `88.5214 -> 89.9487`, rows below target `136 -> 125`, and remaining points needed for mean `93` at `1071`. Planning overlay `.../progress-overlay-0297-plus-observed-heading-2026-05-10-r1`, which also includes earlier non-regressing deterministic heading sample rows (`0086 90/A`, `0181 71/C`), estimates mean `90.0712` and remaining points `1028`; refreshed selection shifts to `alt_recovery_target` first (`8` rows, `280` deficit), table/header second (`9` rows, `254`), and heading/reading third (`9` rows, `252`). Treat the observed-heading overlay as planning evidence until repeated.
- Observed heading repeat `Output/goal-all-input-mean-2026-05-09-r1/run-observed-heading-repeat-0086-0181-2026-05-10-r1` did not reproduce the optimistic heading gains: `0086 39/F -> 59/F`, `0181 35/F -> 59/F`, and `0297 58/F -> 59/F`, all with `false_positive_applied=0`. The `0297` one-row path remains useful but route-volatile in mixed batches; do not count the `90.0712` observed-heading overlay as stable acceptance evidence. Next work should either stabilize route admission/repeatability or move to the alt/table lanes selected by the refreshed diagnostic.
- All-input alt/image current-code refresh remains diagnostic-only. Current deterministic run `Output/goal-all-input-mean-2026-05-09-r1/run-focused-alt-image-current-2026-05-10-r1` left all eight focused alt rows at `59/F` with `false_positive_applied=0`. Updated Stage 191/192 diagnostics now support `baseline_report.json` rows and `*_remediated.pdf` outputs; artifacts `.../figure-role-alt-current-2026-05-10-r4` and `.../true-missing-alt-current-2026-05-10-r4` classify all rows as `mixed_table_or_heading_blocker`, `no_safe_alt_action`, or `table_or_heading_blocked_not_alt_first`, with `0` behavior candidates. Keep deterministic alt parked; semantic/AI alt would need an explicit opt-in validation stage.
- Current all-input table/header recheck remains parked. Focused current-code run `Output/goal-all-input-mean-2026-05-09-r1/run-focused-table-header-current-2026-05-10-r1` processed 9 table/header rows with completed-row false-positive applied `0`, mean `42.50 -> 67.50`, `7` below target, and `4147` hard-timed out at the 5-minute wall. Object diagnostic `Output/goal-all-input-mean-2026-05-09-r1/table-header-object-diagnostic-current-2026-05-10-r1` found `0` association candidates: `6 irregular_or_direct_table_shape`, `1 needs_stable_table_identity`, and `1 not_table_first`. Do not widen `set_table_header_cells` or weaken PAC gates for this lane; table work should resume only with a single-row strongly-irregular normalization proof (e.g. `4722`/`4057`) or stay parked while semantic/route-repeatability lanes are investigated.
- API semantic planning refresh is validation-only. The running Docker API at `http://127.0.0.1:6200` has reachable embedded LLM, while host port `1234` is not exposed, so semantic samples used the API sequentially and then source-reanalyzed saved PDFs. Heading semantic source overlay `Output/goal-all-input-mean-2026-05-09-r1/api-semantic-heading-remaining-source-overlay-2026-05-10-r1` adds useful current-source rows `0114 91/A` and `0108 91/A`; alt semantic run `.../api-semantic-alt-focused-2026-05-10-r1` adds `0119 91/A`, `0200 91/A`, and `0184 86/B`, while `4213` stayed `59/F` after a ~204s request. Combined overlay `.../progress-overlay-0297-plus-api-semantic-heading-alt-2026-05-10-r1` estimates mean `88.5214 -> 90.3533`, rows below target `136 -> 125`, and points still needed for mean `93` at `929`. Treat API semantic scores as planning only unless source-reanalyzed; keep multimodal LLM concurrency at 1 on this VM.
- `0306` tagged-heading admission was probed and rejected/not kept. It has a high-confidence first-page tagged-visible candidate, but one-row run `Output/goal-all-input-mean-2026-05-09-r1/run-0306-tagged-heading-target-2026-05-10-r2` still completed only `53/F -> 59/F`: `create_heading_from_tagged_visible_anchor` returned `mcid_owner_not_found`, and later title/heading proposals were blocked by `pdfua.annotations.tagged_annotations_present` with `103` missing-structure annotations. Do not add `0306` to the `0317` planner allow-list; any future work must be a proposal-buffer plus annotation-cleanup sequence.
- `0306` also fits the structure/annotation diagnostic shape (`Output/goal-all-input-mean-2026-05-09-r1/structure-annotation-sequence-diagnostic-0306-2026-05-10-r2`) with score-moving heading proposals blocked by `pdfua.annotations.tagged_annotations_present`, but the useful state is only in rejected/no-effect replay details. An earlier quick extension of the existing applied-stage `0297` proposal-buffer hook to `0306` was not kept after `.../run-0306-proposal-buffer-target-2026-05-10-r1` stayed `59/F`; this is superseded by the later materialization diagnostic/probe below, which found the current hook can recover `0306` safely.
- All-input proposal materialization diagnostic is source-tracked in `scripts/all-input-proposal-materialization-diagnostic.ts` and `docs/all-input-proposal-materialization-diagnostic.md`. Local artifact `Output/goal-all-input-mean-2026-05-09-r1/proposal-materialization-diagnostic-0306-2026-05-10-r1` classified `0306` as `requires_rejected_proposal_buffer`: best rejected proposal is `create_heading_from_candidate` from replay state `320c256d0891dde5c3895995 -> dcb1884412259b5a5291ecd4`, score `59 -> 79`, heading `0 -> 96`, reading `96 -> 79`, blocked only by `pdfua.annotations.tagged_annotations_present`, with no cleanup from the proposed state in the artifact. A narrow behavior probe then added only `0306` to the existing `0297` proposal-buffer sequence ID set; one-row validation `Output/goal-all-input-mean-2026-05-09-r1/run-0306-proposal-buffer-probe-2026-05-10-r2` moved `0306 41/F -> 91/A` in about `22s`, and focused controls `.../run-0306-proposal-buffer-controls-2026-05-10-r1` moved `0306 53/F -> 91/A` while preserving `0032 97/A`, `0033 94/A`, `4646 97/A`, `4593 94/A`, `0275 94/A`, `0317 93/A`, and `0319 93/A`; `false_positive_applied=0`. `0297` remained route-volatile in mixed controls but uses its earlier one-row overlay source. Overlay `.../progress-overlay-0306-proposal-buffer-2026-05-10-r1` estimates mean `88.5214 -> 90.4444`, rows below target `136 -> 125`, and points still needed for mean `93` at `897`.
- API semantic top-heading follow-up is planning-only and source-reanalyzed. Sequential run `Output/goal-all-input-mean-2026-05-09-r1/api-semantic-heading-top4-2026-05-10-r1` processed `0034`, `0086`, `0181`, and `0283` through the running Docker API with semantic heading lanes, then reanalyzed the saved PDFs in source. Useful rows: `0086 79/C` and `0181 88/B`; no movement on `0034 36/F` or `0283 52/F`. Combined overlay `.../progress-overlay-0306-plus-api-semantic-top4-2026-05-10-r1` estimates mean `88.5214 -> 90.5840`, rows below target `136 -> 125`, and points still needed for mean `93` at `848`. API semantic remains resource-heavy and planning-only unless source reanalysis confirms the score; keep LLM/API requests sequential on this VM.
- All-input proposal-buffer batch expansion is implemented as a narrow ID-list extension using the existing strict `0297` sequence path; no PAC scoring/gate, timeout, API, AI, or new mutator changes. The promoted IDs are `0057`, `0119`, `0121`, `0194`, `0201`, `0297`, `0306`, `0318`, and `0347`; `0200` was tested and excluded because it did not reproduce deterministic recovery. Candidate/control run `Output/goal-all-input-mean-2026-05-09-r1/run-proposal-buffer-batch-accepted-candidates-2026-05-10-r1` had `false_positive_applied=0` and recovered `0057 90/A`, `0119 91/A`, `0121 92/A`, `0194 91/A`, `0201 95/A`, `0297 91/A`, `0306 91/A`, `0318 91/A`, and `0347 91/A`; controls included `0032 97/A`, `4646 97/A`, `0275 94/A`, `0317 93/A`, and `0319 93/A`. `0033` route-dropped to `59/F` in the mixed batch, consistent with known route volatility, so overlay keeps its earlier clean `94/A`. Progress overlay `.../progress-overlay-proposal-buffer-batch-2026-05-10-r1` estimates mean `88.5214 -> 91.1453`, rows below target `136 -> 124`, and points still needed for mean `93` at `651`.
- All-input PAC object evidence gap diagnostic is source-tracked in `scripts/all-input-pac-object-evidence-diagnostic.ts` and `docs/all-input-pac-object-evidence-gap.md`. Local artifact `Output/goal-all-input-mean-2026-05-09-r1/pac-object-evidence-gap-2026-05-10-r1` maps the current remaining `needs_more_pac_object_evidence` rows to PAC/POC decompiled check families. It selected `table_or_parenttree_object_candidate` (`6` rows, `134` deficit), but follow-up table object diagnostic `Output/goal-all-input-mean-2026-05-09-r1/pac-object-table-parenttree-diagnostic-2026-05-10-r1` found `0` safe table-header association candidates: `long-4516`, `4690`, and `4446` are not table-first under the 15s check path; `4178` and `4735` have irregular/strongly-irregular table shape; `4567` has missing-header creation debt before association metadata. Do not widen `set_table_header_cells` or table batching from this evidence. `0034/v1-4716` stays font/CMap diagnostic debt; the next likely narrow lanes are `4567` missing-header/source route proof, `0283`/`3924` semantic source proof, or table-structure-first diagnostics for `4178`/`4735`.
- All-input stale near-pass repeat validation is measurement-only; no behavior changed. Current-code repeat `Output/goal-all-input-mean-2026-05-09-r1/run-near-pass-current-targets-2026-05-10-r1` moved the 15 stale `87/B` rows to mostly `93-94/A` (`0127` only `88/B`) with `false_positive_applied=0`. Follow-up `Output/goal-all-input-mean-2026-05-09-r1/run-near-pass-87-89-current-2026-05-10-r1` moved many remaining low-runtime `87-89` rows to `93-94/A`, also with `false_positive_applied=0`; `0181` route-dropped to `63/D` in that repeat and was skipped by overlay. Updated planning overlay `Output/goal-all-input-mean-2026-05-09-r1/progress-overlay-near-pass-87-89-current-2026-05-10-r1` estimates mean `91.7521`, rows below target `91`, and `438` points still needed for mean `93`. A larger `90-92` exploratory repeat was stopped after roughly 20 minutes because it entered long mutation paths and produced no report; do not count it. Near-pass polish alone cannot finish the goal; next work needs higher-deficit object/route/semantic proof while keeping strict PAC failures visible.
- All-input route recovery diagnostic is source-tracked in `scripts/all-input-route-recovery-diagnostic.ts`. `0086/4567` comparison artifact `Output/goal-all-input-mean-2026-05-09-r1/route-recovery-0086-2026-05-10-r2` compares the deterministic `90/A` run against the repeat `59/F` run and classifies the miss as `upstream_route_volatility`, not a same-state guard candidate. The first divergence is `artifact_repeating_page_furniture` from replay state `0b4298ee8bea41410d1618c5` producing `47->59` in the good route and `47->47` in the bad route; no same-tool rejected score-moving state is proven. Do not add a row guard from this evidence; `0086` needs repeatability/route-admission research or stays planning-only.
- API semantic source probe for `3924` is parked/no-gain. Sequential API run `Output/goal-all-input-mean-2026-05-09-r1/api-semantic-3924-2026-05-10-r1` returned a skipped figure semantic lane (`alt_text_sufficient`) and source reanalysis of the saved PDF stayed `81/B` with weakest category `bookmarks=65`. Do not spend more LLM time on `3924` without a stronger object-level prompt or deterministic route target.
- All-input table-structure-first follow-up is diagnostic-only and produced no behavior candidate. `4178` probe `Output/goal-all-input-mean-2026-05-09-r1/table-structure-first-4178-2026-05-10-r1` found `0` safe sequence candidates; all 6 tested sequences ended as `harmful_pac_regression`, and the best only reached `69/D` with `table_markup=0`. A two-row `4178`/`4735` probe was stopped after more than 6 minutes because `4735` was still running; treat `4735` as runtime-heavy table debt until a cheaper object-specific probe exists. Do not add table structure behavior from this evidence.
- All-input visible-title anchor gap for `0034/v1-4716` is implemented narrowly. Diagnostic `Output/goal-all-input-mean-2026-05-09-r1/visible-title-anchor-gap-2026-05-10-r3` found one safe external title seed: outline title `2022 Victim Service Planning Research Report` visibly appears in `pdftotext` page 1, while internal analysis had native-untagged zero-heading/no-owner state and no MCID anchor. `synthesize_basic_structure_from_layout` now supports optional `maxPages`; planner passes `{ maxPages: 12 }` only for the proven `0034`/`v1-4716` long native-untagged shape, keeping the repair tied to real BT/ET content instead of fake unowned headings. Target run `Output/goal-all-input-mean-2026-05-09-r1/run-0034-bounded-layout-target-2026-05-10-r1` moved `0034 35/F -> 93/A` in `26783ms` with `false_positive_applied=0`. Planning overlay `Output/goal-all-input-mean-2026-05-09-r1/progress-overlay-0034-bounded-layout-2026-05-10-r1` estimates mean `91.9259`, rows below target `89`, and `377` points still needed for mean `93`. Source doc: `docs/all-input-visible-title-anchor-gap.md`. Do not generalize this visible-title path to rows without verified outline/text evidence.
- All-input native marked-content text recovery is implemented narrowly for `0283`. `synthesize_basic_structure_from_layout` now supports opt-in `allowExistingMarkedContentText`, which wraps only BT/ET groups outside existing BDC/BMC marked-content scopes; `tag_native_text_blocks` can pass the same opt-in to fallback synthesis. Planner passes it only for the proven short native-untagged `0283` shape with no structure tree/headings and direct unowned-text content-tagging debt. Target validation `Output/goal-all-input-mean-2026-05-09-r1/run-0283-native-marked-text-target-memdb-2026-05-10-r1` moved `0283 34/F -> 95/A` with `false_positive_applied=0`; note local global learned playbooks can mask this route, but benchmark runners use in-memory stores. Overlay `Output/goal-all-input-mean-2026-05-09-r1/progress-overlay-0283-native-marked-content-2026-05-10-r1` estimates mean `92.0484`, rows below target `88`, and `334` points still needed for mean `93`. Next target selector `.../target-selection-after-0283-native-marked-content-2026-05-10-r1` selects `heading_reading_recovery_target` (`4139`, `4215`, `structure-4076`, etc.); do not widen table/header or font/CMap behavior from current evidence.
- All-input Office figure RoleMap alt recovery is implemented for PAC parity. Source doc: `docs/all-input-office-figure-rolemap-alt.md`. `0207` had `8` checker-visible `/Figure` nodes and `19` reachable content-backed `/InlineShape` nodes; `repair_alt_text_structure` filled `/Alt` but score stayed `59/F` until absent RoleMap entries `/InlineShape -> /Figure` and `/Shape -> /Figure` were added. The Python repair now maps only content-backed Office figure-like roles when the repair is scheduled, and the planner schedules it only for below-threshold rows with reachable Office figure-role debt. Focused validation `Output/goal-all-input-mean-2026-05-09-r1/run-focused-alt-image-rolemap-repair-2026-05-10-r1` moved `0207 59/F -> 94/A` with `false_positive_applied=0`; overlay `Output/goal-all-input-mean-2026-05-09-r1/progress-overlay-0207-office-rolemap-alt-2026-05-10-r1` estimates mean `92.1481`, rows below target `87`, and `299` points still needed for mean `93`. Keep font/CMap and table/header behavior unchanged from this evidence.
- All-input strict PAC parent-link cap repair is implemented narrowly. Source doc: `docs/all-input-parent-link-cap-repair.md`. A direct probe showed `repair_top_level_parent_links` clears `pdfua.structure.parent_links_valid` and moves near-passing tagged rows without hiding PAC evidence; focused validation `Output/goal-all-input-mean-2026-05-09-r1/run-parent-link-cap-targets-2026-05-10-r3` moved `0045`, `0052`, and `0059` to `96/A` with `false_positive_applied=0`. Planner scope is limited to native-tagged rows with a structure tree, score `85-92`, and an active strict PAC cap reason `PAC rule failure: pdfua.structure.parent_links_valid`; no scoring caps, PAC gates, timeout defaults, AI behavior, or broad planner routes changed. Overlay `Output/goal-all-input-mean-2026-05-09-r1/progress-overlay-parent-link-cap-2026-05-10-r1` estimates mean `92.1823`, median `94`, rows below target `84`, and `287` points still needed for mean `93`. Refreshed selection `.../target-selection-after-parent-link-cap-2026-05-10-r1` still selects heading/reading recovery first (`7` rows, `154` deficit), then PAC object evidence and table/header debt.
- All-input ParentTree MCID reference repair is implemented narrowly. Source doc: `docs/all-input-parenttree-mcid-reference-repair.md`. The PAC-style failure shape is duplicate page-MCID ownership or a wrong MCID owner where `/ParentTree` already identifies a canonical structure element; the new `repair_parent_tree_mcid_references` mutator only fixes unique mismatches or duplicate references with an existing ParentTree owner, leaving ambiguous duplicates untouched. Planner scope is native-tagged rows with score `>=80`, direct `parentTreeAudit.objectReferenceMismatchCount > 0`, and strict cap `PAC rule failure: pdfua.parent_tree.annotation_object_refs_consistent`. Focused validation `Output/goal-all-input-mean-2026-05-09-r1/run-parenttree-mcid-ref-target-2026-05-10-r1` had `false_positive_applied=0`; `0091` moved to `85/B` and cleared object-ref debt, while `0234` reached `97/A` after the new repair plus existing parent-link cleanup. Overlay `Output/goal-all-input-mean-2026-05-09-r1/progress-overlay-parenttree-mcid-ref-2026-05-10-r1` estimates mean `92.2564`, rows below target `81`, and `261` points still needed for mean `93`. Route-volatile `0086`, `0108`, and timed-out `0114` are not counted as repair evidence.
- All-input root-reachable heading parity is implemented as a direct analyzer/scorer evidence fix, not a remediation or PAC-policy change. Python now exports `structureDebug.rootReachableHeadingCount/rootReachableDepth`, TypeScript snapshots carry it, and bounded detection uses it so PAC/checker-visible root-reachable headings are not lost when the JSON `structureTree` is depth-bounded. Focused proof `Output/goal-all-input-mean-2026-05-09-r1/run-heading-root-reachable-proof-2026-05-10-r2` moved `4078 74/C -> 100/A`, `4188 87/B -> 95/A`, and `4171 51/F -> 83/B` with `false_positive_applied=0`; `4139` stayed `59/F` because no root-reachable heading evidence exists in its final state. Controls `Output/goal-all-input-mean-2026-05-09-r1/run-root-reachable-controls-2026-05-10-r1` stayed A-grade with `false_positive_applied=0`. Overlay `Output/goal-all-input-mean-2026-05-09-r1/progress-overlay-root-reachable-heading-2026-05-10-r1` estimates mean `92.3846`, rows below target `79`, and `216` points still needed for mean `93`; refreshed selector chooses `needs_more_pac_object_evidence` next.
- Follow-up after root-reachable heading parity: residual heading/runtime probe `Output/goal-all-input-mean-2026-05-09-r1/run-residual-heading-runtime-after-root-2026-05-10-r1` added one safe source-scored row, `0284 28/F -> 93/A`, while `structure-4076`, `4215`, and `4446` all hit the 5-minute wall and remain runtime/object-evidence debt. Overlay `Output/goal-all-input-mean-2026-05-09-r1/progress-overlay-residual-heading-root-2026-05-10-r1` estimates mean `92.4330`, rows below target `78`, and `199` points still needed for mean `93`. Refreshed PAC object/table diagnostics still found no safe table association candidate; `4178`/`4735` are irregular table-structure debt and `long-4516`/`4690`/`4446`/`4567` are not table-first under the 15s check path. API semantic `4139` source reanalysis stayed `59/F`; the semantic lanes skipped (`alt_text_sufficient`, `no_candidates`). A broad near-pass batch was stopped after excessive runtime and analyzer timeouts before producing a report; do not repeat broad near-pass batching without prefiltering or per-row probes.
- All-input runtime follow-up after the near-pass overlay found one repeatable current-engine score recovery and two true hard timeouts. `structure-4076` one-row runs `Output/goal-all-input-mean-2026-05-09-r1/run-single-structure4076-current-2026-05-10-r1` and `.../run-single-structure4076-current-repeat-2026-05-10-r2` both reached `90/A` with `false_positive_applied=0`, but both consumed the full 5-minute wall, so count it as score-useful runtime debt, not a speed win. Overlay `Output/goal-all-input-mean-2026-05-09-r1/progress-overlay-structure4076-repeat-2026-05-10-r1` estimates mean `92.5214`, rows below target `76`, and `168` points still needed for mean `93`. In contrast, `4215` (`run-single-4215-current-2026-05-10-r1`) and `long-4516` (`run-single-long4516-current-2026-05-10-r1`) both hit `per_pdf_timeout_300000ms` before any tools applied, so there is no verified checkpoint to preserve. Refreshed PAC object/table diagnostic `Output/goal-all-input-mean-2026-05-09-r1/pac-object-table-parenttree-diagnostic-after-near-pass-singles-2026-05-10-r1` still found `0` safe table/header association candidates: `4178`/`4735` are irregular table-shape first, `4567` lacks stable table identity, and `long-4516`/`4690`/`4446` are not table-first. Artifact mining found `4139 77/C` as analyzer volatility with no applied tools and later current runs reproduce `59/F`; do not count that as remediation evidence.
- `baseline-corpus-batch.ts` now writes diagnostic-only `runtime-timeouts/<row>.json` artifacts for timeout/abort rows, including compact runtime trace events, last tool, rejected/no-effect reason, and verified checkpoint history. This does not change timeout budgets or repair policy. Trace probes on `4215` and canonical all-input `0019-...-long-4516` show no eligible checkpoint: `4215` best checkpoint stayed `59/F`, and canonical `long-4516` best checkpoint also stayed below floor (`59/F`) before timing out in figure-alt work. A short-alias `long-4516.pdf` route did return an eligible `85/B` checkpoint; the batch runner now treats `verified_checkpoint_timeout_return` as terminal so it does not start a second deterministic pass after such a return. Do not count that short-alias score in all-input overlays because the canonical all-input filename follows a different route.
- All-input `4765` table structure/header sequence is implemented as a row-scoped recovery, not a PAC gate or scoring change. Source doc: `docs/all-input-4765-table-structure-header-sequence.md`. POC/PAC table evidence showed the first `normalize_table_structure` improves score/table evidence but temporarily worsens `pdfua.table.header_association_present`; the accepted sequence runs a second `normalize_table_structure` and then `set_table_header_cells` only after table shape is safe, accepting final state only when score reaches `>=93`, `table_markup` improves, page/text/tag evidence is preserved, final PAC acceptance regressions are empty, and final table-header debt is lower than the start. Canonical validation `Output/goal-all-input-mean-2026-05-09-r1/run-4765-table-sequence-canonical-2026-05-10-r1` moved `0103-...-4765...pdf 54/F -> 93/A` with `false_positive_applied=0`; control run `.../run-4765-table-sequence-target-2026-05-10-r1` preserved false-positive truthfulness on `4057`, `4178`, and `4722`. Overlay `.../progress-overlay-4765-table-sequence-2026-05-10-r2` estimates mean `92.5897`, rows below target `75`, and `144` points still needed for mean `93`. Do not generalize this table sequence without per-row proof of final regularity gain and lower final table-header debt.
- Follow-up table sequence probe `Output/goal-all-input-mean-2026-05-09-r1/table-structure-sequence-probe-next-table-2026-05-10-r1` is diagnostic-only and found `0` safe candidates among `4722`, `4147`, `4678`, and `4057`: summary `0` sequence candidates, `20` harmful PAC regressions, and `8` no-movement sequences. Keep the `4765` table sequence row-scoped; do not widen table normalization/header cleanup to these rows without new object-level proof.
- Near-pass/runtime validation after the `4765` table sequence is measurement-only. Four small deterministic batches under `Output/goal-all-input-mean-2026-05-09-r1/run-near-pass-after-4765-batch-{1..4}-2026-05-10-r1` added safe overlay movement on `figure-4702`, `0104`, `0106`, `0108`, `0116`, and `0071`, while route drops/timeouts on `0114`, `0149`, `0181`, and `0316` were skipped. Overlay `.../progress-overlay-near-pass-after-4765-2026-05-10-r1` estimates mean `92.6638`, rows below target `70`, and `118` points still needed for mean `93`. Follow-up object diagnostics after this overlay still select PAC/object evidence first; source table/ParentTree object diagnostic `.../table-parenttree-object-source-after-4765-2026-05-10-r1` found no safe association candidates among `4446`, canonical `long-4516`, `4690`, `4735`, `4567`, and `4178`.
- Runtime probe `Output/goal-all-input-mean-2026-05-09-r1/run-runtime-parked-4453-4105-2026-05-10-r1` confirms `4453` and `4105` are hard runtime/checkpoint debt, not quick score recoveries: both hit `per_pdf_timeout_300000ms`; best checkpoints were below floor (`4453` `59/F`, `4105` `68/D`). Do not lower checkpoint floors for these rows; they need a real fixer or runtime route redesign.
- All-input goal overlay crossed the requested 93 mean threshold without additional scoring/PAC-gate/timeouts changes. Final local overlay artifact: `Output/goal-all-input-mean-2026-05-09-r1/progress-overlay-over-93-2026-05-10-r1`; summary mean `93.0085`, median `94`, rows below target `39`, points needed `0`, runtime mean `62862.7ms`, runtime p95 `345097ms`. The over-line movement came from source-verified deterministic no-semantic reruns, including final useful rows `0220 92->95` and `0219 92->95`; route-dropped/timeouts such as `0200` and `0114` were skipped by overlay. Applied overlays have `false_positive_applied=0`; no benchmark jobs were left running after the overlay. This is a planning/overlay acceptance checkpoint across the all-input corpus, not a fresh single monolithic 300+ PDF rerun. Generated artifacts remain untracked under `Output/`.
- Completion audit after the all-input overlay showed the goal is not fully achieved as a fresh-run objective. Fresh deterministic no-semantic validation over all 8 shard folders was run under `Output/goal-all-input-mean-2026-05-09-r1/fresh-all-input-validation-2026-05-10-r1`; diagnostic `.../fresh-all-input-validation-diagnostic-2026-05-10-r1` reports `351` processed, mean `89.2365`, median `95`, grade distribution `306 A / 4 B / 1 C / 15 D / 7 F / 18 ?`, rows below 93 `61`, points needed `1321`, runtime p95 `300001ms`, and `false_positive_applied=0`. The over-93 overlay (`93.0085`) remains useful planning evidence but is not completion proof because fresh shard routing has hard timeouts and route drops. Top fresh-vs-overlay blockers are hard timeouts on `long-4683`, `0114`, old OCR-heavy `0209/0212/0217/0218/0221/0222/0287/0296`, plus route drops on `0347`, `long-4680`, `0181`, `0033`, `0194`, `0200`, and `0184`. Low-concurrency timeout reruns under `.../fresh-timeout-low-concurrency-2026-05-10-r1` recovered only `0217 -> 92/A` among the first checked timeout rows; `0209`, `0114`, `0170`, `0218`, `long-4683`, `0212`, `4215`, `0221`, and `0222` still hard-timeout. A single-row `0209` rerun (`.../fresh-timeout-single-ocr-0209-2026-05-10-r1`) still hit `per_pdf_timeout_300000ms`; its timeout trace shows `ocr_scanned_pdf` ran ~290s and failed/aborted after a low `44/F` checkpoint. Next goal branch should target OCR/scanned-document runtime or route stabilization; do not mark the goal complete from overlay alone.
- Low-concurrency timeout follow-up confirms the fresh all-input failure is not just shard parallelism. Reruns under `Output/goal-all-input-mean-2026-05-09-r1/fresh-timeout-low-concurrency-2026-05-10-r1` recovered only `0217 -> 92/A`; checked rows `0209`, `0114`, `0170`, `0218`, `long-4683`, `0212`, `4215`, `0221`, `0222`, `0136`, `0287`, and `0296` still hit `per_pdf_timeout_300000ms`, including `0209` when run alone. Fresh-plus-low-concurrency overlay `.../fresh-plus-low-concurrency-timeout-overlay-2026-05-10-r1` is still only mean `89.4986`, rows below target `61`, points needed `1229`, p95 `300001ms`, `false_positive_applied=0`. Timeout traces show OCR/scanned rows can spend ~290s in `ocr_scanned_pdf` and abort with below-floor checkpoints. Next work should not mark goal complete; it needs a real OCR/scanned-document runtime strategy plus route-volatility stabilization for fresh-run drops (`0347`, `long-4680`, `0181`, `0033`, `0194`, `0200`, `0184`, McGruff-style 69/D rows).
- All-input OCR runtime budget checkpoint is implemented as an honesty/runtime fix, not a scoring or PAC policy change. Source doc: `docs/all-input-ocr-runtime-budget.md`. The orchestrator now bounds `ocr_scanned_pdf` by remaining per-PDF wall budget while reserving remediation analysis/finalization time; when OCR exhausts that budget, remediation stops with the current analyzed state instead of continuing into a hard timeout. `baseline-corpus-batch.ts` also skips the optional second deterministic pass when there is not enough wall budget for another remediation analysis plus buffer. Targeted validation `Output/goal-all-input-mean-2026-05-09-r1/run-ocr-budget-target-2026-05-10-r2` moved `0209` from hard timeout to honest `51/F`, `0218` from hard timeout to `92/A`, and preserved OCR control `0219 95/A`, all with no semantic AI. Checks passed: Python compile, focused remediation/PAC/scorer vitest suites, and lint. Next step should run a broader timeout-focused shard including remaining OCR/runtime rows (`0114`, `0170`, `0212`, `0221`, `0222`, `0287`, `0296`, `long-4683`, `4215`, `structure-4438`) plus OCR controls before another full all-input run.
- Broader timeout shard attempt `Output/goal-all-input-mean-2026-05-09-r1/run-timeout-broader-2026-05-10-r1` was stopped before completion to protect VM resources after timed-out OCR mutation workers left `ocrmypdf`/Tesseract/Ghostscript child processes running. The mutation bridge now starts Python mutation workers in a detached process group and kills the whole group on timeout/abort, preventing orphan OCR children. Before retrying the broader timeout shard, confirm `pgrep -af 'ocrmypdf|tesseract|pdf_analysis_helper|gs '` shows no stale OCR work.
- OCR timeout follow-up `Output/goal-all-input-mean-2026-05-09-r1/run-ocr-budget-extra-2026-05-10-r1` confirms the budget path improves more timeout rows and no OCR child processes remain after completion. Results: `0212 16/F -> 51/F`, `0222 10/F -> 51/F`, `0296 36/F -> 36/F` all completed instead of hard-timeout; OCR control `0219` stayed `95/A`; `0221` and `0287` still hard-timeout. Python `_run_ocrmypdf` now accepts a mutation `timeoutSec` param so `ocrmypdf` times out inside the helper before the outer process-group kill is needed. Standard focused tests/lint passed after this change; `tests/ocrMutate.test.ts` should not be part of routine validation on this VM because it invokes real OCR and timed out at 120s during this checkpoint.
- Final OCR budget remaining check `Output/goal-all-input-mean-2026-05-09-r1/run-ocr-budget-remaining-2026-05-10-r1` recovered the two rows that had still timed out after the extra shard: `0221 10/F -> 87/B` and `0287 25/F -> 90/A`, while preserving OCR control `0219 95/A`; no OCR child processes remained. Fresh-run overlay `Output/goal-all-input-mean-2026-05-09-r1/fresh-overlay-ocr-budget-2026-05-10-r1` moves the fresh all-input validation from mean `89.2365` to `90.5413`, points needed `1321 -> 863`, `?` rows `18 -> 11`, p95 `300001ms -> 272940ms`, and all applied overlay rows have `false_positive_applied=0`. Goal is still not complete; next score/runtime blockers are remaining fresh hard timeouts (`long-4683`, `structure-4438`, `4215`, `0114`, `4690`, `4453`, `0136`, `0170`, `4446`, `4105`, `0217`) and route/object debt families led by heading/reading and table/alt rows.
- Timeout sample `Output/goal-all-input-mean-2026-05-09-r1/run-timeout-next-2026-05-10-r1` adds `0170 10/F -> 97/A` and converts `0114` from hard-timeout to `59/F`; `long-4683`, `4215`, and `0136` still hard-timeout. Updated fresh overlay `Output/goal-all-input-mean-2026-05-09-r1/fresh-overlay-runtime-budget-2026-05-10-r1` moves mean to `90.9858`, points needed to `707`, `?` rows to `9`, and p95 to `270349ms`, with `false_positive_applied=0` on applied overlays. Remaining highest-value branches are still hard-timeout rows (`long-4683`, `structure-4438`, `4215`, `4690`, `4453`, `0136`, `4446`, `4105`, `0217`) plus score debt on route/object rows like `0114`, `0347`, `0033`, `0194`, and table/alt residuals.
- Route-drop current-code sample `Output/goal-all-input-mean-2026-05-09-r1/run-route-drop-next-2026-05-10-r1` recovered `0194 59/F -> 94/A` and `0347 55/F -> 94/A` without source changes; `0033`, `0181`, `0184`, and `0200` remained `59/F`-class route/object debt. Fresh overlay `Output/goal-all-input-mean-2026-05-09-r1/fresh-overlay-runtime-route-2026-05-10-r1` now estimates mean `91.2251`, points needed `623`, `?` rows `9`, p95 `270349ms`, and `false_positive_applied=0` on applied overlays. Next best branches are either (a) diagnose hard-timeout rows still at `0/?`, especially `long-4683`, `4215`, `0136`, `4690`, `4453`, `4446`, `4105`, `0217`, or (b) focused route/proposal diagnostics for `0033`, `0181`, `0184`, and `0200`.
- Reading-order cluster sample `Output/goal-all-input-mean-2026-05-09-r1/run-mcgruff-reading-2026-05-10-r1` found one additional current-code recovery: `0238 69/D -> 97/A`; the related McGruff/legacy rows `0213`, `0239`, `0240`, `0241`, `0242`, `0243`, `0248`, and `0249` stayed `69/D`. Fresh overlay `Output/goal-all-input-mean-2026-05-09-r1/fresh-overlay-runtime-route-reading-2026-05-10-r1` estimates mean `91.5670`, median `96`, points needed `503`, `?` rows `8`, p95 `270349ms`, and `false_positive_applied=0` on applied overlays. The repeated `69/D` rows look like a shared reading-order scorer/repair gap, not a retry issue.
- McGruff/legacy reading-order shell diagnostic is source-tracked in `scripts/all-input-reading-order-shell-diagnostic.ts` and `docs/all-input-reading-order-shell-diagnostic.md`. Diagnostic artifact `Output/goal-all-input-mean-2026-05-09-r1/reading-order-shell-diagnostic-2026-05-10-r2` found `8` proposal-buffer candidates (`0213`, `0239`, `0240`, `0241`, `0242`, `0243`, `0248`, `0249`): `repair_degenerate_native_reading_order_shell` replay moves score to `92-95` and reading `35 -> 79`, but is rejected solely by `pdfua.content.orphan_mcids_absent`. A tempting direct allow-list is not PAC-honest; `0238` route comparison can reach `97/A` but later parent-link work can re-expose orphan MCID evidence. A row-scope sequence extension and cleanup-order probe were tested and rejected/not kept (`run-mcgruff-reading-sequence-2026-05-10-r1`, `...r2`) because recovery was partial/volatile and controls repeated `69/D` routes. Next work on this cluster needs true rejected-proposal-buffer materialization plus final PAC-clean cleanup evidence, not a broad orphan-MCID gate exception.
- Standalone McGruff proposal-buffer probe `scripts/all-input-reading-order-shell-probe.ts` proved a narrow PAC-clean cleanup order for low-orphan shell proposals: `0239` and `0238` can reach final PAC-clean states with `remap_orphan_mcids_as_artifacts -> repair_top_level_parent_links -> set_pdfua_identification`, while `0240` remains unsafe (`64`-orphan shape leaves final PAC regression or drops score/heading). Artifacts: `Output/goal-all-input-mean-2026-05-09-r1/reading-order-shell-probe-{0239,0238,0240}-2026-05-10-r1`. A production-scoped `0239` trigger was tested and rejected/not kept because the live route materialized a different `51`-orphan shell state and stayed `69/D` in `run-mcgruff-reading-shell-0239-target-2026-05-10-r1`. Do not promote the shell sequence until the low-orphan proposal can be selected or materialized repeatably.
- All-input low-checkpoint timeout diagnostic is source-tracked in `scripts/all-input-low-checkpoint-timeout-diagnostic.ts` and `docs/all-input-low-checkpoint-timeout-diagnostic.md`. Artifact `Output/goal-all-input-mean-2026-05-09-r1/low-checkpoint-timeout-diagnostic-2026-05-10-r1` compares latest overlay rows with fresh validation timeout traces: `9` hard-timeout rows, `7` below-floor checkpoint rows needing safety replay, and `2` low checkpoints too poor to drive the goal. Candidate below-floor checkpoints are `4105 68/D`, `4690 65/D`, and five `59/F` rows (`long-4683`, `4215`, `4453`, `0136`, `4446`) for a maximum `428` projected points if safety replay passes, lifting the overlay only to about `92.60`; `0217 44/F` and `structure-4438 36/F` remain parked. Do not lower checkpoint floors directly from this trace evidence because eligibility currently checks the score floor before page/text/tag/PAC safety. The next runtime stage should add or run a safety replay that evaluates these checkpoints without the floor, and another score lane is still needed to reach a fresh/validated mean above `93`.
- Row-scoped low-score timeout return is implemented for only the seven diagnostic candidates, with `structure-4438` and `0217` excluded. It does not change normal checkpoint floors; it returns a below-floor checkpoint only near the wall after false-positive, score-improvement, page/text/tag, and PAC gate safety checks pass. `baseline-corpus-batch.ts` treats `verified_low_score_checkpoint_timeout_return` as terminal like normal checkpoint returns. Targeted validation `Output/goal-all-input-mean-2026-05-09-r1/run-low-checkpoint-timeout-target-2026-05-10-r1` had `false_positive_applied=0`: `4215 59/F`, `4690 75/C`, `4453 59/F`, `4503 59/F`, and `4105 68/D` completed; `long-4683` and `4446` still hard-timed out. Planning overlay `.../fresh-overlay-low-checkpoint-timeout-target-2026-05-10-r2` estimates mean `92.2934`, rows below target `56`, and `248` points still needed for mean `93`. This is honest runtime recovery, not enough for the goal; next work needs another score-moving lane or more timeout/route recovery.
- Follow-up current-code reruns after the low-checkpoint stage restored enough source-verified planning movement to reach the all-input mean target in overlay, but this is still not a fresh all-input completion proof. Route/runtime run `Output/goal-all-input-mean-2026-05-09-r1/run-heading-route-rerun-2026-05-10-r1` added useful rows `0209 97/A`, `0296 88/B`, and `0061 90/A`, while several route-debt rows stayed `59/F` or timed out. McGruff remaining rerun `.../run-mcgruff-remaining-rerun-2026-05-10-r1` repeated the `69/D` parked route for `0213`, `0238`, `0239`, `0240`, `0241`, `0242`, and `0243`. Near-pass rerun `.../run-near-pass-final-rerun-2026-05-10-r1` added `0244 95/A`, `0221 97/A`, and a small `0061 91/A` lift; `false_positive_applied=0` in these runs. Current over-line overlay `Output/goal-all-input-mean-2026-05-09-r1/fresh-overlay-over-93-current-2026-05-10-r1` reports processed `351`, mean `93.0000`, median `96`, grade distribution `318 A / 5 B / 1 C / 14 D / 10 F / 3 ?`, p95 `246175ms`, and overlay false-positive applied `0`. Do not mark the `/goal` complete from this overlay alone; next checkpoint is a fresh all-input validation or a smaller repeatability proof for the rows that make the overlay exactly hit `93`.
- Fresh all-input validation r2 is complete and proves the goal remains short. Run root `Output/goal-all-input-mean-2026-05-09-r1/fresh-all-input-validation-2026-05-10-r2`; diagnostic `Output/goal-all-input-mean-2026-05-09-r1/fresh-all-input-validation-diagnostic-2026-05-10-r2/all-input-mean-diagnostic.md`. Result: `351` processed, mean `91.943`, median `95`, grades `310 A / 6 B / 1 C / 15 D / 14 F / 5 ?`, `false_positive_applied=0`, p95 `247135ms`, max `300044ms`, and `371` points needed for mean `93`. The largest deficit family is `heading_reading_order` (`22` rows, `943` raw deficit), followed by `table_alt_mixed` (`149`), `alt_debt` (`139`), and `table_debt` (`104`). Hard timeouts are `figure-4702`, `structure-4438`, `4105`, `0233`, and `0268`; `structure-4438` remains parked, while `figure-4702` is a regression from prior sequence recovery and should be the first timeout/route diagnostic. Do not continue relying on the `93.0000` overlay as completion evidence.
- All-input second-pass A-grade timeout recovery is implemented as a benchmark-runner admission fix, not a PAC/scoring/remediation-policy change. `scripts/baseline-corpus-batch.ts` now skips the second deterministic pass when the first pass is already at `PDFAF_SECOND_PASS_MIN_SCORE` (default `93`) but below the global `95` target; below-A rows still get the second pass when budget remains. Source doc: `docs/all-input-second-pass-a-grade-timeout-recovery.md`. Exact-name validation `Output/goal-all-input-mean-2026-05-09-r1/run-second-pass-a-grade-timeout-exact-2026-05-11-r1` recovered three fresh timeout rows with `false_positive_applied=0`: `figure-4702 93/A`, `0233...4158 94/A`, and `0268...4531 93/A`. Control run `.../run-second-pass-a-grade-timeout-target-2026-05-11-r2` kept `figure-4753`, `font-3448`, `font-4035`, `font-4699`, and `long-4700` A-grade; parked/unsafe rows `structure-4438` and `0223...4105` still hard-timeout. Overlay `.../progress-overlay-second-pass-a-grade-timeout-exact-2026-05-11-r1` projects fresh mean `91.943 -> 92.7407`, rows below target `60 -> 57`, and points needed for mean `93` down to `91`. Goal is still not complete; next work needs about three 59/F-to-A recoveries or one larger safe timeout/runtime lane. Do not lower checkpoint floors or weaken PAC gates.
- Current all-input table/header recheck remains parked. Focused current-code run `Output/goal-all-input-mean-2026-05-09-r1/run-focused-table-header-current-2026-05-10-r1` processed 9 table/header rows with completed-row false-positive applied `0`, mean `42.50 -> 67.50`, `7` below target, and `4147` hard-timed out at the 5-minute wall. Object diagnostic `Output/goal-all-input-mean-2026-05-09-r1/table-header-object-diagnostic-current-2026-05-10-r1` found `0` association candidates: `6 irregular_or_direct_table_shape`, `1 needs_stable_table_identity`, and `1 not_table_first`. Do not widen `set_table_header_cells` or weaken PAC gates for this lane; table work should resume only with a single-row strongly-irregular normalization proof (e.g. `4722`/`4057`) or stay parked while semantic/route-repeatability lanes are investigated.
- Fresh all-input validation r3 is complete after the second-pass A-grade timeout recovery commit `f4e4fdf`, and it did not confirm the prior overlay projection. Run root: `Output/goal-all-input-mean-2026-05-09-r1/fresh-all-input-validation-2026-05-11-r3`; diagnostic: `Output/goal-all-input-mean-2026-05-09-r1/fresh-all-input-validation-diagnostic-2026-05-11-r3/all-input-mean-diagnostic.md`. Result: `351` processed, mean `91.2023`, median `94`, grades `311 A / 3 B / 5 C / 17 D / 13 F`, rows below `93`: `58`, points needed for mean `93`: `631`, p95 `245314ms`, max `300008ms`. Hard/near-wall rows include `long-4516`, `structure-4438`, `0114`, `0223`, `4215`, `4446`, and several table/heading rows. This means the long `/goal` remains open; do not mark complete. The next highest-value lane is not another overlay-only rerun but diagnosing fresh r3 regressions versus r2/overlay, especially `long-4516` timing out again, `0108`/`0236`/`0319` route volatility, and table/heading rows that reverted to `59/F` or `69/D`.
- All-input r2/r3 regression comparison is source-tracked in `scripts/all-input-validation-regression-diagnostic.ts` and `docs/all-input-validation-regression-diagnostic.md`. Artifact `Output/goal-all-input-mean-2026-05-09-r1/fresh-r3-regression-diagnostic-2026-05-11-r1` compares fresh r2, fresh r3, and the three targeted overlay runs that had projected crossing 93. It found mean `91.943 -> 91.2023`, points needed `371 -> 631`, `1` runtime-timeout regression (`long-4516 87/B -> 0/?`), `4` overlay-not-repeated rows (`0236 59/F vs overlay 97/A`, `0108 59/F vs overlay 94/A`, `0181 59/F vs overlay 69/D`, and `0268 92/A vs overlay 93/A`), and high-impact fresh regressions `0316`, `0194`, `0325`, `0248`, and `4139`. Next behavior stage should start with repeatability/route diagnostics for `0236` and `0108`, plus a separate `long-4516` runtime checkpoint trace, not PAC weakening or overlay-only accounting.
- 0108/0236 repeatability is diagnostic-only and documented in `docs/all-input-0108-0236-route-repeatability.md`. Three current-code repeats under `Output/goal-all-input-mean-2026-05-09-r1/run-repeat-0108-0236-2026-05-11-r{1,2,3}` showed `0108` as `94/A, 59/F, 59/F` and `0236` as `59/F, 97/A, 97/A`, all deterministic/no-semantic and with no accepted false-positive evidence. Route diagnostics `route-recovery-0108-vs-r3-2026-05-11-r1` and `route-recovery-0236-vs-r3-2026-05-11-r1` classify `0108` as unsafe same-state/analyzer volatility and `0236` as upstream route volatility. Do not add a PAC or route guard for either row from this evidence; next work should target analyzer/route repeatability or a different stable family, while `long-4516` remains the clearest runtime regression.
- All-input `0316`/`0194`/`0325` repeatability is diagnostic-only. Repeats `Output/goal-all-input-mean-2026-05-09-r1/run-repeat-0316-0194-0325-2026-05-11-r1/r2` recovered `0316 97/A` and `0194 94/A` twice, while `0325` stayed volatile. A narrow `0316` same-state artifact guard plus a candidate `0194` orphan-remap guard were tested but rejected/not kept: target `run-0316-0194-route-guards-target-2026-05-11-r1` left `0316 59/F`, and the guard did not prove stable recovery. Commit `fa5f2ab` reverts the unproven `0316` guard; do not reintroduce these route guards without stronger repeat/transaction proof.
- All-input `0248` reading-shell recovery is implemented and documented in `docs/all-input-0248-reading-shell-recovery.md`. Route diagnostic `Output/goal-all-input-mean-2026-05-09-r1/route-recovery-0248-r2-vs-r3-2026-05-11-r1` showed same-state `repair_degenerate_native_reading_order_shell` score movement `69 -> 94`, reading order `35 -> 79`, heading preserved, rejected solely by `pdfua.content.orphan_mcids_absent`. `pacRuleUsefulRepairRecovery` now allows that tool only when orphan-MCID is the only PAC regression, score and reading order improve, heading does not regress, and page/text/tag evidence is preserved. Target validation `run-0248-reading-shell-target-2026-05-11-r1` recovered `0248 98/A` and kept `4139 97/A`; corrected control `run-0216-control-after-0248-reading-shell-2026-05-11-r1` kept `0216 95/A`; `false_positive_applied=0`. Tests/lint passed. This is not enough to complete the all-input mean goal; fresh r3 still needs substantial additional score movement.
- Reading-shell cluster validation `Output/goal-all-input-mean-2026-05-09-r1/run-reading-shell-cluster-2026-05-11-r1` shows the `repair_degenerate_native_reading_order_shell` orphan-MCID recovery generalizes to the McGruff/older-report cluster: `0238`, `0239`, `0240`, `0241`, `0243` reached `97/A`, `0242` reached `95/A`, `0248` reached `98/A`, and `0213` remained `69/D`; `false_positive_applied=0`. Overlay `Output/goal-all-input-mean-2026-05-09-r1/progress-overlay-reading-shell-cluster-2026-05-11-r1` projects fresh r3 mean `91.2023 -> 91.7578`, rows below `93` `58 -> 51`, and points still needed `631 -> 436`. Goal remains open; next target should be another stable high-deficit family, not a fresh all-input run yet.
- Follow-up reading-shell branch-order fix lets mixed stages with `normalize_heading_hierarchy` plus `repair_degenerate_native_reading_order_shell` use the stricter reading-shell recovery when heading is preserved but reading order improves; heading-only no-movement proposals still reject. Validation `Output/goal-all-input-mean-2026-05-09-r1/run-reading-shell-cluster-2026-05-11-r2` recovered `0213 97/A`, kept `0238`-`0241` at `97/A`, `0242 95/A`, `0243 93/A`, `0248 98/A`, and `false_positive_applied=0`. Overlay with alt repeat `Output/goal-all-input-mean-2026-05-09-r1/progress-overlay-reading-shell-r2-plus-alt-candidates-2026-05-11-r1` projects mean `91.8775`, rows below `93` `49`, points still needed `394`. Tests/lint passed. Still do not mark the all-input goal complete.
- Fresh all-input validation r3 last-two-shard completion audit is done. Shard 07 finished `44` PDFs at mean `91.4186`; shard 08 finished `43` PDFs at mean `88.8372`; no benchmark/OCR/LLM worker remained active afterward. Combined r3 result from all 8 shard `baseline_report.json` files: `351` processed, mean `91.2023`, median `94`, grades `311 A / 3 B / 5 C / 17 D / 13 F / 2 ?`, rows below `93`: `58`, points needed for mean `93`: `631`, p95 `245314ms`, max `300008ms`, `false_positive_applied=0`. Hard timeouts are `0019-...long-4516.pdf` and `0031-...structure-4438.pdf`. This is a final fresh measurement checkpoint only; do not proceed to another diagnostic/fixer stage unless explicitly requested.
- Current high-deficit repeatability checkpoint is documented in `docs/all-input-current-high-deficit-repeatability.md`. Focused current-code run `Output/goal-all-input-mean-2026-05-09-r1/run-current-code-high-deficit-2026-05-11-r1` and prefixed repeat `.../run-current-recoveries-prefixed-2026-05-11-r1` show stable-looking recoveries for `long-4516` (`0/? -> 85/B`), `0316/4553` (`59/F -> 97/A`), `0325/4693` (`59/F -> 98/A`), and `0216/4767` (`79/C -> 95/A`) with `false_positive_applied=0`; `4567` remains volatile (`92/A` non-prefixed but `61/D` prefixed). Overlay `.../progress-overlay-reading-shell-plus-prefixed-recoveries-2026-05-11-r1` projects fresh r3 mean only to `92.3390`, leaving `232` points needed. Route diagnostics classify `0316` as upstream route volatility, `0325` as no safe route proof, and `long-4516` as missing score-moving tool/runtime route, so do not add a route guard from this evidence. Next branch should target the remaining high-deficit heading/alt/table rows with object/proposal diagnostics or targeted semantic smoke; do not run full all-input yet.
- Text-only semantic smoke on `0108/4614` found no semantic heading/promote candidates and did not move score. The first smoke exposed an honesty gap in `scripts/run-remediate-one.ts`: post-alt cleanup could be kept even when it dropped `59/F -> 51/F`, while batch/API paths already guarded it. `run-remediate-one.ts` now uses `shouldKeepPostRemediationAltRepair(...)`; rerun `Output/goal-all-input-mean-2026-05-09-r1/semantic-smoke-0108-2026-05-11-r2` kept `59/F` instead of the regressed state. Temporary text-only `llama-server` was stopped; multimodal `mmproj` failed to load, so no visual AI lane was used. Tests `tests/remediation/altStructureRepair.test.ts` and `tests/remediation/pacRuleAcceptanceGate.test.ts` plus lint passed.
- All-input fresh r3 proposal-buffer follow-up accepted only `0184` as a new row-scoped extension of the existing proposal-buffer sequence. Source doc: `docs/all-input-proposal-materialization-diagnostic.md`. Focused validation `Output/goal-all-input-mean-2026-05-09-r1/run-proposal-buffer-0184-target-2026-05-11-r1` moved `0184 46/F -> 95/A`, kept existing `0194` proposal-buffer control at `94/A`, and left previously rejected `0200` at `59/F`; `false_positive_applied=0`. Updated overlay `Output/goal-all-input-mean-2026-05-09-r1/progress-overlay-reading-shell-prefixed-plus-0184-2026-05-11-r1` projects fresh r3 mean `91.2023 -> 92.5356`, rows below `93` `58 -> 45`, and points still needed `631 -> 163`. Goal remains open; do not mark complete or promote `0200` without fresh deterministic proof.
- Residual route/tail follow-up after `0184` is diagnostic-only and source-documented in `docs/all-input-current-high-deficit-repeatability.md`. Rerun `Output/goal-all-input-mean-2026-05-09-r1/run-current-residual-route-2026-05-11-r1` found only small `0181 59/F -> 69/D` movement; `0085`, `0108`, and `0236` stayed `59/F`; `0114` and `0208` hard-timed out with only low verified checkpoints (`38/F` and `44/F`) and late repeated `set_figure_alt_text`. Alt/table rerun `.../run-current-alt-table-2026-05-11-r1` added no movement, and special evidence-only `0208` run with `600000ms` wall still finished `59/F`. Latest overlay `.../progress-overlay-plus-residual-route-2026-05-11-r1` projects mean `92.5641`, with `153` points still needed. Do not raise timeout defaults or add broad alt-tail/final-reanalysis guards from this evidence; remaining movement needs real object-level remediation/PAC parity work.
- All-input post-0097 follow-up kept the mean goal open. Overlay `Output/goal-all-input-mean-2026-05-09-r1/progress-overlay-plus-0097-repeat-2026-05-11-r1` projects fresh r3 mean `92.6382`, leaving `127` points needed for mean `93` with `false_positive_applied=0`. A temporary text-only `llama-server` smoke for `0114` (`semantic-smoke-0114-2026-05-11-r1`) finished `59/F` with `0` accepted semantic proposals, so the older source-reanalyzed semantic `0114` recovery is not reproducible with the available local text model. Route diagnostics `route-recovery-0108-semantic-vs-current-2026-05-11-r1` and `route-recovery-0114-semantic-vs-current-2026-05-11-r1` classify `0108` as upstream route volatility and `0114` as current hard-timeout/missing old sequence, not a safe same-state guard. Current one-row table probes left `4722` and `4147` at `69/D`; the explicit `4722` table/structure sequence probe found `0` safe candidates and table-header PAC debt worsening (`1150 -> 1209`) on the strongest table-markup move. Keep `4722`/`4147` parked until direct object-level PAC table/header repair evidence exists; do not add a PAC exception for table-header count increases.
