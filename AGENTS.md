# Repository Working Rules

- Commit and push after every major completed change.
- Do not include local verification artifacts in commits unless they are explicitly required source assets.
- Keep PDF payloads and generated Base64 content out of logs, docs, and commits.
- Before starting any local LLM instance or benchmark path that can spawn one, first check whether an existing LLM process or listener is already running, such as `llama-server` on the expected port. Reuse the existing instance when possible and do not start a second copy on the same machine unless explicitly required.

# Stage Notes

- Stage 43C is provisionally paused: table normalization gains are useful and speed is healthy, but protected-row preservation still needs refinement before acceptance.
- Stage 44 is now frozen as a provisional checkpoint rather than an accepted gate-passing stage. The best measured full run remains `Output/experiment-corpus-baseline/run-stage44.8-full-2026-04-23-r1`: mean `89.36`, median `95`, grades `31 A / 11 B / 1 C / 3 D / 4 F`, attempts `850`, false-positive applied `0`.
- Stage 44 provisional debt is now tracked as:
  - protected-row instability on `fixture-teams-original` and `fixture-teams-remediated`
  - runtime tail on `structure-4076` and `structure-4438`
  - Stage 41 still failing on protected regressions and p95
- Stage 45 cleanup/stabilization is now provisionally implemented on top of the frozen checkpoint. Reference run: `Output/experiment-corpus-baseline/run-stage45-full-2026-04-23-r2` with mean `89.24`, median `95`, grades `31 A / 10 B / 2 C / 3 D / 4 F`, attempts `799`, false-positive applied `0`, and p95 effectively flat versus the freeze checkpoint.
- Stage 45 keeps the same deferred debt:
  - protected-row instability on `fixture-teams-original` and `fixture-teams-remediated`
  - runtime tail on `structure-4076` and `structure-4438`
- Stage 46A is now diagnostic-only. Broad runtime-tail suppression was not safe to keep; use `scripts/stage46-runtime-regression-isolation.ts` and `docs/stage46-runtime-regression-isolation.md` to compare Stage 45 against any new runtime candidate before reintroducing guard behavior.
- Stage 46B was rejected as a no-op candidate: the narrow annotation guard did not materially improve `structure-4438` and regressed `figure-4188`, `structure-4076`, `fixture-teams-targeted-wave1`, and `long-4683`; do not reintroduce it without new diagnostic evidence.
- Stage 47 is Teams-only protected parity. Keep it limited to `fixture-teams-original`, `fixture-teams-remediated`, and `fixture-teams-targeted-wave1`; no broad protected replay, runtime suppression, scorer changes, or new repair breadth.
- Stage 47 attempt r1 was rejected and reverted: quarantining Teams `repair_alt_text_structure` preserved reading order by sacrificing alt recovery, dropping `fixture-teams-remediated` to `80/B`. Do not retry that pattern; the next Stage 47 attempt needs a different mechanism that can preserve both alt and reading order.
- Stage 47A is diagnostic-only. `pnpm run benchmark:teams-parity` showed no same-run floor-reaching state for `fixture-teams-original` or `fixture-teams-remediated` in the compared Stage 45 / Stage 47 r1 artifacts, so do not force Stage 47 acceptance with more protected-row heuristics unless new evidence shows a safe state exists.
- Stage 48A is diagnostic-only. Use `scripts/stage48-df-tail-diagnostic.ts` and `docs/stage48-df-tail-diagnostic.md` to classify the Stage 45 D/F tail before adding new fixer behavior. The first diagnostic recommended `figure_alt_tail`, but the initial figure-like role promotion experiment did not improve the target D/F rows and regressed `structure-4076`, so it was reverted.
- The Stage 45/48 D/F tail set is parked for now. A new v1-derived edge-case corpus has been copied locally under `Input/from_sibling_pdfaf_v1_edge_mix/` to broaden testing beyond the current 50-file corpus before resuming fixer work.
- Stage 49 edge-mix baseline is complete. Run directory: `Output/from_sibling_pdfaf_v1_edge_mix/run-stage49-baseline-2026-04-24-r1`; result: 12/12 completed, mean `42.08 -> 75.00`, grades after `4 A / 2 B / 0 C / 1 D / 5 F`, attempts `219`, false-positive applied `0`. Dominant residual families: `figure_alt_tail: 5`, `mixed_tail: 2`, `zero_heading_tail: 1`. Stage 50 should be `Figure/Alt Recovery v3` and should target v2 residuals, not v1 scores.
- Stage 50 Figure/Alt Recovery v3 is implemented as an edge-mix fixer candidate, not a full 50-file accepted stage yet. Diagnostic output `Output/from_sibling_pdfaf_v1_edge_mix/stage50-figure-diagnostic-2026-04-24-r1` confirmed role-map/checker-visible figure mismatch on `4145` and `4139`; target run `Output/from_sibling_pdfaf_v1_edge_mix/run-stage50-target-edge-mix-2026-04-24-r3` improved the 12-file edge mix to mean `82.75`, grades `5 A / 3 B / 1 C / 0 D / 3 F`, attempts `215`, false-positive applied `0`. Sensitive 50-file subset still shows known protected-row volatility unrelated to `retag_as_figure` (`structure-4076`, `long-4683`, Teams wave1), so do not treat Stage 50 as Stage 41/full-corpus accepted until that debt is resolved or explicitly waived.
- Stage 51 acceptance isolation is diagnostic-only. Report `Output/experiment-corpus-baseline/stage51-stage50-acceptance-isolation-2026-04-24-r2` found `0` Stage50-specific regressions, `3` legacy protected-volatility regressions, and `0` sensitive rows with `retag_as_figure` scheduled. Decision: Stage 50 is edge-mix accepted / full-corpus provisional. Next default direction is Stage 52A zero-heading tail recovery for edge-mix rows `4139`, `4567`, and `4215`; only return to protected parity if full-corpus acceptance is explicitly prioritized.
- Stage 52A is diagnostic-only for now. Report `Output/from_sibling_pdfaf_v1_edge_mix/stage52a-heading-tail-diagnostic-2026-04-24-r1` found hidden root-reachable heading evidence on `v1-4139` and `v1-4215`, plus unscheduled create-heading recovery on `v1-4567`. A first analysis-parity fix lifted target rows in one run but destabilized `v1-4139` / `v1-4683` across reruns, so it was not kept. Next work should either add a transaction-safe heading parity mechanism or move to another edge-mix fixer family; do not commit global heading-scorer parity without a stable edge-mix target run.
