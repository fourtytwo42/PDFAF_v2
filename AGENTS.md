# Repository Working Rules

- Commit and push after every major completed change.
- Do not include local verification artifacts in commits unless they are explicitly required source assets.
- Keep PDF payloads and generated Base64 content out of logs, docs, and commits.
- Before starting any local LLM instance or benchmark path that can spawn one, first check whether an existing LLM process or listener is already running, such as `llama-server` on the expected port. Reuse the existing instance when possible and do not start a second copy on the same machine unless explicitly required.

# Stage Notes

- Stage 43C is provisionally paused: table normalization gains are useful and speed is healthy, but protected-row preservation still needs refinement before acceptance.
- Stage 44 is now frozen as a provisional checkpoint rather than an accepted gate-passing stage. The best measured full run remains `Output/experiment-corpus-baseline/run-stage44.8-full-2026-04-23-r1`: mean `89.36`, median `95`, grades `31 A / 11 B / 1 C / 3 D / 4 F`, attempts `850`, false-positive applied `0`.
- Stage 44 provisional debt is now tracked as:
  - protected-row instability on `fixture-teams-original` and `fixture-teams-remediated`
  - runtime tail on `structure-4076` and `structure-4438`
  - Stage 41 still failing on protected regressions and p95
- Stage 45 cleanup/stabilization is now provisionally implemented on top of the frozen checkpoint. Reference run: `Output/experiment-corpus-baseline/run-stage45-full-2026-04-23-r2` with mean `89.24`, median `95`, grades `31 A / 10 B / 2 C / 3 D / 4 F`, attempts `799`, false-positive applied `0`, and p95 effectively flat versus the freeze checkpoint.
- Stage 45 keeps the same deferred debt:
  - protected-row instability on `fixture-teams-original` and `fixture-teams-remediated`
  - runtime tail on `structure-4076` and `structure-4438`
- Stage 46A is now diagnostic-only. Broad runtime-tail suppression was not safe to keep; use `scripts/stage46-runtime-regression-isolation.ts` and `docs/stage46-runtime-regression-isolation.md` to compare Stage 45 against any new runtime candidate before reintroducing guard behavior.
- Stage 46B was rejected as a no-op candidate: the narrow annotation guard did not materially improve `structure-4438` and regressed `figure-4188`, `structure-4076`, `fixture-teams-targeted-wave1`, and `long-4683`; do not reintroduce it without new diagnostic evidence.
- Stage 47 is Teams-only protected parity. Keep it limited to `fixture-teams-original`, `fixture-teams-remediated`, and `fixture-teams-targeted-wave1`; no broad protected replay, runtime suppression, scorer changes, or new repair breadth.
- Stage 47 attempt r1 was rejected and reverted: quarantining Teams `repair_alt_text_structure` preserved reading order by sacrificing alt recovery, dropping `fixture-teams-remediated` to `80/B`. Do not retry that pattern; the next Stage 47 attempt needs a different mechanism that can preserve both alt and reading order.
- Stage 47A is diagnostic-only. `pnpm run benchmark:teams-parity` showed no same-run floor-reaching state for `fixture-teams-original` or `fixture-teams-remediated` in the compared Stage 45 / Stage 47 r1 artifacts, so do not force Stage 47 acceptance with more protected-row heuristics unless new evidence shows a safe state exists.
- Stage 48A is diagnostic-only. Use `scripts/stage48-df-tail-diagnostic.ts` and `docs/stage48-df-tail-diagnostic.md` to classify the Stage 45 D/F tail before adding new fixer behavior. The first diagnostic recommended `figure_alt_tail`, but the initial figure-like role promotion experiment did not improve the target D/F rows and regressed `structure-4076`, so it was reverted.
- The Stage 45/48 D/F tail set is parked for now. A new v1-derived edge-case corpus has been copied locally under `Input/from_sibling_pdfaf_v1_edge_mix/` to broaden testing beyond the current 50-file corpus before resuming fixer work.
- Stage 49 edge-mix baseline is complete. Run directory: `Output/from_sibling_pdfaf_v1_edge_mix/run-stage49-baseline-2026-04-24-r1`; result: 12/12 completed, mean `42.08 -> 75.00`, grades after `4 A / 2 B / 0 C / 1 D / 5 F`, attempts `219`, false-positive applied `0`. Dominant residual families: `figure_alt_tail: 5`, `mixed_tail: 2`, `zero_heading_tail: 1`. Stage 50 should be `Figure/Alt Recovery v3` and should target v2 residuals, not v1 scores.
- Stage 50 Figure/Alt Recovery v3 is implemented as an edge-mix fixer candidate, not a full 50-file accepted stage yet. Diagnostic output `Output/from_sibling_pdfaf_v1_edge_mix/stage50-figure-diagnostic-2026-04-24-r1` confirmed role-map/checker-visible figure mismatch on `4145` and `4139`; target run `Output/from_sibling_pdfaf_v1_edge_mix/run-stage50-target-edge-mix-2026-04-24-r3` improved the 12-file edge mix to mean `82.75`, grades `5 A / 3 B / 1 C / 0 D / 3 F`, attempts `215`, false-positive applied `0`. Sensitive 50-file subset still shows known protected-row volatility unrelated to `retag_as_figure` (`structure-4076`, `long-4683`, Teams wave1), so do not treat Stage 50 as Stage 41/full-corpus accepted until that debt is resolved or explicitly waived.
- Stage 51 acceptance isolation is diagnostic-only. Report `Output/experiment-corpus-baseline/stage51-stage50-acceptance-isolation-2026-04-24-r2` found `0` Stage50-specific regressions, `3` legacy protected-volatility regressions, and `0` sensitive rows with `retag_as_figure` scheduled. Decision: Stage 50 is edge-mix accepted / full-corpus provisional. Next default direction is Stage 52A zero-heading tail recovery for edge-mix rows `4139`, `4567`, and `4215`; only return to protected parity if full-corpus acceptance is explicitly prioritized.
- Stage 52A is diagnostic-only. Report `Output/from_sibling_pdfaf_v1_edge_mix/stage52a-heading-tail-diagnostic-2026-04-24-r1` found hidden root-reachable heading evidence on `v1-4139` and `v1-4215`, plus unscheduled create-heading recovery on `v1-4567`. The global analysis/scorer parity experiment was not kept because it changed routing and destabilized edge-mix rows.
- Stage 52B final-only hidden-heading parity is implemented as an edge-mix reporting/scoring adjustment only. Simulation report `Output/from_sibling_pdfaf_v1_edge_mix/stage52b-hidden-heading-parity-2026-04-24-r1` applies only structured JSON root-reachable heading evidence and moves the Stage 50 edge-mix artifact from mean `82.75` to `88.00`, grades `6 A / 4 B / 1 C / 0 D / 1 F`, with no applied-tool inflation. Fresh run `Output/from_sibling_pdfaf_v1_edge_mix/run-stage52b-target-edge-mix-2026-04-24-r2` reached mean `90.75`, grades `8 A / 2 B / 2 C / 0 D / 0 F`, attempts `222`, false-positive applied `0`; `v1-4683` still shows run-to-run alt volatility unrelated to the final-only parity helper. Do not convert Stage 52B into planner/analyzer/scorer global parity.
- Stage 53 is diagnostic-only. Report `Output/from_sibling_pdfaf_v1_edge_mix/stage53-edge-mix-volatility-2026-04-24-r3` compares Stage 50 and Stage 52B repeat artifacts and confirms edge-mix volatility happens before final hidden-heading parity in remediation tool sequences. The dominant signature (`remap_orphan_mcids_as_artifacts` appearing where missing before) has mixed score direction, so no Stage 53 behavior guard is safe to keep. Next fixer work should treat `v1-4683` / `v1-4139` / `v1-4567` volatility as unresolved mutator-path debt, not a Stage 52B parity issue.
- Stage 54A is diagnostic-only. Report `Output/from_sibling_pdfaf_v1_edge_mix/stage54a-edge-mix-replay-2026-04-24-r1` compares Stage 50 and Stage 52B repeat artifacts at tool-timeline level. It found no same-state accept/reject bug and no same-state ordering bug; current artifacts mostly lack pre-tool state signatures, so divergences are classified as `inconclusive_missing_state` or final-parity-only. Do not add a suppression/transaction guard from this evidence. The next stable path is either richer pre-tool state instrumentation or a narrow real fixer, likely `v1-4683` figure/table/alt mixed tail.
- Stage 54B is instrumentation-only. Tool rows now include compact `debug.replayState` state signatures for replay diagnostics without changing routing, scoring, mutation, or acceptance decisions. Repeat runs `Output/from_sibling_pdfaf_v1_edge_mix/run-stage54b-edge-mix-r1` and `run-stage54b-edge-mix-r2` completed with false-positive applied `0` and replay coverage on all prioritized volatile tools. Report `Output/from_sibling_pdfaf_v1_edge_mix/stage54b-edge-mix-replay-2026-04-24-r1` can classify Stage54B repeat divergences as `different_state_same_tool` instead of missing-state-only; comparisons to older Stage50/52B artifacts remain partly inconclusive because those older rows lack replay signatures. Do not infer suppression from Stage54B alone; use it to decide Stage 54C versus Stage 55 after a fully instrumented comparison set.
- Stage 54C is diagnostic-only. Report `Output/from_sibling_pdfaf_v1_edge_mix/stage54c-instrumented-replay-2026-04-24-r1` compares fully instrumented repeats `run-stage54b-edge-mix-r1`, `run-stage54b-edge-mix-r2`, and `run-stage54c-edge-mix-r3`; all 214 rows in r3 include replay signatures and false-positive applied remains `0`. The report found one focus-row same-state decision divergence (`v1-4139`, `canonicalize_figure_alt_ownership` rejected vs no_effect from identical replay state) plus broader different-upstream-state drift on `v1-4683` and `v1-4567`. Next default stage is Stage 55 acceptance-determinism cleanup for same-state outcome handling; do not select another v1 batch until this edge-mix volatility is either stabilized or explicitly parked.
- Stage 55 fixed the Stage 54C same-state figure-alt/orphan acceptance instability without adding new mutators. It skips broad `canonicalize_figure_alt_ownership` when precise `retag_as_figure` is already scheduled from a zero checker-visible-figure state, and rejects no-score-gain orphan remap mutations that lack checker-facing structural benefit. Accepted repeats: `Output/from_sibling_pdfaf_v1_edge_mix/run-stage55-edge-mix-r6` (mean `85.83`, grades `6 A / 3 B / 1 C / 0 D / 2 F`) and `run-stage55-edge-mix-r7` (mean `88.42`, grades `7 A / 3 B / 1 C / 0 D / 1 F`), both with false-positive applied `0`. Replay `Output/from_sibling_pdfaf_v1_edge_mix/stage55-instrumented-replay-2026-04-24-r3` reports `move_to_real_fixer`: focus rows no longer show same-state decision/order bugs, so the next stage should stop guard work and target a real fixer, preferably `v1-4683` mixed figure/table/alt tail.
- Stage 56 is diagnostic-only. Report `Output/from_sibling_pdfaf_v1_edge_mix/stage56-mixed-tail-diagnostic-2026-04-24-r1` compares Stage 55 repeats and finds one harmful focus-row initial-analysis variance: `v1-4683` swings `59/F -> 92/A` before the first mutator, with `v1-4139` and `v1-4567` showing replay-state drift but no score movement. Next default stage is Stage 56B analysis/runner determinism for mixed figure/table/alt inputs, not another mutation guard or filename-specific fixer.
- Stage 56B is diagnostic-only. Report `Output/from_sibling_pdfaf_v1_edge_mix/stage56b-analysis-repeat-2026-04-24-r1` attributes the repeat variance to Python structural analysis output while pdf.js signatures remain stable. The stable-object-key traversal experiment (`stage56b-analysis-repeat-2026-04-24-r2`, `run-stage56b-edge-mix-r1/r2`) was not kept because it stabilized analysis at a lower-quality point and regressed edge-mix rows such as `v1-4139` and `v1-4683`. Do not reapply that strict dedup change without a quality-preserving canonicalization design.
- Stage 57 second v1 edge-mix baseline is diagnostic/selection-only. New local corpus path: `Input/from_sibling_pdfaf_v1_edge_mix_2/` with PDFs ignored by git. Baseline run `Output/from_sibling_pdfaf_v1_edge_mix_2/run-stage57-baseline-2026-04-24-r1` completed 16/16 rows: mean `39.88 -> 84.56`, median `37.5 -> 90.5`, grades after `8 A / 4 B / 0 C / 2 D / 2 F`, attempts `227`, false-positive applied `0`. Dominant residual distribution: `figure_alt_tail: 3`, `mixed_tail: 2`, `table_tail: 1`, `zero_heading_tail: 2`; selected next fixer family from v2 evidence is `Figure/Alt Recovery v3` / follow-up figure-alt work, but the worst-row repeat diagnostic below should be accounted for first.
- Stage 57 analysis-repeat on worst rows is diagnostic-only. Report `Output/from_sibling_pdfaf_v1_edge_mix_2/stage57-analysis-repeat-2026-04-24-r1` found Python structural variance again, harmful on `v1-4722` (`42-59`) and `v1-4171` (`40-45`), non-harmful on `v1-4758`, and stable on manual/scanned Fs `v1-3479` and `v1-3507`. Stage 58 should either design quality-preserving Python structural canonicalization before new mutators, or explicitly proceed with a narrow figure-alt follow-up while treating Python analyzer volatility as parked diagnostic debt.
- Stage 58 is diagnostic-only. Structural boundary reports `Output/from_sibling_pdfaf_v1_edge_mix/stage58-structural-boundary-diagnostic-2026-04-24-r1` and `Output/from_sibling_pdfaf_v1_edge_mix_2/stage58-structural-boundary-diagnostic-2026-04-24-r1` show the harmful variance is non-canonicalizable count/drop or page/ref/text mismatch, not pure ordering or exact duplicate ordering. Do not add the rejected strict traversal/dedup canonicalization from this evidence. Park analyzer volatility as unresolved Python structural output debt; the next default stage is Stage 59 `Figure/Alt Recovery v4` for the fresh Stage 57 residuals `v1-4758`, `v1-4700`, and `v1-4699`.
- Stage 59 Figure/Alt Recovery v4 is implemented as a narrow general figure-alt follow-up. Diagnostic `Output/from_sibling_pdfaf_v1_edge_mix_2/stage59-figure-diagnostic-2026-04-24-r1` confirmed low checker-visible figure-alt coverage plus score-shape figure rejections on `v1-4758`, `v1-4700`, and `v1-4699`. The implementation keeps invariant-backed multi-target `set_figure_alt_text` progress when checker-visible figure-alt ownership improves despite a small temporary score dip, and uses deterministic non-weak placeholders (`Illustration (page N)`) instead of generic `Image`. Target run `Output/from_sibling_pdfaf_v1_edge_mix_2/run-stage59-target-edge-mix2-2026-04-24-r3` improved all three primary rows (`4758: 80->81`, `4700: 83->84`, `4699: 83->90`) with false-positive applied `0` and controls stable. Full edge-mix 2 repeat `run-stage59-edge-mix2-2026-04-24-r2` reached mean `83.81`, grades `9 A / 3 B / 0 C / 1 D / 3 F`, attempts `228`, false-positive applied `0`; the extra F is `v1-4171`, already documented Stage 57/58 Python structural volatility. First edge-mix regression run `Output/from_sibling_pdfaf_v1_edge_mix/run-stage59-edge-mix-2026-04-24-r1` reached mean `86.08`, grades `7 A / 2 B / 1 C / 0 D / 2 F`, attempts `215`, false-positive applied `0`.
- Stage 60 is diagnostic/decision-only. Report `Output/from_sibling_pdfaf_v1_edge_mix_2/stage60-volatility-decision-2026-04-24-r2` found `0` Stage59-specific regression rows, `3` parked analyzer-debt rows (`v1-4171`, `v1-4487`, `v1-4683`), and `0` inconclusive rows after a focused analysis repeat for `v1-4487`. Decision: `park_analyzer_volatility_and_continue`. Stage 61 should use stable residual rows only; the best next default is a narrow `Heading Scheduling Tail v3` around stable zero-heading debt such as `v1-4567`, with `v1-4722` table debt as a secondary candidate. Do not build new fixers on parked analyzer-volatility rows unless analyzer determinism is explicitly resumed.
- Stage 61 is diagnostic-only. Report `Output/from_sibling_pdfaf_v1_edge_mix/stage61-heading-scheduling-diagnostic-2026-04-24-r1` found `v1-4567` still has `heading_structure=0` and strong reading order, but it has `paragraphStructElemCount=0`, `eligibleCandidateCount=0`, and `create_heading_from_candidate` has no safe ranked target. Do not add a heading scheduler/planner broadening from this evidence. Next default stage should be Stage 62 `Table Tail Follow-up v2`, focused on stable table debt such as `v1-4722`, while keeping parked analyzer-volatility rows out of fixer acceptance.
- Stage 62 Table Tail Follow-up v2 is implemented as a narrow general strongly-irregular-table repair. Diagnostic `Output/from_sibling_pdfaf_v1_edge_mix_2/stage62-table-tail-diagnostic-2026-04-24-r1` showed `v1-4722` had `table_markup=0`, valid headers/direct cells, but `stronglyIrregularTableCount=9`, so planner now schedules `normalize_table_structure` for low-score strongly irregular dense tables and Python pads short table rows with empty structural cells only when a dominant column count is clear. Target run `Output/from_sibling_pdfaf_v1_edge_mix_2/run-stage62-target-table-2026-04-24-r1` moved `v1-4722` table markup `0 -> 16` with invariant-backed irregular-row reduction and false-positive applied `0`; `v1-4178` improved to `98/A`. Full edge-mix 2 run `run-stage62-edge-mix2-2026-04-24-r1` reached mean `85.75`, grades `9 A / 3 B / 1 C / 1 D / 2 F`, attempts `227`, false-positive applied `0`. First edge-mix run `run-stage62-edge-mix-2026-04-24-r1` reached mean `86.42`, grades `8 A / 1 B / 1 C / 0 D / 2 F`, attempts `216`, false-positive applied `0`; `v1-4567` remains volatile but no Stage 62 table tools fired on it. Next default stage should target the remaining stable deterministic residuals rather than broad heading/table retries.
- Stage 63 is diagnostic/selection-only. Report `Output/from_sibling_pdfaf_v1_edge_mix_2/stage63-residual-decision-2026-04-24-r1/stage63-residual-decision.md` compared Stage 59 vs Stage 62 across both edge-mix corpora and made no remediation behavior changes. Distribution: `figure_alt_residual: 6`, `table_followup_possible: 1`, `mixed_no_safe_target: 2`, `manual_scanned_debt: 2`, `analyzer_volatility: 3`, `resolved_or_not_low: 14`. Parked analyzer-volatility rows remain `v1-4683`, `v1-4171`, and `v1-4487`; manual/scanned debt remains `v1-3479` and `v1-3507`; mixed/no-safe rows include `v1-4139` and `v1-4567`. Selected Stage 64 direction is `Figure/Alt Recovery v5`, using stable low-alt rows with figure-tool evidence rather than parked volatility, manual/scanned rows, or broad table/heading retries.
- Stage 64 Figure/Alt Recovery v5 is implemented as a narrow bounded multi-target alt progression fix. Diagnostics `Output/from_sibling_pdfaf_v1_edge_mix/stage64-figure-diagnostic-2026-04-24-r1` and `Output/from_sibling_pdfaf_v1_edge_mix_2/stage64-figure-diagnostic-2026-04-24-r1` showed multiple reachable checker-visible `/Figure` nodes still missing `/Alt`; the fixer now skips already-attempted figure targets, can apply up to three distinct `set_figure_alt_text` targets in one stage, and preserves invariant-backed checker-visible alt gains despite structural-confidence shape drift. It also normalizes invariant-contradicting recorded `applied` rows to `no_effect` to preserve false-positive-applied truthfulness. Target runs `run-stage64-target-figure-alt-2026-04-24-r2` improved `v1-3921` (`84/B -> 91/A`), `v1-4145` (`59/F -> 78/C`), and `v1-4758` (`81/B -> 90/A`) with false-positive applied `0`. Full runs: edge mix 1 `run-stage64-edge-mix-2026-04-24-r5` mean `88.33`, grades `9 A / 0 B / 1 C / 1 D / 1 F`, attempts `226`, false-positive applied `0`; edge mix 2 `run-stage64-edge-mix2-2026-04-24-r2` mean `85.38`, grades `10 A / 2 B / 0 C / 2 D / 2 F`, attempts `232`, false-positive applied `0`. Next roadmap stage is Stage 65 repeatability/reclassification across both edge-mix corpora.
- Stage 65 is diagnostic/selection-only. Report `Output/from_sibling_pdfaf_v1_edge_mix_2/stage65-repeatability-decision-2026-04-24-r1/stage65-repeatability-decision.md` compared Stage 62, Stage 64, and two fresh repeats per edge-mix corpus. Stage 64 gains repeated on `v1-3921`, `v1-4145`, and `v1-4758`; all configured runs had false-positive applied `0`. Repeat summaries: edge mix 1 r1 mean `86.67`, grades `9 A / 0 B / 1 C / 0 D / 2 F`, attempts `227`; edge mix 1 r2 mean `88.00`, grades `9 A / 0 B / 2 C / 0 D / 1 F`, attempts `225`; edge mix 2 r1 mean `85.13`, grades `10 A / 2 B / 0 C / 1 D / 3 F`, attempts `235`; edge mix 2 r2 mean `85.38`, grades `10 A / 2 B / 0 C / 2 D / 2 F`, attempts `236`. The report parks `7` analyzer-volatility rows, including previously parked `v1-4683`, `v1-4171`, `v1-4487` plus repeat-swing rows `v1-4122`, `v1-4139`, `v1-4215`, and `v1-4567`; manual/scanned debt remains `v1-3479` and `v1-3507`. Selected Stage 66 direction is `Analyzer Volatility Design`; do not add another structural fixer until the repeat instability is addressed or explicitly waived.
- Stage 66 is diagnostic/design-only. Report `Output/from_sibling_pdfaf_v1_edge_mix_2/stage66-analyzer-volatility-design-2026-04-24-r1/stage66-analyzer-volatility-design.md` combines Stage 65 repeats, Stage 56B/57 analysis-repeat artifacts, Stage 58 structural-boundary artifacts, and a focused `v1-4487` structural-boundary run. It classifies all `7` volatile rows (`v1-4122`, `v1-4139`, `v1-4171`, `v1-4215`, `v1-4487`, `v1-4567`, `v1-4683`) as `python_structural_drop_or_count_variance` with non-canonicalizable boundary evidence; `0` rows are order-only/canonicalizable and `0` rows are remediation-path variance. Manual/scanned debt remains `v1-3479` and `v1-3507`. Decision: keep analyzer volatility parked as non-canonicalizable analyzer debt and return only to stable residual fixer selection unless a future dedicated analyzer design stage can preserve best-quality evidence without broad dropping/deduping.
- Stage 67 is diagnostic/selection-only. Report `Output/from_sibling_pdfaf_v1_edge_mix_2/stage67-stable-residual-selection-2026-04-24-r1/stage67-stable-residual-selection.md` compares Stage 62, Stage 64, Stage 65 repeats, and Stage 66 analyzer classifications without changing remediation behavior. Distribution: `resolved_or_high: 12`, `stable_figure_alt_residual: 6`, `stable_table_residual: 1`, `excluded_analyzer_volatility: 7`, `excluded_manual_scanned: 2`. Repeated Stage 64 gains remain preserved on `v1-3921`, `v1-4145`, and `v1-4758`; analyzer-volatility rows (`v1-4122`, `v1-4139`, `v1-4171`, `v1-4215`, `v1-4487`, `v1-4567`, `v1-4683`) and manual/scanned rows (`v1-3479`, `v1-3507`) stay excluded from fixer acceptance. Selected Stage 68 direction is `Table Tail Follow-up v3`, focused on stable `v1-4722` only if a new invariant-backed table improvement path is identified rather than more retries.
- The long-range acceptance plan is documented in `docs/stage64-71-engine-v2-acceptance-roadmap.md`. Use it as the default path: stable structural gains, repeatability, analyzer-volatility decision, legacy reconciliation, then the `Engine v2 General Acceptance` end gate.
